"""Wave-0 fixtures package for Phase 3 extractor tests.

Holds golden / positive / negative Python source samples. Plans 02-06 add their
own fixture strings (class-diagram, sequence, component, package, FSM, spec)
here. Empty in this plan — only the package marker is created so later plans can
``from tests.unit.extractors.fixtures import <sample>``.
"""
