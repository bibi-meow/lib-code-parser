// Doxygen contract extraction: mix of @-form and \-form commands (SPC-03).
/**
 * Compute a normalized score.
 *
 * @pre x > 0
 * \post r >= 0
 * \invariant state ok
 *
 * Traces: REQ-9, US-3
 */
int compute_score(int x) {
    return x;
}
