---
phase: 03-python-diagram-spec-extractors
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - lib_code_parser/extractors/evaluations/__init__.py
  - lib_code_parser/extractors/evaluations/class_diagram.py
  - lib_code_parser/extractors/evaluations/component_diagram.py
  - lib_code_parser/extractors/evaluations/package_diagram.py
  - lib_code_parser/_dispatch.py
  - tests/unit/extractors/test_class_diagram.py
  - tests/unit/extractors/test_component_diagram.py
  - tests/unit/extractors/test_package_diagram.py
  - tests/unit/extractors/test_diagram_schema.py
  - tests/unit/test_dispatch.py
  - tests/acceptance/test_dia01_class_diagram.py
  - tests/acceptance/test_dia03_component_diagram.py
  - tests/acceptance/test_dia04_package_diagram.py
  - tests/unit/extractors/fixtures/dia_structural.py
autonomous: true
requirements: [DIA-01, DIA-03, DIA-04, DIA-07]
user_setup: []

must_haves:
  truths:
    - "Caller extracts a class diagram with inherits + composes(direct type) + aggregates(Optional/list) + associates(undecidable fallback) edges; never an undocumented 'uses' edge"
    - "D-03: class/component edges are emitted in this lib's own strict vocabulary (inherits/composes/imports) — they are NOT renamed to the sibling lib-diagram-parser spelling (inheritance/dependency); the physical↔logical vocabulary gap is resolved by the verifier, not by this extractor"
    - "Caller extracts a component diagram: module GraphNodes + GraphEdge(edge_type='imports') from import-derived TypeDeps"
    - "D-05: package diagram emits GraphNode(node_type='package') as a plain str value — since the sibling lib's node_type is plain str, this needs NO sibling-lib code change"
    - "D-06: package diagram is completed in-lib (DIA-04 is NOT blocked on any sibling-lib PR; the node_type docstring PR is optional/non-blocking); containment is expressed via attributes (or 'contains' edge only if proven necessary)"
    - "Caller extracts a package diagram with GraphNode(node_type='package'); multiple packages per project; containment expressed via attributes (or 'contains' edge only if proven necessary)"
    - "All three diagram outputs validate against GraphNode/GraphEdge/GraphModel (DIA-07 shared schema-conformance)"
    - "Each output is sorted on exit by a stable composite key (DET-04) — byte-identical across PYTHONHASHSEED"
  artifacts:
    - path: "lib_code_parser/extractors/evaluations/class_diagram.py"
      provides: "DIA-01 class diagram extractor"
      contains: "def extract"
    - path: "lib_code_parser/extractors/evaluations/component_diagram.py"
      provides: "DIA-03 component diagram extractor (edge=imports)"
      contains: "imports"
    - path: "lib_code_parser/extractors/evaluations/package_diagram.py"
      provides: "DIA-04 package diagram extractor (node_type=package)"
      contains: "package"
  key_links:
    - from: "lib_code_parser/extractors/evaluations/component_diagram.py"
      to: "lib_code_parser.extractors.primitives.type_deps"
      via: "type_deps.extract(cav, config) filtered to kind=='imports'"
      pattern: "type_deps"
    - from: "lib_code_parser/_dispatch.py"
      to: "EVALUATIONS class_diagram/component_diagram/package_diagram"
      via: "append-only registration"
      pattern: "EVALUATIONS\\[\"(class|component|package)_diagram\"\\]"
---

<objective>
Implement the three structurally-simple "pull-and-map" diagram extractors that need no spike: DIA-01 (class), DIA-03 (component), DIA-04 (package). These reuse Phase 2 primitives almost verbatim (component diagram is the closest exact analog — it just maps `kind=="imports"` TypeDeps to `imports` edges). Each is a pure `def extract(cav, config) -> GraphModel`, registers one append-only EVALUATIONS entry, and feeds the DIA-07 shared schema-conformance test.

Purpose: deliver 4 of the 10 phase requirements with the lowest-risk extractors first, exercising the Wave-0 foundation end-to-end before the spike-bearing plans.
Output: 3 extractor modules, 3 EVALUATIONS registrations, unit + acceptance tests, DIA-07 schema cases, structural fixture file.
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-python-diagram-spec-extractors/03-CONTEXT.md
@.planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md
@.planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md
@.planning/phases/03-python-diagram-spec-extractors/03-01-SUMMARY.md

<interfaces>
<!-- After Plan 01: graph_base.EdgeKind includes "imports"; GraphEdge has source_unresolved. CodeContent has class_diagram/component_diagram/package_diagram slots. executor walks EVALUATIONS. -->
<!-- type_deps.extract(cav, config) -> list[TypeDep]; each TypeDep has source, target, kind ("imports" for import facts), source_line (type_deps.py:99-122). -->
<!-- functions.extract(cav, config) -> list[FunctionNode]. _paths.get_module_name(path) -> module/package name (ARC-04 single source). -->
<!-- GraphModel(nodes=[GraphNode(node_id,node_type,label,attributes)], edges=[GraphEdge(source,target,edge_type,label)], guards=[]). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DIA-03 component_diagram + DIA-04 package_diagram (the exact/near-exact analogs) + register</name>
  <read_first>
    - lib_code_parser/extractors/primitives/type_deps.py (lines 1-40 module skeleton; lines 99-146 — import-fact production + DET-04 sort idiom; this is the DIA-03 exact analog)
    - lib_code_parser/_paths.py (get_module_name — the only path→name source, ARC-04)
    - lib_code_parser/models/evaluations/graph_base.py (GraphNode lines 48-61 has attributes:dict[str,str]; GraphEdge lines 64-83; EdgeKind now includes imports)
    - lib_code_parser/_dispatch.py (registration block lines 50-60 — exact append-only analog)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§component_diagram.py lines 168-184 — pull+map snippet; §package_diagram.py lines 198-202 — containment-via-attributes decision; Shared 1/3 lines 42-126)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§Architectural Responsibility Map; Pitfall 2 lines 486-490 catch-all ban; Open Question #2 containment lines 621-624)
  </read_first>
  <behavior>
    - component_diagram: source with `import os` / `from m import N` → GraphNode(node_type="component") per module + GraphEdge(edge_type="imports", source=module, target="os"/"m.N"). No 'uses'/'depends' edge ever emitted.
    - package_diagram: a multi-module path set → GraphNode(node_type="package") per package; containment via GraphNode.attributes={"parent_package": ...}; multiple packages represented.
    - Both: nodes sorted by node_id; edges sorted by (source,target,edge_type,label); byte-identical under PYTHONHASHSEED=random.
  </behavior>
  <action>
    Create `lib_code_parser/extractors/evaluations/__init__.py` (mirror extractors/primitives/__init__.py). Create `component_diagram.py` (DIA-03): copy Shared-1 skeleton (`from __future__ import annotations`, `import ast`, absolute imports, `__all__=["extract"]`, `def extract(cav: CAV, config: ParserConfig) -> GraphModel`, `assert isinstance(cav.payload, ast.Module)`); pull `type_deps.extract(cav, config)`, build `GraphEdge(source=td.source, target=td.target, edge_type="imports")` for each `td.kind=="imports"` (D-03: keep this lib's own `imports` vocab — do NOT rename to the sibling lib's `dependency`), build component `GraphNode(node_type="component")` for each distinct module via `_paths.get_module_name`; dedup with `dict.fromkeys`; DET-04 sort both collections before return. Module docstring declares `Implements: DIA-03, DIA-07` + `Traces:`. Create `package_diagram.py` (DIA-04): emit `GraphNode(node_type="package")` (plain str per D-04/D-05/D-06 — no GraphNode schema change, no sibling-lib code change, no PR-blocking). DECISION you own (D-01 sub-decision): express package→module containment via `GraphNode.attributes={"parent_package": ...}` (GraphNode already has attributes) — do NOT add a `contains` EdgeKind unless containment as an EDGE is genuinely required for verifier comparison; if you conclude it is, escalate by adding `contains` to graph_base.py with the same comment+test discipline as Plan 01 Task 1 and note it in the SUMMARY. Register both in `_dispatch.py` bottom block append-only: import `extract as _extract_component_diagram` / `_extract_package_diagram`, then `EVALUATIONS["component_diagram"]=...`, `EVALUATIONS["package_diagram"]=...` (registration order MUST follow the canonical 7-order: class_diagram comes first — register class_diagram in Task 2; here register component then package; final order across both tasks is class,sequence?,component,package — see Task 2 note on ordering).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/extractors/test_component_diagram.py tests/unit/extractors/test_package_diagram.py -q && python -m ruff check lib_code_parser/extractors/evaluations/__init__.py lib_code_parser/extractors/evaluations/component_diagram.py lib_code_parser/extractors/evaluations/package_diagram.py lib_code_parser/_dispatch.py && python -m ruff format --check lib_code_parser/extractors/evaluations/__init__.py lib_code_parser/extractors/evaluations/component_diagram.py lib_code_parser/extractors/evaluations/package_diagram.py lib_code_parser/_dispatch.py</automated>
  </verify>
  <done>component_diagram emits imports edges + component nodes; package_diagram emits package nodes with attribute-based containment; both sorted; both registered append-only; unit tests green.</done>
  <acceptance_criteria>
    - `python -m pytest tests/acceptance/test_dia03_component_diagram.py -q` exits 0
    - `python -m pytest tests/acceptance/test_dia04_package_diagram.py -q` exits 0
    - `PYTHONHASHSEED=random python -m pytest tests/unit/extractors/test_component_diagram.py tests/unit/extractors/test_package_diagram.py -q` exits 0
    - `git diff lib_code_parser/models/evaluations/graph_base.py` is EMPTY unless a justified `contains` addition is documented in the SUMMARY
    - `ruff check` and `ruff format --check` exit 0 on component_diagram.py, package_diagram.py, evaluations/__init__.py and _dispatch.py
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DIA-01 class_diagram (composition/aggregation/association rule) + register + DIA-07 schema cases</name>
  <read_first>
    - lib_code_parser/extractors/primitives/type_deps.py (annotation-walk `_collect_annotation_deps` lines 37-66 — ast.unparse + annotation inspection)
    - lib_code_parser/extractors/primitives/contracts.py (per-class iteration `for class_node in tree.body: if not isinstance(class_node, ast.ClassDef): continue` lines ~159-161)
    - lib_code_parser/models/evaluations/graph_base.py (EdgeKind — inherits/composes/aggregates/associates all already exist; NO schema change for DIA-01)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§composition vs aggregation lines 451-476 — the full decision rule + annotation-unwrap AST mechanics for Optional[X]/X|None/list[X]/dict[K,V])
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§class_diagram.py lines 186-190; Shared 5 annotation extraction lines 154-162)
    - tests/unit/extractors/test_diagram_schema.py (the assert_valid_graphmodel helper from Plan 01 Task 3)
  </read_first>
  <behavior>
    - `class B(A)` → GraphEdge(edge_type="inherits", source=B, target=A).
    - `x: Engine` (direct known class) → composes. `y: Optional[Engine]` / `z: list[Engine]` → aggregates. `w: SomeForwardRefUnknown` / Any / TypeVar → associates (fallback, never 'uses').
    - `n: int` / `s: str` (builtin/primitive) → NOT a class edge (field, skipped).
    - class diagram output validates as GraphModel via assert_valid_graphmodel.
  </behavior>
  <action>
    Create `class_diagram.py` (DIA-01) with the Shared-1 skeleton. Iterate `cav.payload` ClassDefs (copy the contracts.py per-class iteration guard). For inheritance: each `ast.ClassDef.bases` Name → `GraphEdge(edge_type="inherits")` (ABC/Protocol bases → `implements` is your discretion). For relationships: collect declared instance attribute annotations from (1) class-body `ast.AnnAssign` and (2) `__init__` `self.x: T = ...` `ast.AnnAssign(target=ast.Attribute(self))`; for each, apply the RESEARCH §composition-vs-aggregation rule using `ast.unparse(annotation)` + structural unwrap (Optional[X]/`X | None` BinOp/list[X]/set[X]/dict[K,V] → aggregates inner class; direct class type → composes; builtin/primitive → skip; undecidable → associates). Per D-03, these edges keep this lib's vocabulary (inherits/composes/aggregates/associates) and are NOT renamed to the sibling lib's spelling — the verifier resolves the inherits↔inheritance gap. Resolve "is inner name a known class" against the set of ClassDef names in the module + imported class names (from type_deps/import map). Emit GraphNode(node_type="class") per class. DET-04 sort both collections. Register in `_dispatch.py`: `EVALUATIONS["class_diagram"]=...` — IMPORTANT ordering: class_diagram is canonical position #1, so place its registration line BEFORE component/package lines from Task 1 in the dict, preserving the canonical order `class_diagram, (sequence_diagram added by Plan 03), component_diagram, package_diagram, ...`. Since sequence_diagram (Plan 03) is registered later, the append-only test asserts subset-in-order, not contiguity — verify test_dispatch passes with the partial set. Add DIA-07 cases to `tests/unit/extractors/test_diagram_schema.py` feeding class/component/package outputs through assert_valid_graphmodel and asserting no non-physical_/source_ metadata leaks. Create the structural fixture file `tests/unit/extractors/fixtures/dia_structural.py` with the class-hierarchy fixture (RESEARCH §Required Test Fixtures: x:Engine composes, y:Optional[Engine]/z:list[Engine] aggregates, w:Unknown associates, class B(A) inherits).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/extractors/test_class_diagram.py tests/unit/extractors/test_diagram_schema.py tests/unit/test_dispatch.py -q && python -m ruff check lib_code_parser/extractors/evaluations/class_diagram.py lib_code_parser/_dispatch.py && python -m ruff format --check lib_code_parser/extractors/evaluations/class_diagram.py lib_code_parser/_dispatch.py</automated>
  </verify>
  <done>class_diagram emits inherits/composes/aggregates/associates per the type-annotation rule with association fallback; no 'uses' ever; registered #1 in canonical order; DIA-07 schema cases green for all 3 diagrams; dispatch test passes with partial registration.</done>
  <acceptance_criteria>
    - `python -m pytest tests/acceptance/test_dia01_class_diagram.py -q` exits 0
    - Grep gate: `grep -v '^#' lib_code_parser/extractors/evaluations/class_diagram.py | grep -cE '\"uses\"|\"depends\"|\"other\"'` returns 0
    - `python -m pytest tests/unit/extractors/test_diagram_schema.py -q` exits 0
    - `python -c "from lib_code_parser._dispatch import EVALUATIONS; ks=list(EVALUATIONS); canon=['class_diagram','sequence_diagram','component_diagram','package_diagram','state_diagram','function_spec','class_spec']; sub=[k for k in canon if k in ks]; assert sub==ks"` exits 0
    - `ruff check` and `ruff format --check` exit 0 on class_diagram.py and _dispatch.py
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → library | Untrusted source bytes parsed by stdlib `ast` (no execution); extractors only walk `cav.payload`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing (data integrity) | DIA-01 known-class resolution | mitigate | Class-name resolution is structural over module ClassDefs + import map (no name guessing); undecidable → explicit `associates`, never a fabricated edge |
| T-03-04 | Tampering (Layer M integrity) | all 3 diagram outputs | mitigate | DET-04 sort-on-exit + dict.fromkeys dedup → byte-identical under PYTHONHASHSEED; no clock/env/network |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — zero package installs (stdlib + Pydantic only); no Package Legitimacy table required |

> Pure in-process AST mapping. No injection/auth/secret/network surface.
</threat_model>

<verification>
- `python -m pytest tests/unit tests/acceptance -q` green.
- `PYTHONHASHSEED=random python -m pytest tests/unit/extractors -q` green (determinism).
- `git diff --name-only` touches no `extractors/primitives/*` or frozen Phase 2 files.
</verification>

<success_criteria>
- DIA-01/DIA-03/DIA-04 extractors implemented, registered append-only in canonical order, emitting valid GraphModels.
- DIA-07 schema-conformance verified for all three diagrams; no catch-all edges; containment via attributes.
- All outputs DET-04-sorted; byte-identical under hash randomization.
</success_criteria>

<output>
Create `.planning/phases/03-python-diagram-spec-extractors/03-02-SUMMARY.md` when done.
</output>
