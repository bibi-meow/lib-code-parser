---
phase: 03-python-diagram-spec-extractors
plan: 06
type: execute
wave: 5
depends_on: ["03-01", "03-05"]
files_modified:
  - lib_code_parser/extractors/evaluations/class_spec.py
  - lib_code_parser/extractors/evaluations/_markers.py
  - lib_code_parser/_dispatch.py
  - tests/unit/extractors/test_class_spec.py
  - tests/unit/extractors/test_aux_markers.py
  - tests/unit/test_dispatch.py
  - tests/acceptance/test_spc02_class_spec.py
  - tests/acceptance/test_spc04_contract_markers.py
  - tests/unit/extractors/fixtures/aux_marker_samples.py
autonomous: true
requirements: [SPC-02, SPC-04]
user_setup: []

must_haves:
  truths:
    - "Caller extracts ClassSpec(definition, members, invariants) from Python source"
    - "Caller extracts auxiliary contract markers: icontract (@require/@ensure/@invariant), deal (@pre/@post/@ensure/@inv), and PEP-316 pre:/post: docstring keywords"
    - "SPC-04 markers are DETECTED only — icontract/deal are never imported/executed (D-10); detection uses import-provenance (a decoy def require() is NOT flagged)"
    - "SPC-04 source_kind values live in models/evaluations/spec.py (SpecConditionSourceKind), NOT in the frozen primitives/contracts.py (invariant #1)"
    - "class_spec output is DET-04-sorted — byte-identical across PYTHONHASHSEED"
    - "All 7 EVALUATIONS entries are registered append-only in canonical order; executor walk produces all 7 outputs"
  artifacts:
    - path: "lib_code_parser/extractors/evaluations/class_spec.py"
      provides: "SPC-02 class spec + SPC-04 marker aggregation"
      contains: "def extract"
    - path: "lib_code_parser/extractors/evaluations/_markers.py"
      provides: "icontract/deal/PEP-316 detection (import-provenance, detection-only)"
      contains: "icontract"
  key_links:
    - from: "lib_code_parser/extractors/evaluations/_markers.py"
      to: "contracts.py import-provenance pattern"
      via: "_resolve_aliases over target_pkgs (icontract, deal)"
      pattern: "icontract|deal"
    - from: "lib_code_parser/_dispatch.py"
      to: "EVALUATIONS class_spec"
      via: "append-only registration (completes all 7)"
      pattern: "EVALUATIONS\\[\"class_spec\"\\]"
---

<objective>
Complete the phase: implement SPC-02 (class spec — definition + members + invariants) and SPC-04 (auxiliary contract marker detection for icontract / deal / PEP-316), and register the 7th and final EVALUATIONS entry. SPC-04 is detection-only (D-10): icontract/deal are never imported — markers are matched by decorator/docstring AST shape with the Phase 2 import-provenance false-positive defense. Per invariant #1 the frozen `primitives/contracts.py` SourceKind Literal is NOT mutated; SPC-04 source_kind values live in the Plan-01 `models/evaluations/spec.py` SpecConditionSourceKind.

Purpose: deliver the last two requirements (SPC-02, SPC-04) and verify the full 7-extractor EVALUATIONS pipeline end-to-end.
Output: _markers.py detector, class_spec extractor, final registration, class-spec + aux-marker unit tests, acceptance tests, marker fixtures.
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-python-diagram-spec-extractors/03-CONTEXT.md
@.planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md
@.planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md
@.planning/phases/03-python-diagram-spec-extractors/03-01-SUMMARY.md
@.planning/phases/03-python-diagram-spec-extractors/03-05-SUMMARY.md

<interfaces>
<!-- After Plan 01: spec.py exports ClassSpec(node_id, definition, members, invariants), SpecCondition(kind,text,source_kind,line_no), SpecConditionSourceKind Literal includes icontract_require/icontract_ensure/icontract_invariant/deal_pre/deal_post/deal_ensure/deal_inv/pep316_pre/pep316_post. CodeContent.class_spec: list[ClassSpec] slot exists. -->
<!-- contracts.py provenance trio (extractors/primitives/contracts.py:89-140): _resolve_aliases / _is_attribute_form / _classify_*. -->
<!-- functions.extract -> list[FunctionNode]; contracts.extract -> dict[str, ContractInfo] (Phase 2 Pydantic/dataclass contracts — SPC-04 is SUPPLEMENTARY to these). -->
<!-- Canonical EVALUATIONS order position #7 (final) = class_spec. -->
<!-- SPC-04 marker table: RESEARCH §Code Examples lines 575-583 ((pkg,attr)->(kind,source_kind)). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _markers.py — icontract/deal/PEP-316 detection (import-provenance, detection-only, D-10)</name>
  <read_first>
    - lib_code_parser/extractors/primitives/contracts.py (lines 89-140 — _resolve_aliases / _is_attribute_form / _classify_* provenance trio to COPY)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§Auxiliary Contract Markers lines 413-449 — icontract require/ensure/invariant, deal pre/post/ensure/inv, PEP-316 pre:/post: regex; §Code Examples lines 572-586 marker table)
    - lib_code_parser/models/evaluations/spec.py (Plan 01 — SpecConditionSourceKind Literal where the SPC-04 values live; SpecCondition shape)
    - tests/unit/extractors/test_contracts_extractor.py (false-positive defense test shapes lines 180-222 — copy for decoy def require()/non-importing cases)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§class_spec.py lines 218-222 — anti-Pitfall 6: do NOT edit frozen contracts.py)
  </read_first>
  <behavior>
    - `@icontract.require(lambda x: x>0)` (or `@require(...)` via `from icontract import require`) → SpecCondition(kind="precondition", source_kind="icontract_require", text=ast.unparse(lambda)).
    - `@deal.post(lambda result: result>=0)` → SpecCondition(kind="postcondition", source_kind="deal_post"). `@deal.inv(...)`/`@icontract.invariant(...)` → invariant.
    - PEP-316 docstring `pre: ...` / `post: ...` (regex `^\s*pre:\s*(.+)$` / `^\s*post(?:\[\w+\])?:\s*(.+)$`) → SpecCondition source_kind="pep316_pre"/"pep316_post".
    - FALSE-POSITIVE DEFENSE: a user's own `def require(...)` / `@require` with NO import of icontract → NOT flagged.
    - icontract/deal are NEVER imported by the lib (D-10).
  </behavior>
  <action>
    Create `lib_code_parser/extractors/evaluations/_markers.py` (detection-only, D-10). Copy the contracts.py provenance trio parameterized for `target_pkgs=("icontract",)` and `("deal",)`. Implement `_classify_decorator(decorator, aliases) -> SpecCondition | None` driven by the marker table from RESEARCH §Code Examples lines 575-583 (`(pkg,attr)->(kind,source_kind)`): icontract require/ensure/invariant, deal pre/post/ensure/inv; classify ONLY when the decorator name resolves via the import map OR is the `@pkg.attr` attribute form (bare names rejected — Pitfall 4). Condition text = `ast.unparse(decorator_call.args[0])` when present. Implement `_detect_pep316(docstring) -> list[SpecCondition]` using the two anchored regexes over `ast.get_docstring()`. Public entries: `def detect_decorator_markers(node, aliases) -> list[SpecCondition]` and `def detect_pep316_markers(docstring) -> list[SpecCondition]`. Use the SpecConditionSourceKind values from spec.py — do NOT touch `primitives/contracts.py` (invariant #1, anti-Pitfall 6). Create `tests/unit/extractors/test_aux_markers.py` (icontract/deal/PEP-316 positive + decoy `def require()`/non-importing-`@require` false-positive defense — copy test_contracts_extractor.py:180-222 shapes) and `tests/unit/extractors/fixtures/aux_marker_samples.py`.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/extractors/test_aux_markers.py -q</automated>
  </verify>
  <done>_markers.py detects icontract/deal/PEP-316 via import-provenance; decoy require() not flagged; SPC-04 source_kinds from spec.py; frozen contracts.py untouched; test green.</done>
  <acceptance_criteria>
    - Grep gate: `grep -v '^#' lib_code_parser/extractors/evaluations/_markers.py | grep -cE 'import icontract|import deal|from icontract|from deal'` returns 0 (D-10 detection-only)
    - `git diff --name-only` does NOT include lib_code_parser/models/primitives/contracts.py or lib_code_parser/extractors/primitives/contracts.py (invariant #1)
    - `python -m pytest tests/unit/extractors/test_aux_markers.py -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: class_spec.py (SPC-02) + SPC-04 aggregation + register the final 7th entry</name>
  <read_first>
    - lib_code_parser/extractors/primitives/contracts.py (class-keyed aggregate iteration lines 143-202 — the EXACT analog for class_spec)
    - lib_code_parser/extractors/evaluations/_markers.py (Task 1 — detect_decorator_markers / detect_pep316_markers)
    - lib_code_parser/models/evaluations/spec.py (ClassSpec/SpecCondition shapes)
    - lib_code_parser/extractors/primitives/functions.py (FunctionNode — members source)
    - lib_code_parser/_dispatch.py (registration block — class_spec is canonical position #7, the final entry)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§class_spec.py lines 218-222; §_dispatch.py lines 259-276 — verify all 7 in canonical order after this)
  </read_first>
  <behavior>
    - For each class: ClassSpec(node_id, definition, members=[method/attr names], invariants).
    - invariants aggregate: Phase 2 dataclass/Pydantic contracts (pull `contracts.extract`) PLUS SPC-04 markers (icontract.invariant/deal.inv class decorators + per-method pre/post + PEP-316) — SPC-04 is SUPPLEMENTARY, marked with its own source_kind.
    - members sorted by name; invariants sorted by (source_kind, line_no, text) (DET-04); byte-identical under PYTHONHASHSEED.
    - After registration, all 7 EVALUATIONS entries present in canonical order; executor.execute produces all 7 CodeContent slots populated.
  </behavior>
  <action>
    Create `class_spec.py` (SPC-02/04) with the Shared-1 skeleton; signature `def extract(cav, config) -> list[ClassSpec]`. Copy the contracts.py class-keyed aggregate iteration (lines 143-202). For each ClassDef: build ClassSpec(node_id, definition=signature/bases summary, members=function/attr names from functions + class body). Aggregate invariants from (a) Phase 2 `contracts.extract(cav, config)` for the matching class node_id (supplementary baseline) and (b) SPC-04 via `_markers.detect_decorator_markers` on class + method decorators and `_markers.detect_pep316_markers` on docstrings — build the import-provenance `aliases` map once via the _markers provenance helper. DET-04 sort members by name, invariants by (source_kind, line_no, text). Module docstring `Implements: SPC-02, SPC-04` + `Traces: SPC-02, SPC-04, US-01, US-22`. Register the FINAL append-only entry `EVALUATIONS["class_spec"]=...` at canonical position #7. Create `tests/unit/extractors/test_class_spec.py` (class → ClassSpec definition/members/invariants) and `tests/acceptance/test_spc02_class_spec.py` + `tests/acceptance/test_spc04_contract_markers.py` (full execute() path). Update `tests/unit/test_dispatch.py` to assert all 7 EVALUATIONS keys in canonical order (mirror test_primitives_dict_has_4_entries_in_append_only_order — now that all 7 exist, change the subset-in-order assertion to the full 7-key equality).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/extractors/test_class_spec.py tests/unit/test_dispatch.py -q</automated>
  </verify>
  <done>class_spec emits SPC-02 definition/members/invariants + SPC-04 supplementary markers; class_spec registered #7 (final); all 7 EVALUATIONS in canonical order; full pipeline produces all 7 outputs; tests green.</done>
  <acceptance_criteria>
    - `python -m pytest tests/acceptance/test_spc02_class_spec.py tests/acceptance/test_spc04_contract_markers.py -q` exits 0
    - `python -c "from lib_code_parser._dispatch import EVALUATIONS; assert list(EVALUATIONS)==['class_diagram','sequence_diagram','component_diagram','package_diagram','state_diagram','function_spec','class_spec']"` exits 0
    - `PYTHONHASHSEED=random python -m pytest tests/unit/extractors -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → library | Untrusted source bytes; contract-marker libraries DETECTED by AST shape, NEVER imported (D-10). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Spoofing (data integrity) | SPC-04 marker detection | mitigate | Import-provenance restriction (contracts.py pattern): a user's own require()/pre()/State is NOT classified without a real icontract/deal import — false-positive defense test asserted |
| T-03-14 | Elevation of privilege (code exec) | marker condition extraction | mitigate | Condition text via `ast.unparse` of the lambda AST only; icontract/deal never imported/executed (D-10) |
| T-03-15 | Tampering (Layer M integrity) | class_spec output ordering | mitigate | DET-04 sort-on-exit on members + invariants; no clock/env/network |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — icontract/deal are detection targets, NOT installed (D-10); zero installs |
</threat_model>

<verification>
- `python -m pytest -q` (FULL suite) green — final phase gate.
- All 7 EVALUATIONS entries in canonical order; executor produces all 7 CodeContent slots.
- No `import icontract`/`import deal` anywhere; frozen primitives/contracts.py untouched.
- `PYTHONHASHSEED=random python -m pytest tests/unit/extractors -q` green (determinism).
</verification>

<success_criteria>
- SPC-02 class_spec (definition/members/invariants) + SPC-04 supplementary marker detection implemented.
- SPC-04 source_kinds in spec.py (not frozen contracts.py); icontract/deal never imported; decoy not flagged.
- class_spec registered append-only #7; all 7 EVALUATIONS in canonical order; full pipeline green; DET-04-sorted.
</success_criteria>

<output>
Create `.planning/phases/03-python-diagram-spec-extractors/03-06-SUMMARY.md` when done.
</output>
