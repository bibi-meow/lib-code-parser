---
phase: 03-python-diagram-spec-extractors
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-04"]
files_modified:
  - lib_code_parser/extractors/evaluations/_docstring.py
  - lib_code_parser/extractors/evaluations/function_spec.py
  - lib_code_parser/_dispatch.py
  - tests/unit/extractors/test_docstring_parser.py
  - tests/unit/extractors/test_function_spec.py
  - tests/unit/test_dispatch.py
  - tests/acceptance/test_spc01_function_spec.py
  - tests/unit/extractors/fixtures/docstring_dialects.py
autonomous: true
requirements: [SPC-01]
user_setup: []

must_haves:
  truths:
    - "Caller extracts FunctionSpec(signature, docstring_sections, preconditions, postconditions) from Python source"
    - "The SAME function documented in Google, NumPy, and Sphinx Napoleon styles yields identical normalized docstring_sections (the strongest SPC-01 determinism proof)"
    - "Docstring parsing is stdlib-only (ast.get_docstring + internal state machine); NO external dependency added (D-09)"
    - "Pre/post conditions are derived by a fixed keyword/regex heuristic (no NLP/scoring); marked source_kind='docstring'; byte-stable"
    - "function_spec output is DET-04-sorted — byte-identical across PYTHONHASHSEED"
  artifacts:
    - path: "lib_code_parser/extractors/evaluations/_docstring.py"
      provides: "Google/NumPy/Sphinx dialect parser (stdlib-only)"
      contains: "def "
    - path: "lib_code_parser/extractors/evaluations/function_spec.py"
      provides: "SPC-01 function spec extractor"
      contains: "def extract"
  key_links:
    - from: "lib_code_parser/extractors/evaluations/function_spec.py"
      to: "lib_code_parser.extractors.evaluations._docstring"
      via: "parse(docstring) -> list[DocstringSection]"
      pattern: "_docstring"
    - from: "lib_code_parser/_dispatch.py"
      to: "EVALUATIONS function_spec"
      via: "append-only registration"
      pattern: "EVALUATIONS\\[\"function_spec\"\\]"
---

<objective>
Implement the SPC-01 function spec extractor and its genuinely-new dependency: the stdlib-only docstring dialect parser (`_docstring.py`). Per D-09 NO external library (`docstring_parser` etc.) may be added — the Google/NumPy/Sphinx Napoleon section grammars are parsed by an internal byte-stable state machine. The strongest correctness/determinism proof is the three-dialect golden fixture: the same function documented three ways must yield identical normalized output.

Purpose: deliver SPC-01 (US-01/US-22 support) with the docstring parser as a reusable evaluation-layer helper (also consumed by Plan 06's class_spec).
Output: _docstring.py parser, function_spec extractor, registration, parser + spec unit tests, 3-dialect golden fixture, acceptance test.
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-python-diagram-spec-extractors/03-CONTEXT.md
@.planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md
@.planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md
@.planning/phases/03-python-diagram-spec-extractors/03-01-SUMMARY.md
@.planning/phases/03-python-diagram-spec-extractors/03-04-SUMMARY.md

<interfaces>
<!-- After Plan 01: models/evaluations/spec.py exports FunctionSpec(node_id, signature, docstring_sections, preconditions, postconditions), DocstringSection(kind,name,type_ref,text), SpecCondition(kind,text,source_kind,line_no). CodeContent.function_spec: list[FunctionSpec] slot exists. -->
<!-- functions.extract(cav, config) -> list[FunctionNode] (signature, params, return_type, docstring, source_range). ast.get_docstring(node) for raw docstrings. -->
<!-- Canonical EVALUATIONS order position #6 = function_spec. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _docstring.py — stdlib-only Google/NumPy/Sphinx dialect parser (the new algorithm)</name>
  <read_first>
    - lib_code_parser/_paths.py (the stdlib-only private-helper-module SHAPE analog — module docstring, from __future__ import annotations, _snake_case helpers, typed signatures)
    - lib_code_parser/models/evaluations/spec.py (Plan 01 — DocstringSection/SpecCondition target shapes)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§Docstring Dialect Parsing lines 383-411 — the 3 section-header grammars, the byte-stable detection order Sphinx→NumPy→Google, section mapping → normalized model, pre/post derivation heuristic with fixed keyword set)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§_docstring.py lines 224-228 — no exact analog, follow module conventions)
  </read_first>
  <behavior>
    - parse("...Google docstring with Args:/Returns:/Raises:...") → DocstringSection list (one per param + returns/raises/summary).
    - parse(NumPy "Parameters\n----------" + dashed-underline form) → identical normalized sections for the same content.
    - parse(Sphinx ":param x:" / ":returns:" / ":raises Exc:") → identical normalized sections.
    - Dialect detection order Sphinx→NumPy→Google (first match wins, byte-stable); ambiguous/no-section docstring → whole text as summary.
    - Pre/post derivation: fixed keyword/regex set (must be, non-negative, > 0, not None, required; Raises: → documented precondition; Returns: constraints → postcondition) → SpecCondition(source_kind="docstring"). No NLP, no scoring. Same input → same output every run.
  </behavior>
  <action>
    Create `lib_code_parser/extractors/evaluations/_docstring.py` (stdlib-only, D-09). Implement a state machine over `docstring.splitlines()` per RESEARCH §Docstring Dialect Parsing: (1) `_detect_dialect(lines) -> Literal["sphinx","numpy","google","none"]` using the byte-stable order — Sphinx if any line matches `^\s*:(param|returns?|raises?|type|rtype)\b`, else NumPy if a header line is immediately followed by `^-{3,}$`, else Google if a known header `^(Args|Arguments|Returns|Raises|Yields|Attributes|Note|Examples?):\s*$`, else none. (2) Three dialect parsers producing the SAME `list[DocstringSection]` shape (params explode one-per-parameter; summary = leading prose). (3) `_derive_conditions(sections) -> tuple[list[SpecCondition], list[SpecCondition]]` (pre, post) using the fixed keyword/regex set from RESEARCH §pre/post lines 405-411, each marked `source_kind="docstring"`. Public entry `def parse(docstring: str) -> tuple[list[DocstringSection], list[SpecCondition], list[SpecCondition]]` (or a small dataclass/pydantic result — your discretion within spec.py shapes). `__all__` exports the public entry. Use `re` (stdlib). Create `tests/unit/extractors/test_docstring_parser.py` asserting the 3-dialect equivalence on identical content + the pre/post heuristic + the byte-stable detection order. Create `tests/unit/extractors/fixtures/docstring_dialects.py` holding the SAME function body documented Google/NumPy/Sphinx (RESEARCH §Required Test Fixtures lines 681 — the strongest determinism proof).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/extractors/test_docstring_parser.py -q && python -m ruff check lib_code_parser/extractors/evaluations/_docstring.py && python -m ruff format --check lib_code_parser/extractors/evaluations/_docstring.py</automated>
  </verify>
  <done>_docstring.py parses all 3 dialects to identical normalized sections for identical content; pre/post heuristic is fixed-keyword byte-stable; no external dependency; test green.</done>
  <acceptance_criteria>
    - Grep gate: `grep -v '^#' lib_code_parser/extractors/evaluations/_docstring.py | grep -cE 'import docstring_parser|from docstring_parser'` returns 0 (D-09 — stdlib only)
    - `git diff pyproject.toml` adds NO new dependency
    - `python -m pytest tests/unit/extractors/test_docstring_parser.py -q` exits 0
    - `ruff check` and `ruff format --check` exit 0 on _docstring.py
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: function_spec.py (SPC-01) pulling functions + _docstring + register</name>
  <read_first>
    - lib_code_parser/extractors/primitives/functions.py (FunctionNode fields — signature/params/return_type/docstring/source_range to pull)
    - lib_code_parser/extractors/primitives/contracts.py (per-member iteration shape)
    - lib_code_parser/extractors/evaluations/_docstring.py (Task 1 — the parse() entry)
    - lib_code_parser/extractors/evaluations/function_spec.py — N/A (creating); mirror Shared-1 skeleton from any Plan 02 extractor
    - lib_code_parser/_dispatch.py (registration block — function_spec at canonical position #6)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§function_spec.py lines 212-216)
  </read_first>
  <behavior>
    - For each function/method: FunctionSpec(node_id, signature, docstring_sections from _docstring.parse(ast.get_docstring), preconditions, postconditions).
    - Functions with no docstring → FunctionSpec with empty sections (inert, not skipped — caller sees the signature).
    - output is a list[FunctionSpec] sorted by node_id (DET-04); byte-identical under PYTHONHASHSEED.
  </behavior>
  <action>
    Create `function_spec.py` (SPC-01) with the Shared-1 skeleton; signature `def extract(cav, config) -> list[FunctionSpec]`. Pull `functions.extract(cav, config)` for signatures; for each FunctionNode get the raw docstring via `ast.get_docstring(node)` (resolve node from cav.payload by matching the FunctionNode — or carry docstring already on FunctionNode if present), feed to `_docstring.parse`, build FunctionSpec with docstring_sections + preconditions + postconditions. Module docstring `Implements: SPC-01` + `Traces: SPC-01, US-01, US-22`. DET-04 sort by node_id. Register append-only `EVALUATIONS["function_spec"]=...` at canonical position #6 (after state_diagram, before class_spec). Create `tests/unit/extractors/test_function_spec.py` (3-dialect golden: same function documented 3 ways → identical FunctionSpec.docstring_sections) and `tests/acceptance/test_spc01_function_spec.py` (full execute() path → result.content.function_spec).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/extractors/test_function_spec.py tests/unit/test_dispatch.py -q && python -m ruff check lib_code_parser/extractors/evaluations/function_spec.py lib_code_parser/_dispatch.py && python -m ruff format --check lib_code_parser/extractors/evaluations/function_spec.py lib_code_parser/_dispatch.py</automated>
  </verify>
  <done>function_spec emits FunctionSpec per function with normalized docstring sections + pre/post; registered #6; DET-04-sorted; tests green.</done>
  <acceptance_criteria>
    - `python -m pytest tests/acceptance/test_spc01_function_spec.py -q` exits 0
    - `PYTHONHASHSEED=random python -m pytest tests/unit/extractors/test_function_spec.py -q` exits 0
    - `python -c "from lib_code_parser._dispatch import EVALUATIONS; ks=list(EVALUATIONS); canon=['class_diagram','sequence_diagram','component_diagram','package_diagram','state_diagram','function_spec','class_spec']; sub=[k for k in canon if k in ks]; assert sub==ks and 'function_spec' in ks"` exits 0
    - `ruff check` and `ruff format --check` exit 0 on function_spec.py and _dispatch.py
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → library | Untrusted source bytes; docstrings extracted via `ast.get_docstring` (no execution); parsed by internal regex/state machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering (Layer M integrity) | docstring pre/post heuristic | mitigate | Fixed keyword/regex set (no NLP/scoring) → same docstring yields same conditions every run; DET-04 sort-on-exit; no clock/env/network |
| T-03-12 | Denial of Service (regex) | _docstring regex over caller text | accept | Regexes are anchored, linear, fixed (no catastrophic-backtracking constructs); docstring size bounded by source file; same risk profile as any parser |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — D-09 forbids adding docstring_parser or any dep; zero installs; grep gate asserts no external import |

> Pure in-process text parsing; no injection/auth/secret/network surface.
</threat_model>

<verification>
- `python -m pytest tests/unit tests/acceptance -q` green.
- No new dependency in pyproject.toml; no `docstring_parser` import.
- 3-dialect golden test asserts identical normalized output.
- `PYTHONHASHSEED=random python -m pytest tests/unit/extractors -q` green.
</verification>

<success_criteria>
- SPC-01 function_spec extractor implemented; _docstring parser is stdlib-only and 3-dialect-equivalent.
- function_spec registered append-only #6; DET-04-sorted output.
- pre/post conditions derived by fixed byte-stable heuristic, marked source_kind="docstring".
</success_criteria>

<output>
Create `.planning/phases/03-python-diagram-spec-extractors/03-05-SUMMARY.md` when done.
</output>
