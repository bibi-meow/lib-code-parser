---
phase: 03-python-diagram-spec-extractors
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - lib_code_parser/extractors/evaluations/sequence_diagram.py
  - lib_code_parser/_dispatch.py
  - tests/unit/extractors/test_sequence_diagram.py
  - tests/unit/extractors/test_diagram_schema.py
  - tests/unit/test_dispatch.py
  - tests/acceptance/test_dia02_sequence_diagram.py
  - tests/spikes/__init__.py
  - tests/spikes/test_sp2_branch_fidelity.py
  - tests/unit/extractors/fixtures/seq_callgraph.py
  - .planning/spikes/SP-2-sequence-branch-fidelity.md
autonomous: true
requirements: [DIA-02, DIA-07]
user_setup: []

must_haves:
  truths:
    - "SP-2 verdict (ship branch-fidelity OR defer DIA-02-FULL to v0.3.0) is recorded with a deterministic-rule rationale in .planning/spikes/SP-2-sequence-branch-fidelity.md"
    - "Caller extracts a linear sequence diagram from the call graph: participants + GraphEdge(edge_type='calls') ordered deterministically (must-have, independent of spike outcome per D-07)"
    - "Sequence diagram output validates against GraphModel (DIA-07)"
    - "Output is DET-04-sorted — byte-identical across PYTHONHASHSEED"
  artifacts:
    - path: ".planning/spikes/SP-2-sequence-branch-fidelity.md"
      provides: "SP-2 ship/defer verdict + rationale"
      contains: "verdict"
    - path: "lib_code_parser/extractors/evaluations/sequence_diagram.py"
      provides: "DIA-02 linear sequence extractor"
      contains: "def extract"
  key_links:
    - from: "lib_code_parser/extractors/evaluations/sequence_diagram.py"
      to: "lib_code_parser.extractors.primitives.callgraph"
      via: "callgraph.extract(cav, config) -> CallGraph"
      pattern: "callgraph"
    - from: "lib_code_parser/_dispatch.py"
      to: "EVALUATIONS sequence_diagram"
      via: "append-only registration"
      pattern: "EVALUATIONS\\[\"sequence_diagram\"\\]"
---

<objective>
Run the SP-2 spike FIRST (gating deliverable), then implement the DIA-02 linear sequence extractor. Per D-07 the linear sequence (call-graph-derived) is a v0.2.0 must-have regardless of spike outcome; per D-08 the SP-2 verdict only decides whether branch fidelity (alt/loop/par frames) ships now or defers to v0.3.0 as DIA-02-FULL. The sole ship criterion is "is a deterministic rule constructible" (byte-identical pure function, no LLM/heuristic).

Purpose: deliver DIA-02 must-have + produce the SP-2 verdict that the roadmap's v0.2.0/v0.3.0 line depends on.
Output: SP-2 spike doc + probe tests, sequence_diagram extractor, registration, unit + acceptance tests, DIA-07 schema case.
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-python-diagram-spec-extractors/03-CONTEXT.md
@.planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md
@.planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md
@.planning/phases/03-python-diagram-spec-extractors/03-01-SUMMARY.md
@.planning/phases/03-python-diagram-spec-extractors/03-02-SUMMARY.md

<interfaces>
<!-- callgraph.extract(cav, config) -> CallGraph(nodes, edges); each CallEdge has caller, callee (callgraph.py:95 sorts by (caller,callee)). -->
<!-- GraphEdge edge_type "calls" already exists in EdgeKind (no schema change for DIA-02). -->
<!-- After Plans 01/02: EVALUATIONS contains class_diagram, component_diagram, package_diagram (canonical order: class_diagram, [sequence_diagram], component_diagram, package_diagram). Insert sequence_diagram in position #2. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SP-2 spike — probe deterministic branch-fidelity extraction, record verdict</name>
  <read_first>
    - .planning/phases/03-python-diagram-spec-extractors/03-CONTEXT.md (D-07/D-08 lines 102-116 — must-have vs spike-defer, determinism-only criterion)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§Open Questions #3 lines 626-629 — SP-2 looks shippable: ast.If/For/While/async → alt/loop/par; confirm with fixtures)
    - lib_code_parser/extractors/primitives/callgraph.py (CallGraph shape + _get_call_name helper lines 34-40)
    - .planning/spikes/SP-3-libclang-macos-arm64.md (Phase 1 spike doc — copy its verdict-doc FORMAT: states ship/defer + rationale)
  </read_first>
  <action>
    Create `tests/spikes/__init__.py` and `tests/spikes/test_sp2_branch_fidelity.py` with deterministic probe fixtures: source containing `if`/`for`/`while`/`async` constructs wrapping calls. The probe asserts whether a pure AST rule can map `ast.If → alt`, `ast.For/ast.While → loop`, `ast.AsyncWith/await → par` frames byte-identically (run the same fixture twice, assert identical structured output). Write `.planning/spikes/SP-2-sequence-branch-fidelity.md` recording the VERDICT (ship OR defer-to-v0.3.0-DIA-02-FULL) with the deterministic-rule rationale, mirroring the SP-3 spike doc format. If verdict = ship: the branch-frame mapping rule is documented and Task 2 implements it. If verdict = defer: Task 2 ships linear-only and the doc states DIA-02-FULL is v0.3.0. DO NOT let the verdict block the linear must-have (D-07). Record which fixtures proved/disproved determinism.
  </action>
  <verify>
    <automated>python -m pytest tests/spikes/test_sp2_branch_fidelity.py -q</automated>
  </verify>
  <done>SP-2 spike probe tests exist and pass; .planning/spikes/SP-2-sequence-branch-fidelity.md states a ship-or-defer verdict with deterministic-rule rationale and the fixtures used.</done>
  <acceptance_criteria>
    - `git ls-files .planning/spikes/SP-2-sequence-branch-fidelity.md` lists the file
    - Grep gate: `grep -v '^#' .planning/spikes/SP-2-sequence-branch-fidelity.md | grep -ciE 'verdict|ship|defer'` returns >= 1
    - `python -m pytest tests/spikes/test_sp2_branch_fidelity.py -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DIA-02 sequence_diagram (linear must-have + branch frames if SP-2 ships) + register</name>
  <read_first>
    - lib_code_parser/extractors/primitives/callgraph.py (FULL — exact source of edges; _get_call_name lines 34-40; DET-04 sort line 95)
    - lib_code_parser/extractors/evaluations/component_diagram.py (Plan 02 — the just-shipped Shared-1 skeleton + DET-04 pattern to mirror)
    - lib_code_parser/_dispatch.py (registration block — insert sequence_diagram at canonical position #2)
    - .planning/spikes/SP-2-sequence-branch-fidelity.md (the verdict from Task 1 — decides whether branch frames are implemented)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§sequence_diagram.py lines 192-196)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (DIA-02 discretion lines: participant resolution / self-call / chain-call decomposition — CONTEXT §Claude's Discretion)
  </read_first>
  <behavior>
    - Linear must-have: pull callgraph → map each CallEdge(caller, callee) to GraphEdge(edge_type="calls", source=caller, target=callee); participant GraphNodes deterministically ordered; output validates as GraphModel.
    - If SP-2=ship: `if`/`for`/`while`/`async` over cav.payload produce alt/loop/par frame markers (encoded per the spike doc's documented scheme — e.g. GuardExpr or label); byte-identical.
    - If SP-2=defer: linear-only; no branch frames; sequence_diagram still emits valid calls edges.
    - DET-04 sorted; byte-identical under PYTHONHASHSEED=random.
  </behavior>
  <action>
    Create `sequence_diagram.py` (DIA-02) with the Shared-1 skeleton. Pull `callgraph.extract(cav, config)`; map `CallEdge(caller, callee)` → `GraphEdge(edge_type="calls", source=caller, target=callee)`; emit participant `GraphNode` per distinct caller/callee (dedup via dict.fromkeys). Participant resolution / self-call representation / chain-call decomposition are your discretion (inherit Phase 2 callgraph representation; re-evaluate the Phase 2 deferred "CallGraph resolution expansion" — if expansion is needed, do the minimum for linear correctness). IF and ONLY IF SP-2 verdict = ship, additionally walk `cav.payload` for `ast.If`/`ast.For`/`ast.While`/`async` and emit branch-frame markers per the scheme documented in the spike doc. DET-04 sort all collections. Register append-only in `_dispatch.py`: import `extract as _extract_sequence_diagram`, place `EVALUATIONS["sequence_diagram"]=...` so the dict's relative order matches canonical position #2 (after class_diagram, before component_diagram). Add a DIA-07 case to test_diagram_schema.py feeding sequence output through assert_valid_graphmodel. Create `tests/unit/extractors/fixtures/seq_callgraph.py` with the call-graph fixture (RESEARCH §Required Test Fixtures).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/extractors/test_sequence_diagram.py tests/unit/extractors/test_diagram_schema.py tests/unit/test_dispatch.py -q</automated>
  </verify>
  <done>sequence_diagram emits linear calls edges (must-have); branch frames present iff SP-2 shipped; registered at canonical position #2; DIA-07 schema case green; dispatch order test passes.</done>
  <acceptance_criteria>
    - `python -m pytest tests/acceptance/test_dia02_sequence_diagram.py -q` exits 0
    - `PYTHONHASHSEED=random python -m pytest tests/unit/extractors/test_sequence_diagram.py -q` exits 0
    - `python -c "from lib_code_parser._dispatch import EVALUATIONS; ks=list(EVALUATIONS); canon=['class_diagram','sequence_diagram','component_diagram','package_diagram','state_diagram','function_spec','class_spec']; sub=[k for k in canon if k in ks]; assert sub==ks and 'sequence_diagram' in ks"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → library | Untrusted source bytes; stdlib `ast` walk over `cav.payload` (no execution). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering (Layer M integrity) | sequence_diagram branch-frame walk | mitigate | Branch-frame extraction ships ONLY if SP-2 proves a deterministic byte-identical rule; otherwise deferred — determinism is the explicit ship gate (D-08) |
| T-03-06 | Tampering | sequence_diagram output ordering | mitigate | DET-04 sort-on-exit + dict.fromkeys dedup; no clock/env/network |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — zero package installs; stdlib + existing pins only |

> Pure in-process AST mapping; no injection/auth/secret/network surface.
</threat_model>

<verification>
- `python -m pytest tests/unit tests/acceptance tests/spikes -q` green.
- SP-2 verdict doc exists and states ship/defer with deterministic rationale.
- `PYTHONHASHSEED=random python -m pytest tests/unit/extractors -q` green.
</verification>

<success_criteria>
- SP-2 spike verdict recorded; linear sequence must-have shipped regardless of verdict.
- sequence_diagram registered append-only at canonical position #2; output valid GraphModel, DET-04-sorted.
- DIA-07 schema-conformance verified for sequence output.
</success_criteria>

<output>
Create `.planning/phases/03-python-diagram-spec-extractors/03-03-SUMMARY.md` when done.
</output>
