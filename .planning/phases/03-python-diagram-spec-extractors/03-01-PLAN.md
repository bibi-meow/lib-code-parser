---
phase: 03-python-diagram-spec-extractors
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib_code_parser/models/evaluations/graph_base.py
  - lib_code_parser/models/evaluations/spec.py
  - lib_code_parser/models/evaluations/__init__.py
  - lib_code_parser/models/infrastructure/artifact.py
  - lib_code_parser/executor.py
  - tests/unit/models/test_graph_base.py
  - tests/unit/test_dispatch.py
  - tests/unit/models/test_spec_extra_forbid.py
  - tests/unit/extractors/test_diagram_schema.py
  - tests/conftest.py
  - tests/unit/extractors/fixtures/__init__.py
autonomous: true
requirements: [DIA-06, DIA-07, DIA-03]
user_setup: []

must_haves:
  truths:
    - "D-01: EdgeKind accepts 'imports' as an append-only explicit-semantic value (existing 11 values immutable; 'contains' added only if DIA-04 design proves attribute-containment insufficient) and still rejects catch-alls (uses/other/misc/depends)"
    - "D-02: graph_base.py stays self-contained — NO direct import of lib-diagram-parser; the strict extra='forbid' / closed EdgeKind schema is retained as the Phase 3 foundation"
    - "GraphEdge can carry an unresolved marker without raising ValidationError under extra='forbid'"
    - "FunctionSpec and ClassSpec models exist with extra='forbid' and physical_*/source_* prefixed optional metadata"
    - "CodeContent exposes 7 new optional default-empty slots (class_diagram, sequence_diagram, component_diagram, package_diagram, state_diagram, function_spec, class_spec) without breaking v0.1.0 callers"
    - "executor.py walks EVALUATIONS append-only after the PRIMITIVES loop and assigns each result to its CodeContent slot, gated per-evaluation"
    - "D-04: DIA-07 schema-conformance basis is graph_base.py — every diagram output validates against GraphNode/GraphEdge/GraphModel/GuardExpr; physical-only metadata uses the physical_*/source_* prefix and node_type stays plain str (this is a shared cross-diagram truth)"
  artifacts:
    - path: "lib_code_parser/models/evaluations/spec.py"
      provides: "FunctionSpec, ClassSpec, DocstringSection, SpecCondition Pydantic models"
      contains: "class FunctionSpec"
    - path: "lib_code_parser/models/evaluations/graph_base.py"
      provides: "EdgeKind += imports; GraphEdge.source_unresolved optional field"
      contains: "imports"
    - path: "lib_code_parser/models/infrastructure/artifact.py"
      provides: "CodeContent 7 additive evaluation slots"
      contains: "class_diagram"
    - path: "lib_code_parser/executor.py"
      provides: "EVALUATIONS walk wired into CodeContent"
      contains: "EVALUATIONS"
  key_links:
    - from: "lib_code_parser/executor.py"
      to: "lib_code_parser._dispatch.EVALUATIONS"
      via: "for name, fn in EVALUATIONS.items()"
      pattern: "EVALUATIONS"
    - from: "lib_code_parser/models/infrastructure/artifact.py"
      to: "lib_code_parser.models.evaluations.graph_base.GraphModel"
      via: "CodeContent diagram slot type annotations"
      pattern: "GraphModel"
---

<objective>
Lay the shared schema + integration foundation that all 7 Phase 3 extractors depend on, WITHOUT writing any extractor yet. This plan closes the four PATTERNS.md integration gaps at the contract level: (b) CodeContent gets 7 new optional slots, (a) executor gets the EVALUATIONS walk it never implemented, (c) the DIA-06 `unresolved` marker gets a schema home, and (d) the two by-design-breaking tests are updated in the same commit as the schema change. It also lands the new `models/evaluations/spec.py` (FunctionSpec/ClassSpec) and the Wave-0 test scaffolding (fixtures dir, conftest helpers, DIA-07 schema-conformance test).

Purpose: every downstream extractor plan (02–06) builds against immutable, already-wired contracts — they only add their own extractor file + register one-or-more EVALUATIONS entries. No extractor plan touches CodeContent/executor/spec-model shape.
Output: schema appends (graph_base.py), new spec model module, CodeContent slots, executor EVALUATIONS loop, updated breaking tests, Wave-0 fixtures + conftest + DIA-07 schema test.
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-python-diagram-spec-extractors/03-CONTEXT.md
@.planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md
@.planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md
@.planning/phases/03-python-diagram-spec-extractors/03-VALIDATION.md

<interfaces>
<!-- Current contracts the executor reads (executor.py:82-110): PRIMITIVES loop with hardcoded 4 slots; CodeContent has only functions/call_graph/type_deps/contracts. -->
<!-- graph_base.py current EdgeKind = 11 values (lines 33-45); GraphEdge has source/target/edge_type/label/physical_module + extra="forbid" (no attributes field). GraphNode HAS attributes: dict[str,str]. -->
<!-- _dispatch.py: EVALUATIONS dict exists but empty (line 43); registration block at bottom lines 50-60 is the append-only analog. -->
<!-- contracts.py model conventions (models/primitives/contracts.py:22-55): SourceKind closed Literal, ContractEntry/ContractInfo, ConfigDict(extra="forbid"), Field(default_factory=...). -->
<!-- artifact.py CodeContent (lines 56-70) uses string forward refs + model_rebuild() at line 111; additive pattern is `contracts: dict[...] = Field(default_factory=dict)`. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append schema (EdgeKind imports + GraphEdge unresolved marker) and update the two by-design-breaking tests</name>
  <read_first>
    - lib_code_parser/models/evaluations/graph_base.py (FULL — EdgeKind at lines 33-45, GraphEdge at 64-83, the semantic comment table at 17-32)
    - tests/unit/models/test_graph_base.py (lines 1-68 — CANONICAL_EDGE_KINDS set lines 24-36, test_edge_kind_has_eleven_values lines 40-42, value-set assertion 44-49)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§"models/evaluations/graph_base.py (MODIFIED — append-only)" lines 253-257; §No Analog "DIA-06 unresolved marker" line 335)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§Return-Value Substitution Pitfall 1 lines 480-484; Open Question #1 lines 616-619; Open Question #2 lines 621-624)
  </read_first>
  <behavior>
    - GraphEdge(source="A", target="B", edge_type="imports") constructs successfully (new explicit semantic value, NOT a catch-all).
    - GraphEdge(source="A", target="B", edge_type="uses") still raises ValidationError (Pitfall 7 catch-all ban preserved).
    - GraphEdge(source="A", target="B", edge_type="transitions_to", source_unresolved=True) constructs successfully under extra="forbid".
    - GraphEdge default source_unresolved is False (parity preserved when omitted).
    - get_args(EdgeKind) returns the new count (12 if only `imports`; 13 if `contains` is also added).
  </behavior>
  <action>
    Append-only edit `graph_base.py` per D-01: add the literal value `"imports"` to the `EdgeKind` Literal (lines 33-45) AND add a matching one-line semantic comment in the table (lines 17-32) in the existing `name — meaning` style, e.g. `imports — module A imports module B (explicit semantic, not catch-all)`. DECISION (D-01 sub-decision, you own it): default to adding ONLY `imports`; express DIA-04 package containment via `GraphNode.attributes={"parent_package": ...}` (GraphNode already has `attributes`), so DO NOT add `contains` unless Plan 02's DIA-04 task proves a containment EDGE is required — if you add `contains`, mirror the same comment+test discipline. Existing 11 values are immutable; never add uses/other/misc/dep/depends. Resolve the DIA-06 marker home (Pitfall 1): add an optional field `source_unresolved: bool = False` to `GraphEdge` (SCH-02 `source_` prefix, keeps verifier-invisible parity), placed after `physical_module`. Then update BOTH breaking tests in THIS commit: in `tests/unit/models/test_graph_base.py` change `test_edge_kind_has_eleven_values` to the new count, add `"imports"` to `CANONICAL_EDGE_KINDS`, and add a positive test that `source_unresolved=True` validates + defaults False. (The `_dispatch.py`-count test is updated in Task 4.)
  </action>
  <verify>
    <automated>python -m pytest tests/unit/models/test_graph_base.py -q && python -m ruff check lib_code_parser/models/evaluations/graph_base.py && python -m ruff format --check lib_code_parser/models/evaluations/graph_base.py</automated>
  </verify>
  <done>graph_base.py EdgeKind includes "imports"; GraphEdge has source_unresolved (default False); catch-alls still rejected; test_graph_base.py asserts the new count and the new field, all green.</done>
  <acceptance_criteria>
    - `python -c "from lib_code_parser.models.evaluations.graph_base import EdgeKind; from typing import get_args; assert 'imports' in get_args(EdgeKind)"` exits 0
    - `python -c "from lib_code_parser.models.evaluations.graph_base import GraphEdge; GraphEdge(source='A',target='B',edge_type='transitions_to',source_unresolved=True)"` exits 0
    - `python -m pytest tests/unit/models/test_graph_base.py -q` exits 0
    - `ruff check` and `ruff format --check` exit 0 on lib_code_parser/models/evaluations/graph_base.py
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create models/evaluations/spec.py (FunctionSpec/ClassSpec/DocstringSection/SpecCondition) + extra="forbid" test</name>
  <read_first>
    - lib_code_parser/models/primitives/contracts.py (lines 1-55 — SourceKind/ContractKind closed Literal, ContractEntry, ConfigDict(extra="forbid"), Field default_factory conventions)
    - lib_code_parser/models/evaluations/graph_base.py (lines 48-83 — model conventions, physical_*/source_* optional-field discipline, default_factory)
    - lib_code_parser/models/evaluations/__init__.py (current __all__ — add new symbols)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§"models/evaluations/spec.py" lines 230-251 — verbatim Pydantic conventions to copy)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§Docstring Dialect Parsing "Section mapping" lines 401-403; §Auxiliary Contract Markers "Extending ContractInfo" lines 449 — SPC-04 source_kind set lives HERE not in frozen contracts.py)
    - tests/unit/models/test_graph_base.py (TestExtraForbid shape lines ~100-110, referenced by PATTERNS line 304)
  </read_first>
  <behavior>
    - FunctionSpec(node_id="m.f"), ClassSpec(node_id="m.C") construct with empty default collections.
    - FunctionSpec(unknown_field=1) raises ValidationError (extra="forbid").
    - DocstringSection and SpecCondition carry a closed Literal kind/source_kind discriminator; SPC-04 source_kind values (icontract_require/deal_pre/pep316_pre/...) live in this module's Literal, NOT in primitives/contracts.py.
  </behavior>
  <action>
    Create `lib_code_parser/models/evaluations/spec.py` with `from __future__ import annotations`, module docstring declaring `Traces: SPC-01, SPC-02, SPC-04`. Define four Pydantic models, each with `model_config = ConfigDict(extra="forbid")`: `DocstringSection` (fields: `kind: Literal["params","returns","raises","summary","other"]`, `name: str = ""`, `type_ref: str = ""`, `text: str = ""`), `SpecCondition` (fields: `kind: Literal["precondition","postcondition","invariant"]`, `text: str`, `source_kind: SpecConditionSourceKind`, `line_no: int = 0`), `FunctionSpec` (fields: `node_id: str`, `signature: str = ""`, `docstring_sections: list[DocstringSection] = Field(default_factory=list)`, `preconditions: list[SpecCondition] = Field(default_factory=list)`, `postconditions: list[SpecCondition] = Field(default_factory=list)`, optional `source_range: ... | None = None`), `ClassSpec` (fields: `node_id: str`, `definition: str = ""`, `members: list[str] = Field(default_factory=list)`, `invariants: list[SpecCondition] = Field(default_factory=list)`). Define `SpecConditionSourceKind` as a closed Literal covering `"docstring"`, `"icontract_require"`, `"icontract_ensure"`, `"icontract_invariant"`, `"deal_pre"`, `"deal_post"`, `"deal_ensure"`, `"deal_inv"`, `"pep316_pre"`, `"pep316_post"` (this is the SPC-04 home per RESEARCH §449 — keeps frozen primitives/contracts.py untouched). Add `model_rebuild()` at bottom only if forward refs are used. Export the four model names from `models/evaluations/__init__.py __all__`. Create `tests/unit/models/test_spec_extra_forbid.py` asserting construction + extra="forbid" rejection for each model (mirror test_graph_base.py TestExtraForbid).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/models/test_spec_extra_forbid.py -q && python -m ruff check lib_code_parser/models/evaluations/spec.py lib_code_parser/models/evaluations/__init__.py && python -m ruff format --check lib_code_parser/models/evaluations/spec.py lib_code_parser/models/evaluations/__init__.py</automated>
  </verify>
  <done>spec.py exports FunctionSpec/ClassSpec/DocstringSection/SpecCondition (all extra="forbid"); SpecConditionSourceKind carries the SPC-04 values; primitives/contracts.py is UNTOUCHED; test green.</done>
  <acceptance_criteria>
    - `python -c "from lib_code_parser.models.evaluations.spec import FunctionSpec, ClassSpec, DocstringSection, SpecCondition"` exits 0
    - `git diff --name-only` does NOT include lib_code_parser/models/primitives/contracts.py or lib_code_parser/extractors/primitives/contracts.py
    - `python -m pytest tests/unit/models/test_spec_extra_forbid.py -q` exits 0
    - `ruff check` and `ruff format --check` exit 0 on lib_code_parser/models/evaluations/spec.py and __init__.py
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 7 optional CodeContent slots + wire the executor EVALUATIONS walk (integration gaps a + b) + DIA-07 schema test scaffold</name>
  <read_first>
    - lib_code_parser/models/infrastructure/artifact.py (FULL — CodeContent lines 56-70, string-forward-ref + model_rebuild() at 91-111, the additive `contracts: dict[...] = Field(default_factory=dict)` pattern)
    - lib_code_parser/executor.py (FULL — PRIMITIVES loop lines 82-94 is the structural analog to copy; CodeContent construction lines 101-110)
    - lib_code_parser/_dispatch.py (FULL — EVALUATIONS dict line 43, registration analog 50-60)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§No Analog rows "executor.py EVALUATIONS walk" + "CodeContent diagram/spec slots" lines 333-334)
    - docs/09-extending.md (§invariant #6 `for name, fn in EVALUATIONS.items(): setattr(content, name, fn(cav, config))` design target + `config.is_enabled(name)` gating note)
    - lib_code_parser/models/infrastructure/config.py (current ParserConfig fields — to pick the gating mechanism)
  </read_first>
  <behavior>
    - CodeContent() (no args) still constructs and every new slot has an inert default (empty GraphModel / default-constructed spec container) — v0.1.0 callers unaffected.
    - After wiring, executor.execute on any Python source returns a NormalizedArtifact whose content has the 7 new slots present (empty GraphModels while no evaluation is registered — Plans 02–06 fill them).
    - The executor body change is dict-driven: adding a future evaluation needs only a _dispatch.py registration, never an executor edit (invariant #6).
  </behavior>
  <action>
    Edit `artifact.py` CodeContent (invariant #3 additive): add 7 optional fields with inert defaults — `class_diagram: "GraphModel" = Field(default_factory=GraphModel)`, `sequence_diagram`, `component_diagram`, `package_diagram`, `state_diagram` (all GraphModel), `function_spec: list["FunctionSpec"] = Field(default_factory=list)`, `class_spec: list["ClassSpec"] = Field(default_factory=list)`. Use string forward refs + extend the bottom `try/except` import block and `model_rebuild()` exactly as the existing primitive forward-ref discipline (lines 91-111). The CodeContent slot KEY names MUST equal the EVALUATIONS dict keys (class_diagram/sequence_diagram/component_diagram/package_diagram/state_diagram/function_spec/class_spec) so the executor can `setattr(content, name, ...)`. Wire executor (gap a): after the existing PRIMITIVES loop and the ContractInfo merger (executor.py:97-99), build the CodeContent with the 4 primitive slots as today, THEN add a loop `for name, eval_fn in EVALUATIONS.items(): if <per-evaluation gate>: setattr(content, name, eval_fn(cav, config))`. Choose the gating mechanism (your discretion per docs/09 `config.is_enabled`): simplest is run-all-registered (no gate) OR a `config`-driven allowlist — pick run-all-registered unless a ParserConfig flag already exists; document the choice in the executor docstring. Import `EVALUATIONS` from `_dispatch` alongside the existing `FRONTENDS, PRIMITIVES` import (executor.py:17). Keep the disabled/C++/unknown-language early-returns unchanged. Create `tests/unit/extractors/test_diagram_schema.py` (DIA-07 shared schema-conformance scaffold): a parametrizable helper `assert_valid_graphmodel(model)` that asserts the value is a `GraphModel` and that no extra/physical metadata leaks outside `physical_*`/`source_*` fields — Plans 02–04 will add cases that feed real diagram outputs through it. Execution note: if context budget tightens, the natural split boundary is between the `artifact.py` CodeContent additive edit and the `executor.py` EVALUATIONS loop wiring.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_executor.py tests/unit/extractors/test_diagram_schema.py -q && python -m ruff check lib_code_parser/models/infrastructure/artifact.py lib_code_parser/executor.py && python -m ruff format --check lib_code_parser/models/infrastructure/artifact.py lib_code_parser/executor.py</automated>
  </verify>
  <done>CodeContent has 7 additive default-empty slots; executor walks EVALUATIONS append-only after PRIMITIVES and setattrs each into its slot; CodeContent() with no args still works; v0.1.0 acceptance/parity tests still pass; DIA-07 schema helper exists.</done>
  <acceptance_criteria>
    - `python -c "from lib_code_parser.models.infrastructure.artifact import CodeContent; c=CodeContent(); assert hasattr(c,'class_diagram') and hasattr(c,'function_spec')"` exits 0
    - Grep gate: `grep -v '^#' lib_code_parser/executor.py | grep -c 'EVALUATIONS'` returns >= 2 (import + loop)
    - `python -m pytest tests/unit -q` exits 0 (no v0.1.0/Phase 2 regression)
    - `ruff check` and `ruff format --check` exit 0 on lib_code_parser/models/infrastructure/artifact.py and lib_code_parser/executor.py
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Wave-0 test scaffold — fixtures dir, conftest CAV builder, dispatch-count test update (integration gap d)</name>
  <read_first>
    - tests/unit/test_dispatch.py (lines 36-48 — test_primitives_dict_has_4_entries_in_append_only_order + test_evaluations_dict_exists_and_empty which BREAKS once Plans 02–06 register)
    - tests/unit/extractors/test_contracts_extractor.py (lines 19-24 — `_build_cav` + `_CONFIG` harness to lift into conftest)
    - .planning/phases/03-python-diagram-spec-extractors/03-VALIDATION.md (§Wave 0 Requirements lines 64-72; Per-Task Verification Map command names — these are the exact acceptance test filenames Plans 02–06 must create)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§"_dispatch.py (MODIFIED)" lines 259-276; §Fixtures lines 323-325)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§Required Test Fixtures lines 676-682)
  </read_first>
  <action>
    Create `tests/unit/extractors/fixtures/__init__.py` (empty package marker) and a `tests/unit/extractors/fixtures/` directory to hold golden/positive/negative source samples (Plans 02–06 add their own fixture strings). Add to `tests/conftest.py` two shared helpers lifted from test_contracts_extractor.py:19-24: a `build_python_cav(source: str, path: str) -> CAV` factory (test-side `ast.parse` only) and a reusable `parser_config` pytest fixture returning `ParserConfig(artifact_type="code", executor_lib="lib_code_parser")` — so every Plan 02–06 test imports one harness. Update integration gap (d) for `_dispatch.py`: change `test_dispatch.py::test_evaluations_dict_exists_and_empty` from asserting `len(EVALUATIONS)==0` to a forward-compatible assertion — replace it with `test_evaluations_registered_append_only` that asserts EVALUATIONS keys are a subset of the canonical 7-in-order list `["class_diagram","sequence_diagram","component_diagram","package_diagram","state_diagram","function_spec","class_spec"]` and that whatever IS present preserves that relative order (so it passes now with 0 entries AND after each later plan registers — mirror test_primitives_dict_has_4_entries_in_append_only_order). Do NOT register any EVALUATIONS entries in this plan (no extractor exists yet).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_dispatch.py -q</automated>
  </verify>
  <done>fixtures package exists; conftest exposes build_python_cav + parser_config; dispatch test is forward-compatible (passes with 0 entries today, will pass as 02–06 append in canonical order); no EVALUATIONS registered yet.</done>
  <acceptance_criteria>
    - `python -c "import tests.unit.extractors.fixtures"` (from repo root) exits 0 OR fixtures/__init__.py exists per `git ls-files tests/unit/extractors/fixtures/__init__.py`
    - `python -m pytest tests/unit/test_dispatch.py -q` exits 0
    - `python -c "from lib_code_parser._dispatch import EVALUATIONS; assert len(EVALUATIONS)==0"` exits 0 (still empty after this plan)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → library | Caller passes untrusted source `bytes` + `path` + `ParserConfig`. Decoded `utf-8, errors="replace"`; parsed by stdlib `ast.parse` (does NOT execute code). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (Layer M integrity) | new CodeContent slots / executor EVALUATIONS walk | mitigate | All new model fields default to inert empty values; no clock/env/network read; DET-04 sort-on-exit is enforced in extractor plans (02–06), preserving byte-identical output |
| T-03-02 | Information disclosure | GraphEdge.source_unresolved / physical_* fields | accept | Physical-side metadata is intentional, verifier-visible-by-prefix; no secrets, pure structural facts derived from caller-supplied source |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — Phase 3 installs ZERO packages (D-09/D-10 stdlib-only). No install tasks, no Package Legitimacy table needed; nothing to slopcheck |

> This plan adds no I/O, no subprocess, no network, no deserialization beyond `ast.parse`. No injection/auth/secret/web threats apply (pure in-process AST library).
</threat_model>

<verification>
- `python -m pytest tests/unit -q` green (no Phase 2 / v0.1.0 regression).
- `python -m pytest tests/acceptance -q` green (executor still produces v0.1.0-equivalent primitive output).
- `git diff --name-only` does not include any `extractors/primitives/*.py` or `models/primitives/contracts.py` (frozen Phase 2 files untouched — invariant #1).
</verification>

<success_criteria>
- EdgeKind has `imports`; GraphEdge has `source_unresolved` (default False); catch-alls still rejected.
- `models/evaluations/spec.py` exports FunctionSpec/ClassSpec/DocstringSection/SpecCondition with extra="forbid" and the SPC-04 source_kind Literal.
- CodeContent has 7 additive default-empty slots; executor walks EVALUATIONS dict-driven; v0.1.0 callers unaffected.
- conftest + fixtures scaffold ready; the two by-design-breaking tests (test_graph_base.py count, test_dispatch.py EVALUATIONS) are updated forward-compatibly.
</success_criteria>

<output>
Create `.planning/phases/03-python-diagram-spec-extractors/03-01-SUMMARY.md` when done.
</output>
