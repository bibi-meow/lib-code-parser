---
phase: 03-python-diagram-spec-extractors
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - lib_code_parser/extractors/evaluations/state_diagram.py
  - lib_code_parser/extractors/evaluations/_fsm_detect.py
  - lib_code_parser/_dispatch.py
  - tests/unit/extractors/test_state_diagram.py
  - tests/unit/extractors/test_state_substitution.py
  - tests/unit/extractors/test_diagram_schema.py
  - tests/unit/test_dispatch.py
  - tests/acceptance/test_dia05_state_fsm.py
  - tests/acceptance/test_dia06_return_substitution.py
  - tests/spikes/test_sp1_control_flow_state.py
  - tests/unit/extractors/fixtures/fsm_samples.py
  - .planning/spikes/SP-1-general-control-flow-state.md
autonomous: true
requirements: [DIA-05, DIA-06, DIA-07]
user_setup: []

must_haves:
  truths:
    - "D-08: SP-1 ship-vs-defer is judged SOLELY by 'is a deterministic rule constructible as a pure function'; the verdict (ship general control-flow→state OR defer DIA-05-FULL to v0.3.0) is recorded with that deterministic-rule rationale in .planning/spikes/SP-1-general-control-flow-state.md"
    - "Caller extracts state diagrams from 3 explicit FSM families: transitions.Machine(...), python-statemachine StateMachine subclass, native Enum + transition method (must-have per D-07)"
    - "class Color(Enum): RED,GREEN,BLUE produces ZERO state machines (fixture-asserted negative case)"
    - "Library detection uses import-provenance restriction: a user's own Machine/State function is NOT misdetected (no import → no FSM)"
    - "Return-value substitution resolves self.state=self._next() intra-class, N-level recursive, cycle-safe; fully-resolved → concrete edges; unresolvable → one placeholder edge with source_unresolved=True"
    - "State diagram output validates against GraphModel/GraphNode/GraphEdge/GuardExpr (DIA-07)"
  artifacts:
    - path: "lib_code_parser/extractors/evaluations/state_diagram.py"
      provides: "DIA-05/06 FSM + substitution extractor"
      contains: "def extract"
    - path: ".planning/spikes/SP-1-general-control-flow-state.md"
      provides: "SP-1 ship/defer verdict"
      contains: "verdict"
  key_links:
    - from: "lib_code_parser/extractors/evaluations/state_diagram.py"
      to: "contracts.py import-provenance pattern"
      via: "_resolve_aliases over target_pkgs (transitions, statemachine)"
      pattern: "transitions|statemachine"
    - from: "lib_code_parser/_dispatch.py"
      to: "EVALUATIONS state_diagram"
      via: "append-only registration"
      pattern: "EVALUATIONS\\[\"state_diagram\"\\]"
---

<objective>
Run the SP-1 spike FIRST (gating deliverable), then implement the state diagram extractor (DIA-05 explicit FSM families + DIA-06 return-value substitution). Per D-07 the 3 explicit-FSM families + return-value substitution are v0.2.0 must-haves regardless of SP-1; per D-08 the SP-1 verdict only decides whether general control-flow→state extraction (beyond explicit FSM) ships now or defers to v0.3.0 as DIA-05-FULL. The hardest algorithmic content in the phase lives here: deterministic FSM AST matching with import-provenance false-positive defense, plus N-level cycle-safe intra-class return resolution.

Purpose: deliver the FSM must-have (incl. the fixture-asserted negative case) and the SP-1 verdict.
Output: SP-1 spike doc + probe, _fsm_detect helper, state_diagram extractor, registration, unit + acceptance tests, DIA-07 schema case, FSM fixtures.
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-python-diagram-spec-extractors/03-CONTEXT.md
@.planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md
@.planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md
@.planning/phases/03-python-diagram-spec-extractors/03-01-SUMMARY.md
@.planning/phases/03-python-diagram-spec-extractors/03-03-SUMMARY.md

<interfaces>
<!-- After Plan 01: GraphEdge has source_unresolved: bool = False (the DIA-06 marker home). GuardExpr(from_state,to_state,condition,action). GraphNode has attributes:dict[str,str]. -->
<!-- contracts.py provenance pattern (extractors/primitives/contracts.py:89-140): _resolve_aliases(module, target_pkgs)->{local:canonical}; _is_attribute_form; _classify_* classifies ONLY when provenance established. -->
<!-- callgraph.py:_get_call_name (lines 34-40) for call/attr name extraction. -->
<!-- EdgeKind "transitions_to" already exists (no schema change). -->
<!-- Canonical EVALUATIONS order position #5 = state_diagram (after class,sequence,component,package). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SP-1 spike — probe deterministic general-control-flow→state, record verdict</name>
  <read_first>
    - .planning/phases/03-python-diagram-spec-extractors/03-CONTEXT.md (D-07/D-08 lines 102-116)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§Open Questions #3 lines 626-629 — SP-1 likely defers: state identity ambiguous without explicit state variable)
    - .planning/spikes/SP-2-sequence-branch-fidelity.md (Plan 03 — the spike-doc format to mirror)
  </read_first>
  <action>
    Create `tests/spikes/test_sp1_control_flow_state.py` with probe fixtures testing whether general control flow (beyond the 3 explicit FSM families) — e.g. a method that mutates `self.x` through nested conditionals without an explicit Enum/State variable — can be deterministically reduced to a state graph byte-identically. Write `.planning/spikes/SP-1-general-control-flow-state.md` recording the VERDICT (ship OR defer-to-v0.3.0-DIA-05-FULL) with the deterministic-rule rationale. D-08: judge ship-vs-defer SOLELY by "is a deterministic rule constructible as a pure function" (no LLM/heuristic) — RESEARCH predicts defer: state identity is ambiguous without an explicit state variable; confirm or refute with the probe. DO NOT let the verdict block the explicit-FSM must-have (D-07). The explicit 3 families + return-value substitution ship in Task 2/3 regardless.
  </action>
  <verify>
    <automated>python -m pytest tests/spikes/test_sp1_control_flow_state.py -q</automated>
  </verify>
  <done>SP-1 probe tests pass; .planning/spikes/SP-1-general-control-flow-state.md states a ship-or-defer verdict (judged per D-08 deterministic-rule criterion) with the deterministic-rule rationale.</done>
  <acceptance_criteria>
    - `git ls-files .planning/spikes/SP-1-general-control-flow-state.md` lists the file
    - Grep gate: `grep -v '^#' .planning/spikes/SP-1-general-control-flow-state.md | grep -ciE 'verdict|ship|defer'` returns >= 1
    - `python -m pytest tests/spikes/test_sp1_control_flow_state.py -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DIA-05 FSM detection (3 families + negative case) via import-provenance — _fsm_detect + state_diagram core</name>
  <read_first>
    - lib_code_parser/extractors/primitives/contracts.py (lines 89-140 — _resolve_aliases / _is_attribute_form / _classify_* provenance pattern to COPY; this is the single most important reuse for DIA-05)
    - lib_code_parser/extractors/primitives/callgraph.py (lines 34-40 — _get_call_name)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§FSM Detection AST Patterns lines 282-363 — Family A transitions.Machine kwargs, Family B python-statemachine State()/.to()/.from_/|-combine, Family C native Enum; §negative case lines 355-363; §Code Examples lines 522-570 _resolve_machine_kwargs + _is_to_transition)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§state_diagram.py lines 204-210; Shared 4 provenance lines 128-152)
    - tests/unit/extractors/test_contracts_extractor.py (false-positive defense test shapes lines 180-222 — copy for decoy Machine/no-import cases)
  </read_first>
  <behavior>
    - Family A: `Machine(states=['A','B'], transitions=[{'trigger':'go','source':'A','dest':'B'}])` (and list-of-list form `[['go','A','B']]`) → GraphNode(node_type="state") per state + GraphEdge(edge_type="transitions_to") per transition (label=trigger). Only resolvable when `Machine` resolves via import-provenance to `transitions`.
    - Family B: `class TM(StateMachine): a=State(initial=True); go=a.to(b)` → states from State() attrs, transitions from `src.to(dst)` (event=LHS name); `|`-combine → multiple edges one event; `target.from_(src)` reverse form handled.
    - Family C: native Enum-typed state attr + `self.state = EnumClass.MEMBER` literal reassignment → transition edge.
    - NEGATIVE: `class Color(Enum): RED=1;GREEN=2;BLUE=3` (no transition method, no self.state= reassignment) → ZERO states, ZERO GraphNode(node_type="state").
    - FALSE-POSITIVE DEFENSE: a user's own `Machine`/`State`/`require` with NO import of transitions/statemachine → NOT detected.
  </behavior>
  <action>
    Create `lib_code_parser/extractors/evaluations/_fsm_detect.py` (shared helper, stdlib-only): copy the contracts.py provenance trio (`_resolve_aliases(module, target_pkgs)`, attribute-form check, provenance-gated classify) parameterized for `target_pkgs=("transitions",)` and `("statemachine",)` / `("python_statemachine",)`. Implement three matchers per RESEARCH §FSM Detection: `_match_transitions_machine` (lift `_resolve_machine_kwargs` from RESEARCH §Code Examples lines 529-552 — handle list-of-dicts AND list-of-lists; only `ast.Constant` str literals resolvable), `_match_python_statemachine` (lift `_is_to_transition` lines 558-569 + `State()` attr detection + `|`-BinOp combine + `from_` reverse), `_match_native_enum` (Enum-typed state attr + literal `self.<attr> = EnumClass.MEMBER` reassignment; Family C REQUIRES BOTH an Enum state attr AND ≥1 transition assignment — this is what makes Color(Enum) a 0-FSM negative). Create `state_diagram.py` (DIA-05) with the Shared-1 skeleton; walk `cav.payload`, run the three matchers gated by import-provenance, emit GraphNode(node_type="state") + GraphEdge(edge_type="transitions_to") + GuardExpr when a guard/event/condition is present. DET-04 sort nodes/edges/guards. Register append-only `EVALUATIONS["state_diagram"]=...` at canonical position #5. Add DIA-07 case to test_diagram_schema.py. Create `tests/unit/extractors/fixtures/fsm_samples.py` with the 3 positive fixtures + the Color(Enum) negative + decoy `Machine` (no import) false-positive fixtures. Copy the false-positive defense test shapes from test_contracts_extractor.py:180-222 into test_state_diagram.py.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/extractors/test_state_diagram.py tests/unit/extractors/test_diagram_schema.py tests/unit/test_dispatch.py -q && python -m ruff check lib_code_parser/extractors/evaluations/_fsm_detect.py lib_code_parser/extractors/evaluations/state_diagram.py lib_code_parser/_dispatch.py && python -m ruff format --check lib_code_parser/extractors/evaluations/_fsm_detect.py lib_code_parser/extractors/evaluations/state_diagram.py lib_code_parser/_dispatch.py</automated>
  </verify>
  <done>3 FSM families detected via import-provenance; Color(Enum) → 0 states (fixture-asserted); decoy Machine without import not detected; state_diagram registered #5; DIA-07 case green.</done>
  <acceptance_criteria>
    - `python -m pytest tests/acceptance/test_dia05_state_fsm.py -q` exits 0
    - The negative-case assertion `len([n for n in result.state_diagram.nodes if n.node_type=='state'])==0` for `class Color(Enum)` input passes (asserted in test_state_diagram.py)
    - Grep gate: `grep -v '^#' lib_code_parser/extractors/evaluations/state_diagram.py lib_code_parser/extractors/evaluations/_fsm_detect.py | grep -cE 'import transitions|import statemachine|from transitions|from statemachine'` returns 0 (detection only — never import the target libs, D-10)
    - `ruff check` and `ruff format --check` exit 0 on _fsm_detect.py, state_diagram.py and _dispatch.py
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DIA-06 return-value substitution (N-level, cycle-safe; unresolved placeholder)</name>
  <read_first>
    - lib_code_parser/extractors/evaluations/state_diagram.py (the Task 2 module — extend it, do not create a new extractor)
    - lib_code_parser/extractors/evaluations/_fsm_detect.py (Task 2 native-Enum matcher — DIA-06 fires on the non-literal `self.state = self._next()` form)
    - lib_code_parser/models/evaluations/graph_base.py (GraphEdge.source_unresolved from Plan 01 — the DIA-06 marker home)
    - .planning/phases/03-python-diagram-spec-extractors/03-RESEARCH.md (§Return-Value Substitution Algorithm lines 365-381 — full algorithm: resolve callee intra-class, collect ast.Return, recurse with visited set, cycle detection, fully-resolved→concrete edges, unresolvable→one placeholder with source_unresolved)
    - .planning/phases/03-python-diagram-spec-extractors/03-PATTERNS.md (§state_diagram schema reconciliation lines 210; §No Analog DIA-06 marker line 335)
  </read_first>
  <behavior>
    - `self.state = self._next()` where `_next` returns `Color.A` / `Color.B` (both literals) → emit concrete GraphEdge(transitions_to) to A and B.
    - N-level: `_next` returns `self._other()` which returns a literal → resolve through recursion.
    - Cycle: `_a → _b → _a` → cycle-safe (visited set), no infinite loop, branch yields no new literal.
    - Unresolvable (external call, or a path that can't reduce to a literal) → exactly ONE placeholder GraphEdge with source_unresolved=True.
  </behavior>
  <action>
    Extend `state_diagram.py` with the return-value substitution algorithm (RESEARCH §Return-Value Substitution lines 365-381): when the native-Enum matcher finds a NON-literal transition assignment `self.<attr> = self._method()`, resolve `_method` intra-class (match by `ast.FunctionDef.name` within the enclosing `ast.ClassDef`); walk callee body for `ast.Return` nodes; for `return EnumClass.MEMBER`/`ast.Constant` → resolved literal; for `return self._other()` → recurse with a `visited: set[str]` of method names (cycle detection — re-entry stops that branch); for conditional/ternary/variable → unresolved fragment. Fully resolved (all paths → literals) → emit one transitions_to edge per distinct resolved target. Unresolvable (any path can't reduce, or callee external/not-in-class) → emit exactly one placeholder GraphEdge with `source_unresolved=True` (the Plan 01 marker home — NOT `attributes`, GraphEdge has none). Recursion is bounded by finite method count + visited set (termination guaranteed). DET-04 sort. Create `tests/unit/extractors/test_state_substitution.py` with the resolved / N-level / cyclic / external fixtures (RESEARCH §Required Test Fixtures lines 680).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/extractors/test_state_substitution.py -q && python -m ruff check lib_code_parser/extractors/evaluations/state_diagram.py && python -m ruff format --check lib_code_parser/extractors/evaluations/state_diagram.py</automated>
  </verify>
  <done>Return-value substitution resolves literals (incl. N-level), is cycle-safe, and emits exactly one source_unresolved=True placeholder for unresolvable cases; tests green.</done>
  <acceptance_criteria>
    - `python -m pytest tests/acceptance/test_dia06_return_substitution.py -q` exits 0
    - The cyclic fixture (`_a→_b→_a`) test completes (no hang/recursion error) — asserted by the test running to green within the suite timeout
    - `python -m pytest tests/unit/extractors -q` exits 0
    - `ruff check` and `ruff format --check` exit 0 on state_diagram.py (touched in this task)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → library | Untrusted source bytes; stdlib `ast` walk over `cav.payload` (no execution). FSM libraries are DETECTED by AST shape, NEVER imported (D-10). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Denial of Service (recursion exhaustion) | DIA-06 return-value substitution walker | mitigate | Recursion is bounded by finite intra-class method count + a `visited: set` guaranteeing termination; cyclic `_a→_b→_a` fixture asserts no infinite loop. This is the SC4-mandated cycle-detection + N-level bound — the phase's one real DoS vector, explicitly bounded |
| T-03-08 | Spoofing (data integrity) | FSM library detection | mitigate | Import-provenance restriction (contracts.py pattern): a user's own Machine/State/require is NOT classified without a real import of transitions/statemachine — false-positive defense test asserted |
| T-03-09 | Elevation of privilege (code exec) | parsing FSM literals | mitigate | `ast.literal_eval` used ONLY on `ast.Constant` nodes (never arbitrary AST); FSM libs never imported/executed (D-10) |
| T-03-10 | Tampering (Layer M integrity) | state_diagram output | mitigate | DET-04 sort-on-exit; no clock/env/network; SP-1 general-control-flow ships only if deterministic |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — zero package installs; transitions/python-statemachine are detection targets, NOT installed (D-10) |
</threat_model>

<verification>
- `python -m pytest tests/unit tests/acceptance tests/spikes -q` green.
- SP-1 verdict doc exists with deterministic rationale.
- Color(Enum) negative case asserts 0 states; cyclic substitution terminates; no `import transitions`/`import statemachine` anywhere in source.
- `PYTHONHASHSEED=random python -m pytest tests/unit/extractors -q` green.
</verification>

<success_criteria>
- SP-1 spike verdict recorded; explicit-FSM must-have (3 families + negative case) shipped regardless of verdict.
- DIA-06 return-value substitution: N-level, cycle-safe, source_unresolved placeholder for unresolvable.
- state_diagram registered append-only #5; output valid GraphModel/GuardExpr; DET-04-sorted; no target-lib imports.
</success_criteria>

<output>
Create `.planning/phases/03-python-diagram-spec-extractors/03-04-SUMMARY.md` when done.
</output>
