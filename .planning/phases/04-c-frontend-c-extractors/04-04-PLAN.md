---
phase: 04-c-frontend-c-extractors
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - lib_code_parser/_cpp_cursor.py
  - lib_code_parser/extractors/primitives/cpp_functions.py
  - lib_code_parser/extractors/primitives/cpp_callgraph.py
  - lib_code_parser/extractors/primitives/cpp_type_deps.py
  - lib_code_parser/_dispatch.py
  - tests/unit/extractors/test_cpp_functions.py
  - tests/unit/extractors/test_cpp_callgraph.py
  - tests/unit/extractors/test_cpp_type_deps.py
autonomous: true
requirements: [LNG-04, LNG-05, TRC-03]
nyquist_compliant: true

must_haves:
  truths:
    - "D-04: cpp_functions emits FunctionNode for free function (FUNCTION_DECL->kind='function'), class/struct (CLASS_DECL/STRUCT_DECL->kind='class'), and method (CXX_METHOD->kind='method') with params/return_type/source_range; cpp_type_deps emits member type deps + #include deps"
    - "D-04: node_id derivation is namespace-qualified (a.b.Class.method) or get_usr()-based - stable across runs; cpp_callgraph emits CallEdge(caller,callee) from CALL_EXPR/MEMBER_REF_EXPR"
    - "D-05: template/macro/overload completeness is best-effort; unresolved members fall back to associates-equivalent, never a fabricated edge; #include parsing tolerates unresolved headers (warns not errors)"
    - "DET-04 parity: every cpp primitive sorts on exit by the SAME composite keys as its Python sibling (FunctionNode by node_id, CallGraph edges by (caller,callee), TypeDep by (source,target,kind,source_line))"
    - "TRC-03 parity: cpp_functions feeds cursor.raw_comment into the BYTE-IDENTICAL Traces: regex used by the Python functions extractor"
    - "Each cpp primitive asserts isinstance(cav.payload, clang.cindex.TranslationUnit) and NEVER branches on cav.language (invariant #2)"
  artifacts:
    - path: "lib_code_parser/_cpp_cursor.py"
      provides: "shared cursor-walk helpers: main-file filter, USR/qualified node_id, verbatim TRC-03 trace-tag regex, composes/aggregates field-relation classifier"
      contains: "_TRACE_TAGS_RE"
    - path: "lib_code_parser/extractors/primitives/cpp_functions.py"
      provides: "FunctionNode extraction from libclang cursors"
      contains: "TranslationUnit"
    - path: "lib_code_parser/extractors/primitives/cpp_callgraph.py"
      provides: "CallGraph from CALL_EXPR/MEMBER_REF_EXPR"
    - path: "lib_code_parser/extractors/primitives/cpp_type_deps.py"
      provides: "TypeDep from #include + member types (no pyright path)"
    - path: "lib_code_parser/_dispatch.py"
      provides: "PRIMITIVES['cpp'] registrations for functions/call_graph/type_deps"
      contains: "PRIMITIVES[\"cpp\"]"
  key_links:
    - from: "lib_code_parser/extractors/primitives/cpp_functions.py"
      to: "lib_code_parser/_cpp_cursor.py"
      via: "shared TRC-03 regex + main-file filter + node_id helper"
      pattern: "_cpp_cursor"
    - from: "lib_code_parser/_dispatch.py"
      to: "lib_code_parser/extractors/primitives/cpp_functions.py"
      via: "PRIMITIVES['cpp']['functions'] = extract"
      pattern: "PRIMITIVES\\[\"cpp\"\\]\\[\"functions\"\\]"
---

<objective>
Implement the three C++ AST primitive extractors - `cpp_functions` (FunctionNode), `cpp_callgraph` (CallGraph), `cpp_type_deps` (TypeDep from #include + member types) - plus the shared `_cpp_cursor.py` helper module (main-file filter, USR/qualified node_id, verbatim TRC-03 regex, field-relation classifier). Each emits the EXISTING Pydantic shapes unchanged (LNG-04 parity) with DET-04 sort-on-exit, and registers into `PRIMITIVES["cpp"]`.

Purpose: These are the C++ analogs of the Python primitive extractors (D-04 must-have surface). The shared `_cpp_cursor.py` keeps the TRC-03 trace regex byte-identical to Python and centralizes the composes/aggregates classifier the class diagram will reuse. No model changes (invariant #1).
Output: `_cpp_cursor.py`, 3 cpp primitive modules, PRIMITIVES["cpp"] registrations, unit tests.
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-c-frontend-c-extractors/04-RESEARCH.md
@.planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md
@.planning/phases/04-c-frontend-c-extractors/04-CONTEXT.md

<interfaces>
<!-- All cpp primitives mirror the Python signature: -->
def extract(cav: CAV, config: ParserConfig) -> ConcreteType:
    assert isinstance(cav.payload, clang.cindex.TranslationUnit), "name extractor requires C++ CAV ..."

<!-- TRC-03 regex (functions.py L32) - copy VERBATIM for byte-identical Python/C++ behavior: -->
_TRACE_TAGS_RE = re.compile(r"Traces:\s*([A-Z]+-\d+(?:\s*,\s*[A-Z]+-\d+)*)", re.MULTILINE)

<!-- DET-04 sort keys (verbatim from Python siblings):
       CallGraph: edges.sort(key=lambda e: (e.caller, e.callee)); nodes via list(dict.fromkeys(...))
       TypeDep:   raw_deps.sort(key=lambda d: (d.source, d.target, d.kind, d.source_line))
       FunctionNode list: sort by node_id -->

<!-- Field-relation rule (RESEARCH C++ AST section, verified):
       FIELD_DECL.type.kind in (POINTER, LVALUEREFERENCE) -> aggregates (use get_pointee().spelling)
       ELABORATED/RECORD of a known main-file class -> composes
       unresolved/unknown-class/template-dependent -> associates  (never a "uses"/"other" catch-all)
       builtin primitive -> no edge -->

<!-- Cursor kinds: FUNCTION_DECL, CLASS_DECL, STRUCT_DECL, CXX_METHOD, FIELD_DECL, NAMESPACE,
     PARM_DECL, CALL_EXPR, MEMBER_REF_EXPR, INCLUSION_DIRECTIVE.  TypeKind: POINTER, LVALUEREFERENCE,
     ELABORATED, RECORD.  node_id via cursor.get_usr() or namespace-qualified dotted name. -->
<!-- PRIMITIVES['cpp'] keys MUST match the Python spelling: "functions","call_graph","type_deps". -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared _cpp_cursor.py helpers (main-file filter, node_id, TRC-03 regex, field classifier)</name>
  <files>lib_code_parser/_cpp_cursor.py</files>
  <read_first>
    - lib_code_parser/_paths.py (the single-source-helper idiom for module-name derivation)
    - lib_code_parser/extractors/primitives/functions.py (TRC-03 regex L32 + _extract_trace_tags L42-48 to copy verbatim)
    - lib_code_parser/extractors/evaluations/class_diagram.py (_classify_annotation / _collect_known_classes - the Python analog of the field classifier)
    - .planning/phases/04-c-frontend-c-extractors/04-RESEARCH.md section "Deterministic main-file cursor walk" (_in_main_file / _field_relation L402-421) + Pitfall 3 (main-file filter)
    - .planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md section "Shared Patterns" (language guard, sort-on-exit, TRC-03 regex L402-408)
  </read_first>
  <behavior>
    - _in_main_file(cursor, path) returns True only when cursor.location.file is not None and its name == path (drops builtin/header decls - Pitfall 3).
    - _TRACE_TAGS_RE is byte-identical to functions.py; extract_trace_tags(text)->list[TraceTag] is the verbatim copy that downstream cpp_functions/cpp_contracts share.
    - node_id helper produces a stable, namespace-qualified dotted id (a.b.Class.method) or falls back to cursor.get_usr() - identical id for identical input across runs.
    - field_relation(field_cursor, known_classes)->tuple[str,str]|None implements the verified composes/aggregates/associates/none rule using TypeKind + get_pointee(); returns None for builtin primitives.
  </behavior>
  <action>Create `_cpp_cursor.py` (mirroring the single-source `_paths.py` idiom) exporting: `_in_main_file(cursor, path)` per RESEARCH L407-409; `_TRACE_TAGS_RE` and `extract_trace_tags(text)` copied VERBATIM from `functions.py` (the regex literal must be byte-identical so TRC-03 parity holds - D-09); a `qualified_node_id(cursor)` building a dotted namespace-qualified name (walk `cursor.semantic_parent` chain collecting NAMESPACE/CLASS/STRUCT spellings) with `cursor.get_usr()` as the deterministic fallback; and `field_relation(field_cursor, known_classes)` per RESEARCH L411-419 (POINTER/LVALUEREFERENCE via `get_pointee().spelling` -> aggregates if base in known_classes else associates; ELABORATED/RECORD -> composes if known else associates; builtin -> None). Use `clang.cindex.CursorKind`/`TypeKind` enums by name. This module imports libclang at top level (only loaded on the cpp path - same rationale as frontends/cpp.py).</action>
  <verify>
    <automated>cd C:/work/agent_company/spec-reviewer-libs/lib-code-parser && python -c "from lib_code_parser import _cpp_cursor as h; import pathlib,re; fn=pathlib.Path('lib_code_parser/extractors/primitives/functions.py').read_text(); m=re.search(r'_TRACE_TAGS_RE = re\.compile\((.*?)\)', fn); assert m and m.group(1) in pathlib.Path('lib_code_parser/_cpp_cursor.py').read_text(), 'TRC-03 regex must be byte-identical'; tags=h.extract_trace_tags('Traces: REQ-9, US-3'); assert tags and tags[0].refs==['REQ-9','US-3']; print('OK')"</automated>
  </verify>
  <done>`_cpp_cursor.py` exports the main-file filter, the byte-identical TRC-03 regex + `extract_trace_tags`, the qualified node_id helper, and the field-relation classifier; the regex literal matches `functions.py` exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cpp_functions + cpp_callgraph extractors with DET-04 sort + PRIMITIVES['cpp'] registration</name>
  <files>lib_code_parser/extractors/primitives/cpp_functions.py, lib_code_parser/extractors/primitives/cpp_callgraph.py, lib_code_parser/_dispatch.py, tests/unit/extractors/test_cpp_functions.py, tests/unit/extractors/test_cpp_callgraph.py</files>
  <read_first>
    - lib_code_parser/extractors/primitives/functions.py (signature + language guard L73,81-84; two-pass emit + node_id L88-136; FunctionNode/ParamInfo/SourceRange/TraceTag imports L23-28)
    - lib_code_parser/extractors/primitives/callgraph.py (sort-on-exit L93-96; CallEdge/CallGraph models; dict.fromkeys node dedup)
    - lib_code_parser/_cpp_cursor.py (Task 1 helpers)
    - .planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md section "cpp_functions.py" (L70-99) + "cpp_callgraph.py" (L103-115)
    - lib_code_parser/_dispatch.py (post-04-03 - PRIMITIVES["cpp"] empty sub-dict + append-only idiom)
  </read_first>
  <behavior>
    - cpp_functions.extract(cav,config)->list[FunctionNode]: asserts TranslationUnit payload; walks main-file cursors; CLASS_DECL/STRUCT_DECL->FunctionNode(kind="class"), CXX_METHOD->kind="method" (node_id Class.method), FUNCTION_DECL->kind="function"; params from PARM_DECL; return_type from cursor.result_type.spelling; source_range from cursor.extent; trace_tags from cursor.raw_comment via the shared extract_trace_tags; module docstring declares "Traces: LNG-04, TRC-03".
    - cpp_callgraph.extract(cav,config)->CallGraph: collects CallEdge(caller=enclosing method/function node_id, callee=call cursor spelling) from CALL_EXPR/MEMBER_REF_EXPR within main-file function bodies; emits CallGraph(nodes=list(dict.fromkeys(nodes)), edges sorted by (caller,callee)).
    - Both sort on exit (FunctionNode list by node_id; CallGraph edges by (caller,callee)) and assert payload type; NEITHER branches on cav.language.
    - PRIMITIVES["cpp"]["functions"] and PRIMITIVES["cpp"]["call_graph"] registered.
  </behavior>
  <action>Write `cpp_functions.py` mirroring `functions.py`'s structure: module docstring with `Implements: LNG-04` + `Traces: LNG-04, TRC-03`, the `assert isinstance(cav.payload, clang.cindex.TranslationUnit)` guard with the symmetric message, a recursive main-file walk using `_cpp_cursor._in_main_file`, kind mapping (CLASS_DECL/STRUCT_DECL->"class", CXX_METHOD->"method", FUNCTION_DECL->"function"), node_id via `_cpp_cursor.qualified_node_id`, params from PARM_DECL children (read functions.py for the ParamInfo fields), `return_type=cursor.result_type.spelling`, source_range from `cursor.extent.start.line`/`.end.line` into `SourceRange`, `trace_tags=_cpp_cursor.extract_trace_tags(cursor.raw_comment or "")`. Sort the returned list by `node_id`. Reuse the existing `FunctionNode/ParamInfo/SourceRange/TraceTag` models unchanged (invariant #1). Write `cpp_callgraph.py` mirroring `callgraph.py`: collect `CallEdge(caller=<enclosing node_id>, callee=<call cursor spelling>)` by walking method/function bodies for CALL_EXPR/MEMBER_REF_EXPR, then `edges.sort(key=lambda e: (e.caller, e.callee))` and `CallGraph(nodes=list(dict.fromkeys(nodes)), edges=edges)` verbatim. Author the two unit tests using `build_cpp_cav` + the `inheritance.cpp` fixture: assert kind discrimination, node_id qualification, trace-tag extraction, and (callgraph) edge sort order. In `_dispatch.py`, append `# noqa: E402` imports and `PRIMITIVES["cpp"]["functions"] = _extract_cpp_functions` / `PRIMITIVES["cpp"]["call_graph"] = _extract_cpp_callgraph` (exact key spelling, append-only into the existing `["cpp"]` sub-dict).</action>
  <verify>
    <automated>cd C:/work/agent_company/spec-reviewer-libs/lib-code-parser && python -m pytest tests/unit/extractors/test_cpp_functions.py tests/unit/extractors/test_cpp_callgraph.py -x -q && python -c "from lib_code_parser._dispatch import PRIMITIVES; assert 'functions' in PRIMITIVES['cpp'] and 'call_graph' in PRIMITIVES['cpp']; print('OK')"</automated>
  </verify>
  <done>cpp_functions emits the three FunctionNode kinds with qualified node_ids + trace tags; cpp_callgraph emits (caller,callee)-sorted edges; both registered under PRIMITIVES["cpp"]; unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cpp_type_deps extractor (#include + member types, no pyright path) + registration</name>
  <files>lib_code_parser/extractors/primitives/cpp_type_deps.py, lib_code_parser/_dispatch.py, tests/unit/extractors/test_cpp_type_deps.py</files>
  <read_first>
    - lib_code_parser/extractors/primitives/type_deps.py (the DEFAULT pure path L143-145; TypeDep model + sort key; SKIP the pyright/PyrightAdapter branch L147-171 - Python-only)
    - lib_code_parser/_cpp_cursor.py (main-file filter, field_relation for member type deps)
    - .planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md section "cpp_type_deps.py" (L119-132) + RESEARCH Open Question 3 (#include via regex on raw_content)
    - lib_code_parser/_dispatch.py (PRIMITIVES["cpp"] append-only)
  </read_first>
  <behavior>
    - cpp_type_deps.extract(cav,config)->list[TypeDep]: asserts TranslationUnit payload; emits TypeDep(kind="imports") from #include directives parsed by regex over cav.raw_content (deterministic, avoids the macro-polluting PARSE_DETAILED_PROCESSING_RECORD flag) and TypeDep member-type deps from FIELD_DECL types in main-file classes.
    - NO pyright/adapter branch (C++ has no pyright equivalent; libclang is in-process per D-06).
    - Unresolved/template-dependent member types do not crash; #include of a missing header still yields an import TypeDep (warn-not-error, LNG-05) - no raise.
    - Sorted on exit by (source, target, kind, source_line) - verbatim DET-04 key.
    - PRIMITIVES["cpp"]["type_deps"] registered.
  </behavior>
  <action>Write `cpp_type_deps.py` mirroring the DEFAULT (non-pyright) path of `type_deps.py`. Parse `#include` lines from `cav.raw_content.decode("utf-8", errors="replace")` with a regex like `^\s*#\s*include\s*[<"]([^>"]+)[>"]` (MULTILINE) to emit `TypeDep(source=<module>, target=<included header>, kind="imports", source_line=<lineno>)` - this is the RESEARCH Open Question 3 recommendation (regex on raw_content, no detailed-processing flag). Add member-type deps by walking main-file FIELD_DECL cursors and emitting `TypeDep(source=<class node_id>, target=<member type base>, kind="uses", source_line=...)` for resolvable types. Use `_cpp_cursor` helpers (main-file filter, field base extraction). End with `raw_deps.sort(key=lambda d: (d.source, d.target, d.kind, d.source_line))` (verbatim). Reuse the `TypeDep` model unchanged; `TypeDep.kind` is free-form str so cpp-specific kinds are allowed without a MAJOR bump. Do NOT import or branch into any pyright/adapter code. Author the unit test using `includes.cpp` + `relations.cpp` fixtures: assert import deps appear, a missing-header include still yields an import TypeDep without raising, and the sort key holds. In `_dispatch.py`, append the `# noqa: E402` import + `PRIMITIVES["cpp"]["type_deps"] = _extract_cpp_type_deps`.</action>
  <verify>
    <automated>cd C:/work/agent_company/spec-reviewer-libs/lib-code-parser && python -m pytest tests/unit/extractors/test_cpp_type_deps.py -x -q && python -c "import pathlib; s=pathlib.Path('lib_code_parser/extractors/primitives/cpp_type_deps.py').read_text(); assert 'pyright' not in s.lower() and 'adapter' not in s.lower(), 'no pyright/adapter path in cpp'; from lib_code_parser._dispatch import PRIMITIVES; assert 'type_deps' in PRIMITIVES['cpp']; print('OK')"</automated>
  </verify>
  <done>cpp_type_deps emits #include import deps (regex on raw_content) + member-type deps, sorted by (source,target,kind,source_line), with no pyright/adapter path; registered under PRIMITIVES["cpp"]; unit test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cpp CAV payload (TranslationUnit) + raw_content -> extractors | The extractors read an already-parsed TranslationUnit and the carried raw bytes; no new external input crosses here (the parse happened in the frontend). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | extractor payload-type assumption | mitigate | Each extractor asserts `isinstance(cav.payload, TranslationUnit)`; the nested dispatch (04-01) guarantees cpp extractors only ever receive cpp CAV, so the assert documents and enforces the precondition. |
| T-04-09 | Integrity | #include regex over attacker source | accept | The regex only reads header names to build deterministic import edges; it never opens or reads the referenced files. Unresolved headers are edges/warnings, not file-read attempts. |
</threat_model>

<verification>
- 3 cpp primitives emit existing Pydantic shapes with DET-04 sort-on-exit.
- TRC-03 regex byte-identical to Python (shared helper).
- No pyright/adapter path in cpp_type_deps (D-06 in-process boundary).
- `pytest tests/unit/extractors/test_cpp_*.py` green.
</verification>

<success_criteria>
- PRIMITIVES["cpp"] holds functions/call_graph/type_deps with the Python key spelling.
- node_id stable + namespace-qualified; composes/aggregates/associates classifier centralized in _cpp_cursor.
- Member + #include deps deterministic; unresolved headers warn-not-error.
</success_criteria>

<output>
Create `.planning/phases/04-c-frontend-c-extractors/04-04-SUMMARY.md` when done.
</output>
