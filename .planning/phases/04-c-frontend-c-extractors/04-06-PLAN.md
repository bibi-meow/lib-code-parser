---
phase: 04-c-frontend-c-extractors
plan: 06
type: execute
wave: 5
depends_on: ["04-04", "04-05"]
files_modified:
  - lib_code_parser/extractors/evaluations/cpp_class_diagram.py
  - lib_code_parser/extractors/evaluations/cpp_component_diagram.py
  - lib_code_parser/extractors/evaluations/cpp_sequence_diagram.py
  - lib_code_parser/extractors/evaluations/cpp_package_diagram.py
  - lib_code_parser/extractors/evaluations/cpp_state_diagram.py
  - lib_code_parser/_dispatch.py
  - tests/acceptance/test_cpp_class_diagram.py
  - tests/parity/test_cpp_python_schema_parity.py
  - tests/unit/frontends/test_cpp_determinism.py
autonomous: true
requirements: [LNG-04, LNG-05]
nyquist_compliant: true

must_haves:
  truths:
    - "D-04: cpp_class_diagram emits inherits edges (CXX_BASE_SPECIFIER, incl. multiple) + composes/aggregates/associates edges via the value-member/pointer-or-reference/unresolvable rule (associates is the explicit undecidable fallback, NEVER a 'uses'/'other' catch-all)"
    - "D-04: cpp_component_diagram (imports edges from #include), cpp_package_diagram (namespace nesting -> node_type='package' with attributes['parent_package']), and cpp_sequence_diagram (linear calls edges from cpp callgraph) emit the same GraphNode/GraphEdge/GraphModel slots as their Python siblings"
    - "A1 / D-05: cpp_state_diagram emits an empty GraphModel (zero state nodes) for v0.2.0 - parity-as-empty-shape, identical to Python Color(Enum) yielding zero FSMs - and this is fixture-asserted on the not_a_state_machine.cpp fixture, not silently skipped"
    - "LNG-04: the cpp NormalizedArtifact has byte-identical Pydantic shape (same CodeContent slots) to the Python output (structural parity assertion through the public executor)"
    - "DET-04: every cpp diagram ends with sort-on-exit (nodes by node_id, edges by (source,target,edge_type,label)); per-extractor output is byte-identical across 3 runs"
    - "Every EVALUATIONS['cpp'] key validates against the shared CodeContent slot guard (LNG-04 parity is automatic because slot names are common across languages)"
  artifacts:
    - path: "lib_code_parser/extractors/evaluations/cpp_class_diagram.py"
      provides: "inheritance + composes/aggregates/associates class diagram"
      contains: "GraphModel"
    - path: "lib_code_parser/extractors/evaluations/cpp_state_diagram.py"
      provides: "empty-GraphModel C++ state diagram (parity-as-empty-shape, A1/D-05)"
    - path: "lib_code_parser/_dispatch.py"
      provides: "EVALUATIONS['cpp'] registrations for the 5 diagrams"
      contains: "EVALUATIONS[\"cpp\"]"
    - path: "tests/parity/test_cpp_python_schema_parity.py"
      provides: "LNG-04 structural parity assertion (cpp vs Python CodeContent shape)"
    - path: "tests/unit/frontends/test_cpp_determinism.py"
      provides: "per-extractor 3-run byte-identity"
  key_links:
    - from: "lib_code_parser/extractors/evaluations/cpp_component_diagram.py"
      to: "lib_code_parser/extractors/primitives/cpp_type_deps.py"
      via: "pull-a-primitive (invariant #5): import cpp_type_deps directly"
      pattern: "cpp_type_deps"
    - from: "lib_code_parser/_dispatch.py"
      to: "lib_code_parser/extractors/evaluations/cpp_class_diagram.py"
      via: "EVALUATIONS['cpp']['class_diagram'] = extract"
      pattern: "EVALUATIONS\\[\"cpp\"\\]\\[\"class_diagram\"\\]"
---

<objective>
Implement the five C++ diagram evaluation extractors — `cpp_class_diagram` (inheritance + composes/aggregates/associates), `cpp_component_diagram` (#include imports), `cpp_sequence_diagram` (linear calls from cpp callgraph), `cpp_package_diagram` (namespace nesting → packages), and `cpp_state_diagram` (empty GraphModel, parity-as-empty-shape) — each emitting the SAME `GraphNode`/`GraphEdge`/`GraphModel` slots as its Python sibling with DET-04 sort-on-exit. Register `EVALUATIONS["cpp"]`. Prove LNG-04 structural parity (cpp ≡ Python CodeContent shape) and per-extractor 3-run determinism.

Purpose: Completes the C++ track's diagram surface (D-04/D-05) and closes the phase's LNG-04 schema-parity success criterion. The state diagram's empty-output decision (A1) is resolved here per the planning directive: emit the correct empty Pydantic shape, fixture-asserted.
Output: 5 cpp diagram modules, EVALUATIONS["cpp"] registrations, LNG-04 parity test, class-diagram acceptance test, per-extractor determinism test.
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-c-frontend-c-extractors/04-RESEARCH.md
@.planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md
@.planning/phases/04-c-frontend-c-extractors/04-CONTEXT.md

<interfaces>
<!-- Every cpp diagram: def extract(cav, config) -> GraphModel; assert TranslationUnit payload; sort-on-exit. -->
<!-- DET-04 diagram sort tail (verbatim from class_diagram.py L359-366):
       node_ids = list(dict.fromkeys(node_ids))
       nodes.sort(key=lambda n: n.node_id)
       edges.sort(key=lambda e: (e.source, e.target, e.edge_type, e.label))
       return GraphModel(nodes=nodes, edges=edges) -->
<!-- GraphEdge.edge_type stays in THIS lib's vocabulary: inherits/composes/aggregates/associates/imports/calls.
     EdgeKind is a closed 11-value Literal; NEVER add "uses"/"other"/"misc". -->
<!-- Field-relation classifier lives in _cpp_cursor.field_relation (composes/aggregates/associates/None). -->
<!-- Pull-a-primitive (invariant #5): component/sequence import cpp_type_deps / cpp_callgraph directly. -->
<!-- package_diagram: node_type="package", attributes={"parent_package": parent} (NO contains edge). -->
<!-- EVALUATIONS['cpp'] keys MUST equal the Python slot names: class_diagram, sequence_diagram,
     component_diagram, package_diagram, state_diagram. (function_spec/class_spec are Python-only; NOT
     registered under cpp - the executor walks EVALUATIONS[cav.language], so cpp simply omits them.) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cpp_class_diagram (inheritance + composes/aggregates/associates) + acceptance test</name>
  <files>lib_code_parser/extractors/evaluations/cpp_class_diagram.py, tests/acceptance/test_cpp_class_diagram.py</files>
  <read_first>
    - lib_code_parser/extractors/evaluations/class_diagram.py (inheritance edge emission L346-357; _classify_annotation/_collect_known_classes L97-172; sort-on-exit L359-366; edge-vocabulary docstring L15-18)
    - lib_code_parser/_cpp_cursor.py (field_relation classifier + _in_main_file + node_id)
    - lib_code_parser/models/evaluations/graph_base.py (GraphNode/GraphEdge/GraphModel/EdgeKind shapes)
    - .planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md section "cpp_class_diagram.py" (L169-199) + RESEARCH composes/aggregates rule (L291-299)
    - tests/acceptance/test_dia01_class_diagram.py (acceptance pattern: edge-spectrum assert + "uses" not in kinds)
  </read_first>
  <behavior>
    - extract(cav,config)->GraphModel: asserts TranslationUnit payload; collects known classes (CLASS_DECL/STRUCT_DECL in main file); emits one inherits edge per CXX_BASE_SPECIFIER child (multiple inheritance => multiple edges); emits composes/aggregates/associates edges from FIELD_DECL via _cpp_cursor.field_relation; builtin members emit no edge.
    - edge_type values are inherits/composes/aggregates/associates ONLY (this lib's vocabulary, not sibling spelling); "uses"/"other" never emitted.
    - Ends with the verbatim DET-04 sort tail; node_type="class".
  </behavior>
  <action>Write `cpp_class_diagram.py` mirroring `class_diagram.py`'s structure: assert payload type; build the `known_classes` set from main-file CLASS_DECL/STRUCT_DECL spellings (analog of `_collect_known_classes`); for each class, emit `GraphEdge(source=class_id, target=base_name, edge_type="inherits")` per CXX_BASE_SPECIFIER child (RESEARCH verified multiple inheritance), then for each FIELD_DECL call `_cpp_cursor.field_relation(field, known_classes)` and, when it returns `(relation, target)`, emit `GraphEdge(source=class_id, target=target, edge_type=relation)`. `None` (builtin) emits no edge. Build class `GraphNode(node_id, node_type="class", label=...)`. End with the verbatim sort tail (`node_ids = list(dict.fromkeys(...))`, sort nodes by node_id, sort edges by `(source,target,edge_type,label)`, `return GraphModel(...)`). Write the acceptance test driving `CodeParserExecutor().execute(..., inheritance/relations fixture bytes, "X.cpp")`, reading `result.content.class_diagram`, asserting the edge_type spectrum includes `{inherits, composes, aggregates, associates}` and `"uses" not in {e.edge_type for e in edges}`, and that multiple inheritance produced ≥2 inherits edges.</action>
  <verify>
    <automated>cd C:/work/agent_company/spec-reviewer-libs/lib-code-parser && python -m pytest tests/acceptance/test_cpp_class_diagram.py -x -q</automated>
  </verify>
  <done>cpp_class_diagram emits inherits (incl. multiple) + composes/aggregates/associates edges, no "uses"/"other", node_type="class", DET-04 sorted; acceptance test green through the public executor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cpp_component / cpp_sequence / cpp_package / cpp_state diagrams + EVALUATIONS['cpp'] registration</name>
  <files>lib_code_parser/extractors/evaluations/cpp_component_diagram.py, lib_code_parser/extractors/evaluations/cpp_sequence_diagram.py, lib_code_parser/extractors/evaluations/cpp_package_diagram.py, lib_code_parser/extractors/evaluations/cpp_state_diagram.py, lib_code_parser/_dispatch.py</files>
  <read_first>
    - lib_code_parser/extractors/evaluations/component_diagram.py (pull-a-primitive invariant #5 L27,51-64)
    - lib_code_parser/extractors/evaluations/package_diagram.py (containment-via-attributes L75-92, node_type="package", no contains edge)
    - lib_code_parser/extractors/evaluations/state_diagram.py (empty-output docstring L18-21 + DET-04 sort tail L109-114)
    - lib_code_parser/extractors/primitives/cpp_type_deps.py and cpp_callgraph.py (the primitives these pull)
    - .planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md sections cpp_component_diagram (L203-220), cpp_sequence_diagram (L224-228), cpp_package_diagram (L232-245), cpp_state_diagram (L249-253)
    - lib_code_parser/_dispatch.py (EVALUATIONS["cpp"] append-only sub-dict)
  </read_first>
  <behavior>
    - cpp_component_diagram pulls cpp_type_deps (invariant #5, its OWN primitive), filters kind=="imports", emits node_type="component" nodes + imports edges; DET-04 sort.
    - cpp_sequence_diagram pulls cpp_callgraph, emits linear calls edges (frame fidelity best-effort D-05; linear is the must-have); DET-04 sort.
    - cpp_package_diagram derives packages primarily from namespace nesting -> node_type="package" with attributes={"parent_package": parent} (NO contains edge); DET-04 sort.
    - cpp_state_diagram emits an EMPTY GraphModel (zero state nodes) - parity-as-empty-shape (A1/D-05); still ends with the DET-04 sort tail; module docstring documents the empty-output decision as best-effort.
    - All four assert TranslationUnit payload; none branch on cav.language.
    - EVALUATIONS["cpp"] gets the 5 keys (class_diagram from Task 1 + these 4) in canonical slot-name order.
  </behavior>
  <action>Write the four modules mirroring their Python siblings' structure. `cpp_component_diagram.py`: `from lib_code_parser.extractors.primitives import cpp_type_deps`; `tds = cpp_type_deps.extract(cav, config)`; filter `td.kind == "imports"`; build `node_type="component"` nodes + `GraphEdge(edge_type="imports")` edges; verbatim sort tail. `cpp_sequence_diagram.py`: pull `cpp_callgraph`, emit linear `GraphEdge(edge_type="calls")` per CallEdge (best-effort frames per D-05; linear is the must-have); sort tail. `cpp_package_diagram.py`: walk NAMESPACE cursors building dotted package ids, emit `GraphNode(node_type="package", attributes={"parent_package": parent} if parent else {})` (no contains edge — mirror package_diagram.py), sort tail. `cpp_state_diagram.py`: assert payload, then return an empty `GraphModel()` (zero nodes/edges) — document in the docstring that C++ has no portable deterministic FSM idiom mapping to the Python families, so v0.2.0 emits the empty parity shape (A1/D-05, best-effort); still apply the DET-04 sort tail over the empty lists for structural identity. In `_dispatch.py`, append `# noqa: E402` imports and register `EVALUATIONS["cpp"]["class_diagram"]`, `["sequence_diagram"]`, `["component_diagram"]`, `["package_diagram"]`, `["state_diagram"]` (canonical order; do NOT register function_spec/class_spec for cpp — those are Python-only and the executor walks EVALUATIONS[cav.language]).</action>
  <verify>
    <automated>cd C:/work/agent_company/spec-reviewer-libs/lib-code-parser && python -c "from lib_code_parser._dispatch import EVALUATIONS; assert set(EVALUATIONS['cpp'].keys())=={'class_diagram','sequence_diagram','component_diagram','package_diagram','state_diagram'}, list(EVALUATIONS['cpp'].keys()); import sys; sys.path.insert(0,'tests'); from conftest import build_cpp_cav; from lib_code_parser.extractors.evaluations import cpp_state_diagram; from lib_code_parser.models.infrastructure.config import ParserConfig; src=open('tests/fixtures/cpp/not_a_state_machine.cpp').read(); cav=build_cpp_cav(src,'not_a_state_machine.cpp'); c=ParserConfig(artifact_type='code',executor_lib='lib_code_parser',language='cpp'); gm=cpp_state_diagram.extract(cav,c); assert len(gm.nodes)==0, 'state diagram must be empty (parity-as-empty-shape A1)'; print('OK')"</automated>
  </verify>
  <done>All 4 diagrams emit their Python-sibling GraphModel slots; the state diagram is fixture-asserted empty (A1/D-05); EVALUATIONS["cpp"] holds the 5 diagram keys (no function_spec/class_spec) and passes the per-language slot guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LNG-04 schema-parity test + per-extractor 3-run determinism test</name>
  <files>tests/parity/test_cpp_python_schema_parity.py, tests/unit/frontends/test_cpp_determinism.py</files>
  <read_first>
    - tests/parity/test_v01_v02_compat.py (the structural-parity assertion analog)
    - lib_code_parser/models/infrastructure/artifact.py (CodeContent slot set — the shape both languages must share)
    - lib_code_parser/executor.py (execute returns NormalizedArtifact[CodeContent]; suffix override selects cpp)
    - .planning/phases/04-c-frontend-c-extractors/04-VALIDATION.md test map (LNG-04 parity + determinism rows L46,51)
  </read_first>
  <behavior>
    - The parity test runs execute() on a Python fixture and on a C++ fixture and asserts the two NormalizedArtifact.content objects expose the IDENTICAL set of CodeContent field names (same slots) and that each shared slot has the same Pydantic type (LNG-04 structural parity).
    - The determinism test runs each cpp extractor (or the full cpp execute) three times on the same fixture+config and asserts byte-identical model_dump_json() output (per-extractor; full-pipeline DET-01 snapshot is Phase 5).
  </behavior>
  <action>Write `test_cpp_python_schema_parity.py`: call `CodeParserExecutor().execute` once with a Python fixture (e.g. EXAMPLE_SOURCE / "x.py") and once with a C++ fixture ("x.cpp"); assert `set(py.content.model_fields.keys()) == set(cpp.content.model_fields.keys())` (same CodeContent slots) and that each shared field's annotated type matches (structural parity, LNG-04). Write `test_cpp_determinism.py`: run the cpp `execute` (or each cpp extractor) three times on the same fixture bytes + ParserConfig and assert all three `model_dump_json()` (or per-extractor `model_dump_json()`) outputs are byte-identical — proving DET-04 sort-on-exit absorbs libclang's nondeterministic cursor order (Pitfall 5). Note in a comment that the full-pipeline 3-fresh-subprocess DET-01 snapshot is Phase 5 scope, not this test.</action>
  <verify>
    <automated>cd C:/work/agent_company/spec-reviewer-libs/lib-code-parser && python -m pytest tests/parity/test_cpp_python_schema_parity.py tests/unit/frontends/test_cpp_determinism.py -x -q && python -m pytest --tb=short -q</automated>
  </verify>
  <done>The parity test proves cpp and Python share identical CodeContent slots/types (LNG-04); the determinism test proves cpp output is byte-identical across 3 runs; the full suite is green (no Python regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cpp CAV -> diagram extractors | Diagram extractors read the already-parsed TranslationUnit / pull cpp primitives; no new external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | EdgeKind closed Literal in diagram edges | mitigate | edge_type stays within the closed 11-value EdgeKind Literal (inherits/composes/aggregates/associates/imports/calls); "associates" is the explicit undecidable fallback. A fabricated "uses"/"other" would fail the Pydantic Literal validation — no silent vocabulary drift. |
| T-04-13 | Integrity | nondeterministic cursor traversal order | mitigate | Every diagram ends with DET-04 sort-on-exit; the 3-run determinism test gates against Pitfall 5 regressions. |
</threat_model>

<verification>
- 5 cpp diagrams emit Python-sibling GraphModel slots with DET-04 sort.
- State diagram empty (A1/D-05), fixture-asserted.
- LNG-04 structural parity + per-extractor determinism green; full suite green.
</verification>

<success_criteria>
- EVALUATIONS["cpp"] holds the 5 diagram keys (Python slot names); per-language slot guard passes.
- cpp NormalizedArtifact shape identical to Python (LNG-04).
- composes/aggregates/associates spectrum + multiple inheritance proven; no "uses" catch-all.
</success_criteria>

<output>
Create `.planning/phases/04-c-frontend-c-extractors/04-06-SUMMARY.md` when done.
</output>
