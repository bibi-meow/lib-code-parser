---
phase: 04-c-frontend-c-extractors
plan: 07
type: execute
wave: 6
depends_on: ["04-06"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [LNG-01, LNG-02]
nyquist_compliant: true

must_haves:
  truths:
    - "LNG-01: a MANDATORY CI matrix (no continue-on-error) runs pip install + import + the test suite on Linux x86_64, Linux aarch64, and Windows x86_64 across CPython 3.11/3.12/3.13/3.14 - all-green gates the phase"
    - "LNG-02: macOS arm64 x Python 3.13/3.14 runs with continue-on-error: true (best-effort observed, not gated) - graduated from the Phase 1 sp3-libclang-spike seed job"
    - "The matrix reuses the verified libclang smoke steps (Index.create(), Config.library_path assertion, minimal C++ parse) and runs the full pytest suite so the cpp track is exercised on every platform"
    - "Linux aarch64 uses a native arm64 runner label if available, with QEMU + manylinux2014_aarch64 documented as the fallback (A2 - exact label confirmed at implementation time)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "mandatory multi-platform/multi-Python matrix + best-effort macOS arm64 job"
      contains: "matrix"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "pip install -e .[dev] pulls libclang==18.1.1 (already pinned Phase 1)"
      pattern: "pip install -e"
---

<objective>
Graduate the CI from the single Phase-1 `sp3-libclang-spike` seed job to the full LNG-01 mandatory matrix (Linux x86_64/aarch64 + Windows x86_64 × CPython 3.11–3.14, all-green-gating) while keeping macOS arm64 × 3.13/3.14 as the LNG-02 best-effort `continue-on-error` job. Each matrix cell installs the lib (pulling the already-pinned `libclang==18.1.1`), runs the verified libclang smoke steps, and runs the full pytest suite so the cpp track is exercised on every platform.

Purpose: LNG-01/LNG-02 success criteria require the platform matrix to gate PR merges. This is the last Phase-4 plan because it depends on the cpp track existing and being importable (Waves 1–5).
Output: a single `ci.yml` with a mandatory matrix job + a best-effort macOS job.
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-c-frontend-c-extractors/04-RESEARCH.md
@.planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md

<interfaces>
<!-- Existing ci.yml has two jobs: `test` (ubuntu-latest, py3.11, pytest+ruff) and
     `sp3-libclang-spike` (macos-14, continue-on-error, py3.13/3.14, libclang smoke steps a-d). -->
<!-- The smoke steps to reuse (verified in the spike job):
       Index.create();  Config.library_path assertion (non-empty);  minimal C++ parse of 'int main(){return 0;}' -->
<!-- pyproject.toml already declares libclang==18.1.1 in [project.optional-dependencies] dev. -->
<!-- A2 (RESEARCH Open Question 2 / Assumption A2): GitHub Linux arm64 runner label
     (ubuntu-24.04-arm) availability is the only unconfirmed detail; QEMU + docker manylinux2014_aarch64
     is the documented fallback. CI YAML structure is Claude's Discretion per CONTEXT.md. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Graduate ci.yml to the LNG-01 mandatory matrix + LNG-02 best-effort macOS job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the file being modified — `test` job + `sp3-libclang-spike` smoke steps a-d to reuse)
    - .planning/phases/04-c-frontend-c-extractors/04-RESEARCH.md section "CI note" (L538, A2 runner label) + Environment Availability + section "State of the Art" (self-contained wheel, no system LLVM)
    - .planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md section ".github/workflows/ci.yml (MODIFY — mandatory CI matrix)" (L327-336)
    - pyproject.toml (confirm libclang==18.1.1 dev extra; no new install)
  </read_first>
  <action>Replace the single `test` job with a mandatory matrix job (NO `continue-on-error`): `strategy.fail-fast: false`, `matrix.python-version: ["3.11","3.12","3.13","3.14"]` × `matrix.os` covering Linux x86_64 (`ubuntu-latest`), Linux aarch64 (native `ubuntu-24.04-arm` runner label — A2; add a YAML comment noting the QEMU + `docker/setup-qemu-action` + `manylinux2014_aarch64` container fallback if the native label is unavailable, to be confirmed at implementation), and Windows x86_64 (`windows-latest`). Each cell: `actions/checkout@v4` → `actions/setup-python@v5` with `python-version: ${{ matrix.python-version }}` and `allow-prereleases: true` (for 3.14) → `pip install -e ".[dev]"` → `python -c "import lib_code_parser"` (LNG-01 import) → the three verified libclang smoke steps lifted from the spike job (Index.create(); assert `Config.library_path`; minimal C++ parse of `int main(){return 0;}`) → `pytest --tb=short` (full suite — exercises the cpp track on every platform) → `ruff check .` and `ruff format --check .` (keep the existing lint/format gates, on the Linux x86_64 / py3.11 cell or all cells per discretion). Keep/rename the `sp3-libclang-spike` job as the LNG-02 best-effort job: `runs-on: macos-14`, `continue-on-error: true`, `matrix.python-version: ["3.13","3.14"]`, same install + smoke steps + `pytest --tb=short`. Use a `matrix.os` list with `runs-on: ${{ matrix.os }}`. Do NOT add any system-LLVM install step — the libclang wheel is self-contained (RESEARCH State of the Art). Validate the YAML is well-formed before finishing.</action>
  <verify>
    <automated>cd C:/work/agent_company/spec-reviewer-libs/lib-code-parser && python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml',encoding='utf-8')); jobs=d['jobs']; mand=[j for j in jobs.values() if 'strategy' in j and not j.get('continue-on-error')]; assert mand, 'a mandatory matrix job must exist'; m=mand[0]['strategy']['matrix']; pys=m.get('python-version',[]); assert set(['3.11','3.12','3.13','3.14'])<=set(map(str,pys)), pys; best=[j for j in jobs.values() if j.get('continue-on-error')]; assert best and any('macos' in str(b.get('runs-on','')) for b in best), 'macOS best-effort job must be continue-on-error'; txt=open('.github/workflows/ci.yml',encoding='utf-8').read(); assert 'aarch64' in txt or 'arm' in txt.lower(), 'Linux aarch64 coverage'; assert 'pip install -e' in txt and 'Index.create()' in txt; print('OK')"</automated>
  </verify>
  <done>`ci.yml` parses as valid YAML with a mandatory matrix job (no continue-on-error) covering Linux x86_64/aarch64 + Windows x86_64 × Python 3.11–3.14 (install + import + libclang smoke + full pytest), a best-effort `continue-on-error: true` macOS arm64 job for 3.13/3.14, the Linux aarch64 fallback documented, and lint/format gates retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner pip install | CI installs the already-pinned `libclang==18.1.1` from PyPI; no new or unpinned package is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | CI `pip install -e ".[dev]"` (supply chain) | accept | The only third-party native dep is `libclang==18.1.1`, exact-pinned and approved in Phase 1 (RESEARCH Package Legitimacy Audit); no new package introduced by this plan, so no install-time legitimacy checkpoint is required. The exact pin + DET-02 import guard (04-03) reject any swapped ABI at runtime. |
| T-04-14 | Denial of Service | matrix cell hang on a flaky runner | accept | `fail-fast: false` isolates cells; the best-effort macOS job is `continue-on-error` so it never blocks. No untrusted input on CI. |
</threat_model>

<verification>
- `ci.yml` is valid YAML; mandatory matrix covers the three platforms × 4 Python versions; macOS arm64 stays best-effort.
- Every cell installs + imports + smoke-tests libclang + runs the full suite.
</verification>

<success_criteria>
- LNG-01 mandatory matrix gates merges (all-green required).
- LNG-02 macOS arm64 observed best-effort (continue-on-error).
- Linux aarch64 native-or-QEMU path documented (A2).
</success_criteria>

<output>
Create `.planning/phases/04-c-frontend-c-extractors/04-07-SUMMARY.md` when done.
</output>
