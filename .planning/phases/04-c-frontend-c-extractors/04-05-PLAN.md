---
phase: 04-c-frontend-c-extractors
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - lib_code_parser/models/primitives/contracts.py
  - lib_code_parser/extractors/primitives/cpp_contracts.py
  - lib_code_parser/_dispatch.py
  - docs/09-extending.md
  - tests/acceptance/test_cpp_doxygen_contracts.py
  - tests/parity/test_trc03_cpp_parity.py
autonomous: true
requirements: [SPC-03, TRC-03, LNG-04]
nyquist_compliant: true

must_haves:
  truths:
    - "D-08: SourceKind Literal gains the additive value 'doxygen' (single value; pre/post/invariant discriminated by the existing ContractEntry.kind); the existing 4 values are neither deleted nor renamed; docs/09-extending.md records 'additive Literal extension allowed, deletion/rename forbidden' (same policy as EdgeKind MAJOR)"
    - "D-09: C++ Doxygen contracts reuse the existing CodeContent.contracts slot (no new field) and emit the SAME ContractInfo/ContractEntry schema as Python (parity)"
    - "SPC-03: \\pre/@pre -> ContractEntry(kind='precondition'), \\post/@post -> 'postcondition', \\invariant/@invariant -> 'invariant', all with source_kind='doxygen', read off the EXACT decl cursor (Pitfall 4 - no textual-proximity inference)"
    - "TRC-03 parity: Traces: REQ-ID,US-NN extraction is byte-identical for Python docstrings and C++ Doxygen comments (proven by a parity test feeding the same tag text through both paths)"
  artifacts:
    - path: "lib_code_parser/models/primitives/contracts.py"
      provides: "additive SourceKind 'doxygen' value"
      contains: "doxygen"
    - path: "lib_code_parser/extractors/primitives/cpp_contracts.py"
      provides: "Doxygen ContractInfo extraction (regex on raw_comment)"
      contains: "ContractInfo"
    - path: "lib_code_parser/_dispatch.py"
      provides: "PRIMITIVES['cpp']['contracts'] registration"
      contains: "PRIMITIVES[\"cpp\"][\"contracts\"]"
    - path: "tests/acceptance/test_cpp_doxygen_contracts.py"
      provides: "SPC-03 acceptance test through CodeParserExecutor.execute"
    - path: "tests/parity/test_trc03_cpp_parity.py"
      provides: "TRC-03 Python/C++ trace-tag parity test"
  key_links:
    - from: "lib_code_parser/extractors/primitives/cpp_contracts.py"
      to: "lib_code_parser/_cpp_cursor.py"
      via: "verbatim TRC-03 extract_trace_tags"
      pattern: "_cpp_cursor|extract_trace_tags"
    - from: "lib_code_parser/_dispatch.py"
      to: "lib_code_parser/extractors/primitives/cpp_contracts.py"
      via: "PRIMITIVES['cpp']['contracts'] = extract"
      pattern: "PRIMITIVES\\[\"cpp\"\\]\\[\"contracts\"\\]"
---

<objective>
Implement Doxygen-driven C++ contract extraction (SPC-03): add the additive `SourceKind="doxygen"` value (D-08), build `cpp_contracts.py` that maps `\pre`/`\post`/`\invariant` (both `\` and `@` forms) read off the exact decl cursor's `raw_comment` into the EXISTING `ContractInfo`/`ContractEntry` schema reusing the `CodeContent.contracts` slot (D-09), and register `PRIMITIVES["cpp"]["contracts"]`. Prove Python/C++ symmetry: same contract schema (acceptance test) and byte-identical `Traces:` extraction (parity test, TRC-03).

Purpose: The verifier (spec_code_verifier) must process Python and C++ contracts symmetrically; that only holds if C++ emits the same schema into the same slot. D-08's additive Literal is the minimal model touch (the only model change in Phase 4).
Output: additive SourceKind, cpp_contracts module, contracts registration, docs/09 additive-Literal policy, acceptance + TRC-03 parity tests.
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-c-frontend-c-extractors/04-RESEARCH.md
@.planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md
@.planning/phases/04-c-frontend-c-extractors/04-CONTEXT.md

<interfaces>
<!-- SourceKind (models/primitives/contracts.py L26-31) - append "doxygen" only, delete/rename nothing: -->
SourceKind = Literal[
    "pydantic_validator", "pydantic_model_validator",
    "pydantic_field_validator", "dataclass_post_init",
    "doxygen",          # ADDITIVE - D-08
]
<!-- ContractKind already = Literal["precondition","invariant","postcondition"] - \post maps to "postcondition" cleanly, NO ContractKind change. -->
<!-- ContractEntry(name, source_kind, kind, decorator_name="", line_no=0); ContractInfo(node_id="", entries=[]) -->

<!-- Doxygen regex (RESEARCH verified, both \ and @ forms): -->
_DOXY_RE = re.compile(r"[\\@](pre|post|invariant)\b[ \t]*(.*)", re.IGNORECASE)
<!-- pre->precondition, post->postcondition, invariant->invariant -->

<!-- Pitfall 4: read raw_comment on the EXACT decl cursor being emitted (function/method/class),
     NEVER infer from a namespace's leading comment. -->
<!-- Reuse _cpp_cursor.extract_trace_tags (byte-identical TRC-03 regex) for Traces: parity. -->
<!-- Python contracts extractor returns dict[str, ContractInfo] keyed by class node_id; mirror that shape. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive SourceKind 'doxygen' + docs/09 additive-Literal policy</name>
  <files>lib_code_parser/models/primitives/contracts.py, docs/09-extending.md</files>
  <read_first>
    - lib_code_parser/models/primitives/contracts.py (SourceKind Literal L26-31 + ContractEntry/ContractInfo + ContractKind)
    - docs/09-extending.md (the EdgeKind MAJOR-policy section is the wording template)
    - .planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md section "models/primitives/contracts.py" (L309-323) + RESEARCH section "SourceKind extension" (L331-345)
  </read_first>
  <action>In `contracts.py`, append `"doxygen"` as the FINAL value of the `SourceKind` Literal — do not delete, reorder, or rename the existing four values. Add an inline comment `# ADDITIVE - D-08 (single value; ContractEntry.kind discriminates pre/post/invariant)`. Make NO change to `ContractKind`, `ContractEntry`, or `ContractInfo` (their shapes already cover everything). In `docs/09-extending.md`, add a short note in the additive-extension policy area stating that `SourceKind` Literal additions follow the same rule already documented for `EdgeKind`: additive values are allowed (no MAJOR bump for an additive provenance value), but deleting or renaming an existing value is forbidden / a breaking change. Mirror the existing EdgeKind-policy wording.</action>
  <verify>
    <automated>cd C:/work/agent_company/spec-reviewer-libs/lib-code-parser && python -c "from lib_code_parser.models.primitives.contracts import SourceKind, ContractEntry; import typing; vals=typing.get_args(SourceKind); assert vals[-1]=='doxygen' and 'pydantic_validator' in vals and len(vals)==5, vals; e=ContractEntry(name='f', source_kind='doxygen', kind='precondition'); assert e.source_kind=='doxygen'; print('OK')" && python -c "s=open('docs/09-extending.md',encoding='utf-8').read(); assert 'SourceKind' in s and ('additive' in s.lower() or 'additive 拡張' in s); print('OK')"</automated>
  </verify>
  <done>`SourceKind` has 5 values ending in `"doxygen"`; the original 4 are intact; a `ContractEntry(source_kind="doxygen", ...)` constructs; docs/09 records the additive-Literal policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cpp_contracts extractor (Doxygen regex on raw_comment) + PRIMITIVES['cpp']['contracts']</name>
  <files>lib_code_parser/extractors/primitives/cpp_contracts.py, lib_code_parser/_dispatch.py</files>
  <read_first>
    - lib_code_parser/extractors/primitives/contracts.py (per-class ContractInfo aggregation shape L143,157-201; the dict-keyed-by-class-id return)
    - lib_code_parser/_cpp_cursor.py (main-file filter + verbatim extract_trace_tags + qualified_node_id)
    - .planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md section "cpp_contracts.py" (L135-165) + RESEARCH section "Doxygen Contract Extraction" + Pitfall 4 (comment-to-cursor association)
    - lib_code_parser/_dispatch.py (PRIMITIVES["cpp"] append-only)
  </read_first>
  <behavior>
    - cpp_contracts.extract(cav,config)->dict[str, ContractInfo]: asserts TranslationUnit payload; for each main-file decl cursor (function/method/class) it reads THAT cursor's raw_comment (Pitfall 4 - never the enclosing namespace's comment), runs _DOXY_RE, and emits ContractEntry(name=<decl spelling>, source_kind="doxygen", kind=<precondition|postcondition|invariant>, line_no=<decl line>).
    - Mapping: \pre/@pre->precondition, \post/@post->postcondition, \invariant/@invariant->invariant (case-insensitive).
    - Returns the same dict[node_id, ContractInfo] shape as the Python contracts extractor; node_id via qualified_node_id.
    - Sorted/deterministic emit; reuses _cpp_cursor.extract_trace_tags for any Traces: tags (TRC-03 parity).
    - PRIMITIVES["cpp"]["contracts"] registered.
  </behavior>
  <action>Write `cpp_contracts.py` mirroring the OUTPUT shape of `contracts.py` (dict[node_id, ContractInfo] of ContractEntry lists) — but the PROVENANCE machinery is Doxygen, not decorator-alias resolution (that Python machinery does NOT carry over). Define `_DOXY_RE = re.compile(r"[\\@](pre|post|invariant)\b[ \t]*(.*)", re.IGNORECASE)`. Walk main-file decl cursors (FUNCTION_DECL/CXX_METHOD/CLASS_DECL/STRUCT_DECL via `_cpp_cursor._in_main_file`); for each, read `cursor.raw_comment or ""` ON THAT EXACT CURSOR (Pitfall 4), find all `_DOXY_RE` matches, map the marker word to `ContractKind` (`pre`->`precondition`, `post`->`postcondition`, `invariant`->`invariant`), and build `ContractEntry(name=cursor.spelling, source_kind="doxygen", kind=<mapped>, line_no=<cursor.extent.start.line>)`. Group entries under `qualified_node_id(cursor)` into `ContractInfo(node_id=..., entries=...)`. Reuse the verbatim `_cpp_cursor.extract_trace_tags` if trace tags are emitted alongside (for TRC-03 parity). Emit deterministically (stable iteration + sort entries if needed). In `_dispatch.py`, append the `# noqa: E402` import + `PRIMITIVES["cpp"]["contracts"] = _extract_cpp_contracts`. Note: the executor already gates the contracts primitive on `config.extract_contracts` by the shared `"contracts"` slot name — no executor change needed.</action>
  <verify>
    <automated>cd C:/work/agent_company/spec-reviewer-libs/lib-code-parser && python -c "import sys; sys.path.insert(0,'tests'); from conftest import build_cpp_cav; from lib_code_parser.extractors.primitives import cpp_contracts; from lib_code_parser.models.infrastructure.config import ParserConfig; src=open('tests/fixtures/cpp/doxygen_contracts.cpp').read(); cav=build_cpp_cav(src,'doxygen_contracts.cpp'); c=ParserConfig(artifact_type='code',executor_lib='lib_code_parser',language='cpp',extract_contracts=True); res=cpp_contracts.extract(cav,c); kinds={e.kind for ci in res.values() for e in ci.entries}; sk={e.source_kind for ci in res.values() for e in ci.entries}; assert {'precondition','postcondition','invariant'} <= kinds, kinds; assert sk=={'doxygen'}, sk; from lib_code_parser._dispatch import PRIMITIVES; assert 'contracts' in PRIMITIVES['cpp']; print('OK')"</automated>
  </verify>
  <done>cpp_contracts emits precondition/postcondition/invariant ContractEntry with source_kind="doxygen" off the exact decl cursor, in the dict[node_id, ContractInfo] shape; registered under PRIMITIVES["cpp"]["contracts"].</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SPC-03 acceptance test + TRC-03 Python/C++ parity test</name>
  <files>tests/acceptance/test_cpp_doxygen_contracts.py, tests/parity/test_trc03_cpp_parity.py</files>
  <read_first>
    - tests/acceptance/test_dia01_class_diagram.py (drives the public CodeParserExecutor.execute surface; uses a .cpp/.h path so the suffix-override selects cpp)
    - lib_code_parser/executor.py (execute signature + suffix override to cpp)
    - lib_code_parser/extractors/primitives/functions.py (Python TRC-03 path - the parity reference)
    - .planning/phases/04-c-frontend-c-extractors/04-PATTERNS.md section "Test files" (acceptance + parity L350-367)
  </read_first>
  <action>Write `test_cpp_doxygen_contracts.py` driving the PUBLIC surface: `CodeParserExecutor().execute(config_with_extract_contracts_True, doxygen_fixture_bytes, "doxygen_contracts.cpp")` (the `.cpp` suffix makes the executor select the cpp track), then read `result.content.contracts` and assert it contains a ContractInfo whose entries cover {precondition, postcondition, invariant} all with `source_kind == "doxygen"`, and that the schema (ContractInfo/ContractEntry field set) is identical to what the Python contracts path produces (D-09 parity). Write `test_trc03_cpp_parity.py` proving TRC-03 byte-identical behavior: take a single tag string `"Traces: REQ-9, US-3"`, feed it through the Python `functions._extract_trace_tags` (or the shared `_cpp_cursor.extract_trace_tags`) AND through the C++ path (build a cpp CAV from a fixture whose Doxygen comment carries the same `Traces:` line, run cpp_contracts/cpp_functions, extract the tags), and assert the resulting `TraceTag.refs` lists are equal — demonstrating the regex produces identical output for a Python docstring and a C++ Doxygen comment.</action>
  <verify>
    <automated>cd C:/work/agent_company/spec-reviewer-libs/lib-code-parser && python -m pytest tests/acceptance/test_cpp_doxygen_contracts.py tests/parity/test_trc03_cpp_parity.py -x -q</automated>
  </verify>
  <done>The acceptance test proves SPC-03 through the public executor (3 Doxygen kinds, source_kind="doxygen", same schema as Python); the parity test proves the `Traces:` regex yields byte-identical `TraceTag.refs` for Python docstrings and C++ Doxygen comments (TRC-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doxygen comment text (from parsed source) -> regex | Comment text originates in the parsed source (already in-process). The regex extracts marker conditions; it never evaluates them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | additive SourceKind Literal | mitigate | The new value is additive-only; `extra="forbid"` Pydantic config + the closed Literal still reject unknown source_kind values, so a typo'd provenance fails loudly. Existing values untouched (no silent schema drift). |
| T-04-11 | Integrity | comment-to-cursor misassociation | mitigate | Read `raw_comment` on the exact decl cursor being emitted (Pitfall 4), never inferring from textual proximity, so contracts attach to the correct node. |
</threat_model>

<verification>
- SourceKind additive value 'doxygen'; original 4 intact.
- cpp_contracts emits the same ContractInfo/ContractEntry schema into the shared contracts slot.
- SPC-03 acceptance + TRC-03 parity green.
</verification>

<success_criteria>
- Doxygen \pre/\post/\invariant -> ContractEntry(source_kind="doxygen") in the existing schema.
- Python/C++ contract symmetry proven through the public executor.
- TRC-03 byte-identical extraction proven.
</success_criteria>

<output>
Create `.planning/phases/04-c-frontend-c-extractors/04-05-SUMMARY.md` when done.
</output>
