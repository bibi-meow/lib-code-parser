---
phase: 02-python-frontend-ast-primitives-acl-2-adapters
plan: 07
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - lib_code_parser/ast_extractor.py
  - lib_code_parser/callgraph_builder.py
  - lib_code_parser/contract_extractor.py
  - lib_code_parser/type_dep_builder.py
  - lib_code_parser/__init__.py
  - lib_code_parser/models/__init__.py
  - lib_code_parser/adapters/__init__.py
  - tests/acceptance/test_fr01_function_extraction.py
  - tests/acceptance/test_fr02_callgraph.py
  - tests/acceptance/test_fr03_type_deps.py
  - tests/acceptance/test_fr04_contracts.py
  - tests/acceptance/test_fr05_trace_tags.py
  - tests/acceptance/test_fr06_disabled.py
  - tests/parity/test_v01_v02_compat.py
  - tests/parity/test_trc_02_docstring.py
  - tests/parity/test_snapshot_v01_fixture.py
  - tests/parity/fixtures/v01_snapshot.json
autonomous: true
requirements: [AST-01, AST-02, AST-03, AST-04, AST-05, DET-03, DET-04, TRC-02, TRC-03]
must_haves:
  truths:
    - "v0.1.0 legacy 4 ファイル (ast_extractor.py / callgraph_builder.py / contract_extractor.py / type_dep_builder.py) are git-rm'd; `ls lib_code_parser/*.py` shows zero matches (D-01 clean break)"
    - "lib_code_parser/__init__.py exposes ParserConfig from the TYPED path (lib_code_parser.models.infrastructure.config) — the barrel-level ParserConfig is now the typed v0.2.0 variant; the v0.1.0 stub in lib_code_parser/models/__init__.py is deleted (D-01 / D-02 — explicit break, documented in CONTEXT.md)"
    - "lib_code_parser barrel exposes PyrightAdapter / PyrightOutput / PyrightDiagnostic as v0.2.0 additions on the canonical lib import surface (`from lib_code_parser import PyrightAdapter` succeeds)"
    - "tests/parity/test_v01_v02_compat.py is restructured per D-04: name surface 13 + no-duplication grep gate are PRESERVED; stub-based JSON byte-identical parity is DROPPED; the new shipped-v0.1.0-fixture snapshot test is ADDED"
    - "tests/parity/test_trc_02_docstring.py is shipped per VALIDATION.md Wave 0 list: grep-based gate that asserts every extractor module under lib_code_parser/extractors/ and lib_code_parser/frontends/ contains an `Implements: AST-NN` line in its module docstring"
    - "tests/parity/test_snapshot_v01_fixture.py + tests/parity/fixtures/v01_snapshot.json are shipped per D-04 + VALIDATION.md: the conftest EXAMPLE_SOURCE fixture is run through typed ParserConfig + dispatch-driven executor and the resulting NormalizedArtifact JSON is compared byte-identically with the captured snapshot file"
    - "All 6 acceptance tests (tests/acceptance/test_fr01..06) are rewritten to consume the typed ParserConfig + dispatch-driven executor through the v0.2.0 public surface (`from lib_code_parser import CodeParserExecutor, ParserConfig`) — no legacy `from lib_code_parser.ast_extractor import extract_functions` style imports remain"
    - "Full test suite green: `pytest tests/ -x -q` exits 0 — Phase 2 end-state where Phase 1's 187-test baseline is replaced with the v0.2.0 equivalent (legacy-stub-dependent tests are now typed-ParserConfig-driven)"
  artifacts:
    - path: "tests/parity/test_v01_v02_compat.py"
      provides: "Redesigned parity gate per D-04: name surface 13 + no-duplication + snapshot pointer"
      contains: "test_v01_caller_surface_intact|test_no_duplicate_module_name_helper"
    - path: "tests/parity/test_trc_02_docstring.py"
      provides: "TRC-02 grep gate — every extractor module under lib_code_parser/extractors/ + frontends/ has Implements: AST-NN docstring"
      contains: "def test_all_extractor_modules"
    - path: "tests/parity/test_snapshot_v01_fixture.py"
      provides: "D-04 shipped v0.1.0 fixture snapshot — byte-identical against tests/parity/fixtures/v01_snapshot.json"
      contains: "def test_v01_fixture_snapshot"
    - path: "tests/parity/fixtures/v01_snapshot.json"
      provides: "Captured NormalizedArtifact JSON for conftest EXAMPLE_SOURCE — generated by Phase 2 executor; locks output drift for Phase 3+"
      contains: "artifact_id|content"
  key_links:
    - from: "tests/parity/test_snapshot_v01_fixture.py"
      to: "tests/parity/fixtures/v01_snapshot.json"
      via: "byte-identical model_dump_json comparison"
      pattern: "v01_snapshot\\.json"
    - from: "tests/parity/test_trc_02_docstring.py"
      to: "lib_code_parser/extractors/ and lib_code_parser/frontends/"
      via: "grep-based static gate for Implements: AST-NN"
      pattern: "Implements:"
---

<objective>
Wave 3 sequential closer for Phase 2. Wave 1+2 で確立した v0.2.0 全機能が稼働している前提で、 D-01 「Clean break」 を完遂する:

1. **v0.1.0 legacy 4 ファイル削除**: `lib_code_parser/{ast_extractor,callgraph_builder,contract_extractor,type_dep_builder}.py` を `git rm` で削除。 これらは Plan 02-06 で executor から import 解除済、 acceptance tests (test_fr01..06) が依然 `from lib_code_parser.ast_extractor import extract_functions` 系の legacy path を使っているため、 削除時点で test が壊れる → 同 Task で acceptance test 6 件を rewrite する。

2. **barrel ParserConfig graduation** (D-01 後半): `lib_code_parser/models/__init__.py` の v0.1.0 ParserConfig 並列定義 (Phase 1 Plan 09 で残した v0.1.0 stub class、 SUMMARY-09 §Known Stubs に明記) を **削除**。 barrel `lib_code_parser.ParserConfig` を typed 版 (`lib_code_parser.models.infrastructure.config.ParserConfig`) に re-export 切り替え。 これは v0.1.0 caller の dict-style API (`ParserConfig(params={...})`) を **明示的に break** する (D-02 explicit break + PROJECT.md §Constraints の互換性破壊明示要件を CONTEXT.md D-02 が満たす)。

3. **barrel に PyrightAdapter family を expose**: v0.2.0 public surface の 6 名 (Phase 1 で確立した `CAV, EdgeKind, GraphNode, GraphEdge, GraphModel, GuardExpr`) に加えて、 Plan 02-05 deliverable の 3 名 (`PyrightAdapter, PyrightOutput, PyrightDiagnostic`) を `lib_code_parser/__init__.py` `__all__` に追加 + adapters/__init__.py の re-export を拡張。 Phase 2 v0.2.0 surface = 13 (v0.1.0) + 6 (Phase 1 v0.2.0) + 3 (Phase 2 v0.2.0) = 22 名。

4. **parity test 再設計** (D-04): `tests/parity/test_v01_v02_compat.py` を 3 軸で書き換え:
   - **保持**: name surface importability gate (13 v0.1.0 + 9 v0.2.0 = 22 名)、 no-duplication `_get_module_name` grep gate
   - **廃止**: stub-based `test_normalized_artifact_json_byte_identical` (D-04 — JSON byte-identical parity は新 snapshot test に置き換え、 D-06 自体は維持されるが parity test の form は snapshot に move)、 `test_executor_runs_on_example_source` (legacy `b"def foo(): pass"` 形式; typed ParserConfig + snapshot 経由に統合)
   - **新規**: snapshot test pointer + ParserConfig typed graduation 後の constraint test (v0.1.0 dict-style `ParserConfig(params={...})` を渡すと `ValidationError` raise する旨を assert — D-02 explicit break のテストレベル文書化)

5. **新規 parity test 2 件追加** (VALIDATION.md Wave 0 必須):
   - `tests/parity/test_trc_02_docstring.py` — RESEARCH §6 + VALIDATION.md 必須。 `lib_code_parser/extractors/` + `lib_code_parser/frontends/` 配下のすべての `.py` ファイル (`__init__.py` 除く) で module docstring に `Implements: AST-NN` (NN は数字) パターンが存在することを subprocess grep で assert
   - `tests/parity/test_snapshot_v01_fixture.py` + `tests/parity/fixtures/v01_snapshot.json` — D-04 + VALIDATION.md 必須。 conftest `EXAMPLE_SOURCE` を typed ParserConfig + dispatch executor で実行した `NormalizedArtifact.model_dump_json()` を fixture file と byte-identical 比較。 fixture file は **本 plan で初回生成** (実 pyright 起動環境で executor を 1 回回し、 出力 JSON を `tests/parity/fixtures/v01_snapshot.json` に書き出して commit)。 環境依存性 (pyright install) は `_has_pyright()` skip で扱う

6. **acceptance test 6 件 rewrite**: 既存 `tests/acceptance/test_fr01..06_*.py` を typed ParserConfig + 公開 API 経由に書き換え。 `from lib_code_parser import CodeParserExecutor, ParserConfig` (typed) を使い、 `extract_functions / build_callgraph / extract_contracts / build_type_deps` の v0.1.0 legacy direct call は **全廃**。 各 acceptance test の assertion は v0.1.0 fixture parity 維持を意図 (RESEARCH §7.1-7.5 baseline + Phase 2 で改良された source_kind per-entry / TypeDep.resolved を反映):
   - test_fr01 — FunctionNode 抽出 assertion (v0.1.0 と同 node_id 形式 / kind / params / docstring / source_range)
   - test_fr02 — CallGraph DET-04 sort 後の `(caller, callee)` 順 (v0.1.0 と完全 parity ではなく、 sort 後の順序で assertion)
   - test_fr03 — TypeDep 抽出 + `.resolved` field 検証 (clean source なら全 True)
   - test_fr04 — ContractInfo per-entry source_kind 検証 (D-12 (β) restructure 反映: `entries[0].source_kind == "pydantic_field_validator"` 等)、 v0.1.0 `.preconditions` / `.invariants` list[str] interface は `@computed_field` 経由で動く (Plan 02-04 deliverable)
   - test_fr05 — TraceTag regex (`Traces: REQ-ID, US-NN`) の v0.1.0 verbatim parity
   - test_fr06 — disabled (config.enabled=False) 早期 return + empty CodeContent (v0.1.0 parity 維持)

7. **Phase 2 acceptance test の文書: SCH-02 `extra="forbid"` 互換性**: ContractEntry / TypeDep の additive field 追加が SCH-02 を破らないこと、 Pydantic v2 computed_field の JSON 出力含有性が D-04 snapshot 安定性を満たすこと、 を rationale として SUMMARY に記録。

Purpose: Phase 2 全 ROADMAP success criteria + 全 phase requirements (AST-01..05, DET-03, TRC-02, TRC-03) の最終 close。 D-01 clean break + D-02 explicit break + D-04 parity test 再設計の完遂。 Phase 3 entry 前の v0.2.0 baseline (legacy なし + typed barrel + snapshot test 確立) を固定。

Output:
- 4 legacy files DELETED via `git rm`
- 3 source files MODIFIED (barrel `__init__.py`, models barrel, adapters barrel)
- 6 acceptance files REWRITTEN
- 1 existing parity file REWRITTEN (`test_v01_v02_compat.py`)
- 2 new parity files CREATED (`test_trc_02_docstring.py`, `test_snapshot_v01_fixture.py`)
- 1 new fixture file CREATED (`tests/parity/fixtures/v01_snapshot.json`)
</objective>

<execution_context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/workflows/execute-plan.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/PROJECT.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/REQUIREMENTS.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/ROADMAP.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/phases/02-python-frontend-ast-primitives-acl-2-adapters/02-CONTEXT.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/phases/02-python-frontend-ast-primitives-acl-2-adapters/02-RESEARCH.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/phases/02-python-frontend-ast-primitives-acl-2-adapters/02-VALIDATION.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/phases/01-architecture-foundation-spec-correction/01-09-SUMMARY.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/codebase/CONVENTIONS.md
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/lib_code_parser/__init__.py
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/lib_code_parser/models/__init__.py
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/lib_code_parser/adapters/__init__.py
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/parity/test_v01_v02_compat.py
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/acceptance/test_fr01_function_extraction.py
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/acceptance/test_fr02_callgraph.py
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/acceptance/test_fr03_type_deps.py
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/acceptance/test_fr04_contracts.py
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/acceptance/test_fr05_trace_tags.py
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/acceptance/test_fr06_disabled.py
@C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/conftest.py

<interfaces>
<!-- Plan 02-06 deliverables that Plan 02-07 consumes:
- _dispatch.FRONTENDS["python"] / PRIMITIVES x4 populated
- executor.py is dispatch-driven, uses typed ParserConfig
- ContractInfo merger logic preserved

Plan 02-05 deliverables exposed on barrel:
- adapters.pyright.PyrightAdapter / PyrightOutput / PyrightDiagnostic

Phase 1 Plan 09 known stub being graduated:
- lib_code_parser/models/__init__.py defines a local v0.1.0 ParserConfig class
  (params: dict[str, object], no extra="forbid") for executor body compatibility.
  Plan 02-06 removed the executor's dependency on this stub. Plan 02-07 now
  removes the stub itself.
-->

<!-- Final v0.2.0 v0.1.0 surface (22 names):

v0.1.0 compat (13, ORDER PRESERVED):
    CodeParserExecutor, ArtifactId, CallEdge, CallGraph, CodeContent,
    ContractInfo, FunctionNode, NormalizedArtifact, ParamInfo, ParserConfig,
    SourceRange, TraceTag, TypeDep

v0.2.0 Phase 1 additions (6):
    CAV, EdgeKind, GraphEdge, GraphModel, GraphNode, GuardExpr

v0.2.0 Phase 2 additions (3):
    PyrightAdapter, PyrightOutput, PyrightDiagnostic

NOTE: ContractEntry (Plan 02-04 introduction) is NOT in the public barrel —
it is the storage class for ContractInfo.entries; callers consume it via
ContractInfo.entries[i].source_kind, no need for direct barrel exposure.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete legacy 4 files + rewrite 6 acceptance tests for typed ParserConfig + dispatch executor</name>
  <read_first>
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/lib_code_parser/ast_extractor.py (削除対象 — 既存 import path をすべて acceptance test から取り除く前提)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/lib_code_parser/callgraph_builder.py (削除対象)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/lib_code_parser/contract_extractor.py (削除対象)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/lib_code_parser/type_dep_builder.py (削除対象)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/acceptance/test_fr01..06_*.py (現状すべて v0.1.0 direct call、 typed ParserConfig + 公開 API 経由に書き換える)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/phases/02-python-frontend-ast-primitives-acl-2-adapters/02-RESEARCH.md (§7.1-§7.5 + §7.6 fixture matrix; 各 acceptance test の write-back guidance)
  </read_first>
  <behavior>
    - `ls lib_code_parser/ast_extractor.py lib_code_parser/callgraph_builder.py lib_code_parser/contract_extractor.py lib_code_parser/type_dep_builder.py 2>/dev/null` が exit 1 + 全 4 ファイル absent
    - `grep -rn "from lib_code_parser.ast_extractor\|from lib_code_parser.callgraph_builder\|from lib_code_parser.contract_extractor\|from lib_code_parser.type_dep_builder" tests/` が 0 件 (acceptance + unit いずれにも legacy import path が残っていない)
    - 6 acceptance test がすべて `from lib_code_parser import CodeParserExecutor, ParserConfig` (typed) を使い、 `CodeParserExecutor().execute(config, raw_bytes, path)` 経由で結果を取得
    - acceptance test の assertion は RESEARCH §7.1-§7.5 baseline に整合:
      - test_fr01 (FR-01): FunctionNode 抽出 = v0.1.0 ID 形式 + kind + skip_self_cls + return_type + docstring + source_range
      - test_fr02 (FR-02): CallGraph edges が `(caller, callee)` lex sorted (DET-04)、 `create_order` → `_calculate_total` edge 存在
      - test_fr03 (FR-03): TypeDep 抽出 + `.resolved` field 存在 (clean source なので全 True、 ただし `_has_pyright()` で skip 可能)
      - test_fr04 (FR-04): ContractInfo.entries に validate_status (precondition / pydantic_field_validator) + check_total (invariant / pydantic_model_validator); `.preconditions` helper property は ["validate_status"]、 `.invariants` は ["check_total"] (Plan 02-04 backward-compat shim)
      - test_fr05 (FR-05): TraceTag 抽出が v0.1.0 と byte-identical (`OrderService` docstring `Traces: US-01, FR-02` から `[TraceTag(tag="Traces", refs=["US-01", "FR-02"])]`)
      - test_fr06 (FR-06): `ParserConfig(artifact_type="code", executor_lib="lib_code_parser", enabled=False)` で結果が空 CodeContent
    - 6 acceptance すべて `pytest tests/acceptance/ -x -q` で pass (test_fr03 は環境依存で skip 可能)
  </behavior>
  <action>
    Step 1 — 4 legacy ファイルを `git rm`:
    ```
    git rm lib_code_parser/ast_extractor.py
    git rm lib_code_parser/callgraph_builder.py
    git rm lib_code_parser/contract_extractor.py
    git rm lib_code_parser/type_dep_builder.py
    ```

    Step 2 — 6 acceptance test を rewrite。 各 test ファイルの新 template:

    `tests/acceptance/test_fr01_function_extraction.py`:
    ```
    """FR-01: Function and class extraction acceptance tests (Phase 2 v0.2.0 form)."""

    from __future__ import annotations

    import pytest

    from lib_code_parser import CodeParserExecutor, ParserConfig
    from tests.conftest import EXAMPLE_SOURCE, EXAMPLE_PATH


    @pytest.fixture
    def executor_result():
        config = ParserConfig(artifact_type="code", executor_lib="lib_code_parser")
        exe = CodeParserExecutor()
        return exe.execute(config, EXAMPLE_SOURCE.encode("utf-8"), EXAMPLE_PATH)


    def _nodes_by_id(executor_result) -> dict:
        return {fn.node_id: fn for fn in executor_result.content.functions}


    class TestClassExtraction:
        def test_class_node_extracted(self, executor_result) -> None:
            nodes = _nodes_by_id(executor_result)
            assert "order_service.OrderService" in nodes

        def test_class_kind(self, executor_result) -> None:
            nodes = _nodes_by_id(executor_result)
            assert nodes["order_service.OrderService"].kind == "class"

        def test_model_class_extracted(self, executor_result) -> None:
            nodes = _nodes_by_id(executor_result)
            assert "order_service.OrderModel" in nodes

        def test_class_docstring(self, executor_result) -> None:
            nodes = _nodes_by_id(executor_result)
            doc = nodes["order_service.OrderService"].docstring
            assert "Order management service" in doc


    class TestMethodExtraction:
        # ... 同様 v0.1.0 assertion を executor_result.content.functions 経由に書き換え ...


    class TestTopLevelFunctionExtraction:
        # ... 同様 ...
    ```

    test_fr02 / test_fr03 / test_fr04 / test_fr05 / test_fr06 も同 pattern で書き換える。 特に注意点:

    - test_fr02 の `TestCallGraphEdges` で edges 順序を assert する場合、 sort 後の `(caller, callee)` lex 順を前提とする。 例: v0.1.0 acceptance では `edges[0]` が `(OrderService.create_order, _calculate_total)` だったかもしれないが、 sort 後は別 edge が先頭 — set 化して in 検査に書き換える:
      ```
      def test_create_order_calls_calculate_total(self, executor_result):
          edges = executor_result.content.call_graph.edges
          edge_pairs = {(e.caller, e.callee) for e in edges}
          assert ("order_service.OrderService.create_order", "_calculate_total") in edge_pairs
      ```

    - test_fr03 は実 pyright 必要 → ファイル先頭で `_has_pyright()` helper を定義し、 module-level `pytestmark = pytest.mark.skipif(not _has_pyright(), reason="pyright not installed")` で全 test を skip 可能にする。 (pyright が install 済みなら resolved=True で全件、 not installed の場合は skip)
      別 path: test 全件を skip するのは過剰なので、 「実 pyright 起動 path を使う test」と「mock 経由で resolved を assertion する test」を 2 分離し、 後者は環境非依存にする選択肢もあるが、 acceptance test は end-to-end 性が要点なので前者 (skip 可能 marker) を採用。

    - test_fr04 は Plan 02-04 の ContractInfo restructure を反映:
      ```
      def test_field_validator_is_precondition(self, executor_result):
          contracts = executor_result.content.contracts
          ci = contracts["order_service.OrderModel"]
          # v0.1.0 backward-compat helper:
          assert "validate_status" in ci.preconditions
          # v0.2.0 per-entry verification:
          entry = next(e for e in ci.entries if e.name == "validate_status")
          assert entry.source_kind == "pydantic_field_validator"
          assert entry.kind == "precondition"
      ```

    - test_fr05 (TraceTag parity): `executor_result.content.functions` 内の `OrderService` 等の `.trace_tags` を assert (v0.1.0 と同 form)

    - test_fr06 (disabled): `ParserConfig(artifact_type="code", executor_lib="lib_code_parser", enabled=False)` で `.content.functions == []` and `.content.call_graph.edges == []` and `.content.type_deps == []`

    全 acceptance test で `from lib_code_parser.ast_extractor import ...` のような legacy path を **削除**。 unit tests (`tests/unit/test_ast_extractor.py` 等) も同じ legacy path を使っているはずなので、 Phase 1 で確立した Phase 1 unit suite と本 plan の関係を整理: Phase 1 unit `tests/unit/test_*.py` の中で legacy extractor を import する test は本 plan で **削除** (legacy ファイルが消えるため import error)。 該当 unit:
    - `tests/unit/test_ast_extractor.py` — 削除 (Plan 02-02 unit が代替)
    - `tests/unit/test_callgraph_builder.py` — 削除 (Plan 02-03 unit が代替)
    - `tests/unit/test_type_dep_builder.py` — 削除 (Plan 02-06 unit が代替)
    - `tests/unit/test_contract_extractor.py` — 削除 (Plan 02-04 unit が代替)

    削除する unit は `git rm` する。 重複削除を Task 2 / Task 3 と分担しないよう、 本 Task 1 で完結させる。
  </action>
  <verify>
    <automated>cd "C:/work/agent_company/spec-reviewer-libs/lib-code-parser" &amp;&amp; pytest tests/acceptance/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/acceptance/ -x -q` exit 0 (6 acceptance すべて pass、 test_fr03 は環境依存で skip 可能)
    - `ls lib_code_parser/ast_extractor.py lib_code_parser/callgraph_builder.py lib_code_parser/contract_extractor.py lib_code_parser/type_dep_builder.py 2>&1 | grep -c "No such"` >= 4 (POSIX) もしくは Windows 環境では `Get-Item` の error が 4 件 — 簡略化のため Python チェックを使う: `python -c "from pathlib import Path; import sys; sys.exit(0 if not any(Path(f'lib_code_parser/{n}.py').exists() for n in ['ast_extractor','callgraph_builder','contract_extractor','type_dep_builder']) else 1)"` exit 0
    - `grep -rn "from lib_code_parser.ast_extractor\|from lib_code_parser.callgraph_builder\|from lib_code_parser.contract_extractor\|from lib_code_parser.type_dep_builder" tests/ lib_code_parser/` が 0 件 (legacy import path 全廃)
    - `ls tests/unit/test_ast_extractor.py tests/unit/test_callgraph_builder.py tests/unit/test_type_dep_builder.py tests/unit/test_contract_extractor.py 2>&1 | grep -c "No such"` >= 4 (legacy unit 削除済)
    - ARC-04 no-duplication grep gate (Phase 1 SC-3) 引き続き 1 件: `grep -rn -E "^def _get_module_name|^def get_module_name" lib_code_parser/ --include="*.py" | wc -l` が 1 (`_paths.py`)
    - acceptance test 6 件すべてが `from lib_code_parser import CodeParserExecutor, ParserConfig` を import: `grep -c "from lib_code_parser import CodeParserExecutor, ParserConfig" tests/acceptance/test_fr*.py` が 6 件 (1 per file)
    - `ruff check tests/acceptance/` exit 0
  </acceptance_criteria>
  <done>v0.1.0 legacy 4 source ファイル + 4 legacy unit ファイルが削除。 6 acceptance test が typed ParserConfig + 公開 API 経由に rewrite。 ARC-04 / DET-04 single source 不変条件は依然 1 件 (_paths.py)。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Graduate barrel ParserConfig + expose PyrightAdapter family on lib + Phase 1 parity test redesign (D-04)</name>
  <read_first>
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/lib_code_parser/__init__.py (現状: 19 名 barrel; v0.2.0 Phase 2 additions 3 名追加 + ParserConfig 切替)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/lib_code_parser/models/__init__.py (現状: v0.1.0 ParserConfig stub class 定義; 本 plan で削除 + typed re-export)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/lib_code_parser/adapters/__init__.py (Phase 1 で `run_subprocess` / `SubprocessAdapter` re-export 済; Plan 02-05 deliverable の 3 名を追加)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/parity/test_v01_v02_compat.py (Phase 1 で確立した 11 件; D-04 のとおり再設計)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/phases/01-architecture-foundation-spec-correction/01-09-SUMMARY.md (v0.1.0 ParserConfig parity stub の deviation 1 詳細 — 本 task で graduation する根拠)
  </read_first>
  <behavior>
    - `from lib_code_parser import ParserConfig` が typed ParserConfig (`extra="forbid"`、 typed `language` / `extract_contracts` / `python_version` / `compile_args` field) を返す
    - `ParserConfig(artifact_type="code", executor_lib="lib_code_parser", params={"language": "python"})` が `ValidationError` を raise (D-02 explicit break)
    - `ParserConfig(artifact_type="code", executor_lib="lib_code_parser")` (typed の最小構築) が成功し、 default は `enabled=True, language="python", extract_contracts=True, python_version="3.12", compile_args=["-std=c++17"]`
    - `from lib_code_parser import PyrightAdapter, PyrightOutput, PyrightDiagnostic` 成功
    - `from lib_code_parser.adapters import PyrightAdapter` 成功 (adapters barrel 経由)
    - `lib_code_parser.__all__` が **22 名** (13 v0.1.0 + 6 Phase 1 v0.2.0 + 3 Phase 2 v0.2.0); 順序 = (1) v0.1.0 13、 (2) Phase 1 v0.2.0 6、 (3) Phase 2 v0.2.0 3
    - `lib_code_parser.models.ParserConfig` が typed (旧 v0.1.0 stub class は削除済) — 同一 object: `lib_code_parser.ParserConfig is lib_code_parser.models.infrastructure.config.ParserConfig`
    - 既存 `tests/parity/test_v01_v02_compat.py` の構造変化 (D-04):
      - **保持**: `test_v01_caller_surface_intact`、 `test_v02_new_surface_present` (6 名)、 `test_version_bumped`、 `test_no_duplicate_module_name_helper` (Phase 1 SC-3 hard gate)、 `test_normalized_artifact_unparameterized_works`、 `test_normalized_artifact_parameterized_works` (D-06 generic 互換性は維持)、 `test_lib_code_parser_module_is_a_package_not_a_module`
      - **廃止 / 書き換え**: `test_normalized_artifact_json_byte_identical` → 削除 (新 snapshot test で代替); `test_executor_runs_on_example_source` → 削除 (新 snapshot test で代替); `test_parser_config_unknown_field_raises` → typed v0.2.0 graduation 後の barrel form に rewrite (`from lib_code_parser import ParserConfig` を使う、 infrastructure path から import するのは廃止 — 同 object なので意味は同じだが、 graduation を明示的に test); `test_edge_kind_rejects_uses` は維持
      - **新規**: `test_v02_phase2_surface_present` (PyrightAdapter / PyrightOutput / PyrightDiagnostic の 3 名 importability)、 `test_v01_params_dict_breaks` (D-02 explicit break: `ParserConfig(..., params={"language": "python"})` が `ValidationError` を raise)
  </behavior>
  <action>
    Step 1 — `lib_code_parser/models/__init__.py` を編集: v0.1.0 ParserConfig stub class 定義を **完全削除** (Plan 01-09 で残した `class ParserConfig(BaseModel): artifact_type / executor_lib / params / enabled` の class 全体)。 代わりに `from lib_code_parser.models.infrastructure.config import ParserConfig` を import 行に追加 (Phase 1 ではこれを意図的に避けていた、 本 task で実施)。

    更新後の models/__init__.py 関連箇所:
    ```
    # Phase 1 で skip された Plan 03 deliverable ParserConfig を Phase 2 で graduation
    from lib_code_parser.models.infrastructure.config import ParserConfig

    # v0.1.0 ParserConfig stub class を削除 (D-01 clean break)
    # 削除: class ParserConfig(BaseModel): artifact_type / executor_lib / params / enabled
    ```

    `__all__` リスト内の `"ParserConfig"` 位置は維持 (infrastructure section に存在)。 docstring から「ParserConfig 並行 stub」の rationale 説明を **削除** (graduation 後は無効化された情報)。

    Step 2 — `lib_code_parser/__init__.py` を編集:

    既存 imports に 3 名追加 (PyrightAdapter / PyrightOutput / PyrightDiagnostic、 abstract 段階で):
    ```
    from lib_code_parser.adapters.pyright import (
        PyrightAdapter,
        PyrightDiagnostic,
        PyrightOutput,
    )
    ```

    既存 imports は維持 (models 経由でも resolve 可能; ParserConfig は typed object に変わる、 import 行は同じ)。

    docstring 内の「ParserConfig parity note (Phase 1):」 段落を **削除**、 代わりに「Phase 2 graduation: barrel ParserConfig は typed v0.2.0 (lib_code_parser.models.infrastructure.config) に切替。 v0.1.0 dict-style API `params={...}` は明示的に break (D-02)」 と書き換える。

    `__all__` リストの末尾に Phase 2 v0.2.0 additions section を追加:
    ```
    __all__ = [
        # v0.1.0 compat — ORDER PRESERVED
        "CodeParserExecutor", "ArtifactId", "CallEdge", "CallGraph", "CodeContent",
        "ContractInfo", "FunctionNode", "NormalizedArtifact", "ParamInfo", "ParserConfig",
        "SourceRange", "TraceTag", "TypeDep",
        # v0.2.0 Phase 1 additions
        "CAV", "EdgeKind", "GraphEdge", "GraphModel", "GraphNode", "GuardExpr",
        # v0.2.0 Phase 2 additions
        "PyrightAdapter", "PyrightOutput", "PyrightDiagnostic",
    ]
    ```

    Step 3 — `lib_code_parser/adapters/__init__.py` を編集 (Phase 1 で `run_subprocess` / `SubprocessAdapter` re-export 済) 末尾に追加:
    ```
    from lib_code_parser.adapters.pyright import (
        PyrightAdapter,
        PyrightDiagnostic,
        PyrightOutput,
    )

    __all__ = [
        "run_subprocess",
        "SubprocessAdapter",
        "PyrightAdapter",
        "PyrightOutput",
        "PyrightDiagnostic",
    ]
    ```

    Step 4 — `tests/parity/test_v01_v02_compat.py` を D-04 のとおり再設計。 完全書き換えではなく、 以下の section level 変更:

    削除する test 関数 (3 件):
    - `test_normalized_artifact_json_byte_identical` — `test_snapshot_v01_fixture.py` の包括的 snapshot test に置換
    - `test_executor_runs_on_example_source` — 同 snapshot test に置換
    - `test_parser_config_unknown_field_raises` — typed v0.2.0 graduation 後の barrel test に置換 (新 `test_parser_config_typed_at_barrel_rejects_unknown`)

    新規追加 test 関数 (3 件):
    1. `test_v02_phase2_surface_present`:
       ```
       def test_v02_phase2_surface_present() -> None:
           """All 3 v0.2.0 Phase 2 additions importable from the top-level barrel."""
           from lib_code_parser import PyrightAdapter, PyrightOutput, PyrightDiagnostic
           for name, obj in {
               "PyrightAdapter": PyrightAdapter,
               "PyrightOutput": PyrightOutput,
               "PyrightDiagnostic": PyrightDiagnostic,
           }.items():
               assert obj is not None, f"v0.2.0 Phase 2 name '{name}' resolved to None"
       ```
    2. `test_parser_config_typed_at_barrel_rejects_unknown`:
       ```
       def test_parser_config_typed_at_barrel_rejects_unknown() -> None:
           """D-01 / D-02: barrel-level ParserConfig is now the typed v0.2.0 variant."""
           from lib_code_parser import ParserConfig
           with pytest.raises(ValidationError):
               ParserConfig(
                   artifact_type="code",
                   executor_lib="lib_code_parser",
                   surprise=1,  # type: ignore[call-arg]
               )
       ```
    3. `test_v01_params_dict_explicit_break`:
       ```
       def test_v01_params_dict_explicit_break() -> None:
           """D-02 explicit break: v0.1.0 dict-style ParserConfig API raises ValidationError."""
           from lib_code_parser import ParserConfig
           with pytest.raises(ValidationError):
               ParserConfig(
                   artifact_type="code",
                   executor_lib="lib_code_parser",
                   params={"language": "python"},  # type: ignore[call-arg]
               )
       ```

    維持する test (変更不要、 Phase 1 設計のまま):
    - `test_v01_caller_surface_intact` — 13 名 importability (graduation 後も ParserConfig が import 可能のため pass、 ただし object は typed に変わる)
    - `test_v02_new_surface_present` — Phase 1 v0.2.0 6 名
    - `test_version_bumped` — `"0.2.0"`
    - `test_no_duplicate_module_name_helper` — Phase 1 SC-3 hard gate
    - `test_normalized_artifact_unparameterized_works` / `test_normalized_artifact_parameterized_works` — D-06 generic 互換性
    - `test_edge_kind_rejects_uses` — SCH-03
    - `test_lib_code_parser_module_is_a_package_not_a_module` — defensive

    結果として `test_v01_v02_compat.py` 内の test 関数数は: 11 件 (Phase 1) - 3 件削除 + 3 件追加 = 11 件 (Phase 2 closer)
  </action>
  <verify>
    <automated>cd "C:/work/agent_company/spec-reviewer-libs/lib-code-parser" &amp;&amp; pytest tests/parity/test_v01_v02_compat.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/parity/test_v01_v02_compat.py -x -q` exit 0 with 11 件 pass
    - `python -c "from lib_code_parser import ParserConfig; from lib_code_parser.models.infrastructure.config import ParserConfig as Typed; assert ParserConfig is Typed"` exit 0 (barrel graduation の identity 検証)
    - `python -c "from lib_code_parser import PyrightAdapter, PyrightOutput, PyrightDiagnostic; assert PyrightAdapter.__name__ == 'PyrightAdapter'"` exit 0
    - `python -c "import lib_code_parser; assert 'PyrightAdapter' in lib_code_parser.__all__ and 'PyrightOutput' in lib_code_parser.__all__ and 'PyrightDiagnostic' in lib_code_parser.__all__"` exit 0
    - `python -c "from lib_code_parser import ParserConfig; ParserConfig(artifact_type='code', executor_lib='lib_code_parser', params={'language':'python'})"` が ValidationError で exit 非 0 (D-02 explicit break の動的検証)
    - `grep -c "class ParserConfig(BaseModel)" lib_code_parser/models/__init__.py` が 0 (旧 stub class 削除済)
    - `grep -c "from lib_code_parser.models.infrastructure.config import ParserConfig" lib_code_parser/models/__init__.py` が 1 (typed graduation 経路)
    - `grep -c "PyrightAdapter" lib_code_parser/__init__.py` >= 1
    - `grep -c "PyrightAdapter" lib_code_parser/adapters/__init__.py` >= 1
    - Phase 1 SC-3 hard gate 引き続き 1 件: `grep -rn -E "^def _get_module_name|^def get_module_name" lib_code_parser/ --include="*.py" | wc -l` が 1
    - フル baseline: `pytest tests/ -x -q` exit 0 (acceptance + parity + unit すべて green、 Plan 02-07 完了時点で test_fr03 / test_snapshot の `_has_pyright()` skip を除く全 test pass)
    - `ruff check lib_code_parser/ tests/parity/test_v01_v02_compat.py` exit 0
  </acceptance_criteria>
  <done>barrel ParserConfig が typed v0.2.0 に graduation。 PyrightAdapter family が barrel + adapters barrel 両方から expose。 D-04 のとおり parity test 再設計 (3 件削除 + 3 件追加 = 11 件 total)。 D-02 explicit break が unit でロック。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: New parity tests — TRC-02 docstring grep gate + D-04 v0.1.0 fixture snapshot + initial snapshot generation</name>
  <read_first>
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/phases/02-python-frontend-ast-primitives-acl-2-adapters/02-VALIDATION.md (Wave 0 必須: `tests/parity/test_trc_02_docstring.py` と `tests/parity/test_snapshot_v01_fixture.py` + fixture)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/.planning/phases/02-python-frontend-ast-primitives-acl-2-adapters/02-RESEARCH.md (§6 docstring template + §7.6 fixture snapshot rationale)
    - C:/work/agent_company/spec-reviewer-libs/lib-code-parser/tests/conftest.py (`EXAMPLE_SOURCE` を snapshot test の入力に使う)
  </read_first>
  <behavior>
    `tests/parity/test_trc_02_docstring.py`:
    1. `test_all_extractor_modules_declare_implements_req_id` — subprocess `["grep", "-rn", "-E", r"^Implements: (AST|DET|TRC|SCH|ARC)-\d+", "lib_code_parser/extractors/", "lib_code_parser/frontends/", "lib_code_parser/adapters/pyright.py", "--include=*.py"]` を実行し、 期待される全 module (`extractors/primitives/functions.py` / `callgraph.py` / `type_deps.py` / `contracts.py` / `frontends/python.py` / `adapters/pyright.py` = 6 files) で `Implements:` 行が **少なくとも 1 件ずつ存在** することを assert
    2. `test_all_extractor_modules_have_traces_line` — 同範囲で `Traces:` regex の v0.1.0 verbatim form (`r"Traces:\s*([A-Z]+-\d+(?:\s*,\s*[A-Z]+-\d+)*)"`) でマッチが 6 file 各 1 件以上発生することを assert (TRC-03 grep gate)
    3. `test_no_implements_in_non_extractor_files` — `lib_code_parser/_paths.py` や `lib_code_parser/_dispatch.py` のような non-extractor file には `Implements: AST-NN` がないことを assert (誤宣言防止、 TRC-02 適用範囲の明確化)

    `tests/parity/test_snapshot_v01_fixture.py`:
    1. `test_v01_fixture_snapshot_byte_identical` — conftest `EXAMPLE_SOURCE` を `CodeParserExecutor().execute(typed_config, source.encode(), EXAMPLE_PATH)` で実行、 `result.model_dump_json(indent=2, sort_keys=True)` を `tests/parity/fixtures/v01_snapshot.json` と byte-identical 比較。 ただし `_has_pyright()` skip
    2. `test_v01_fixture_snapshot_deterministic_across_3_runs` — 同 source を 3 回 execute して JSON 出力が 3 回とも equal (DET-01 byte-identical 前提の deterministic 性確認)

    `tests/parity/fixtures/v01_snapshot.json`:
    本 task 内で実 pyright を起動して 1 回 executor を回し、 出力 JSON を fixture file に書き出して commit。 fixture 生成スクリプトは production code に commit しない (`scripts/` 配下の throwaway script で OK)、 fixture 自体だけが repo に残る。
  </behavior>
  <action>
    Step 1 — `tests/parity/test_trc_02_docstring.py` を新規作成。 上記 3 件の test を実装。 `_REPO_ROOT` helper は `test_ast_05_one_parse.py` と同 form。

    ```
    from __future__ import annotations
    import subprocess
    from pathlib import Path

    _REPO_ROOT = Path(__file__).resolve().parent.parent.parent
    _EXPECTED_EXTRACTOR_FILES = [
        "lib_code_parser/extractors/primitives/functions.py",
        "lib_code_parser/extractors/primitives/callgraph.py",
        "lib_code_parser/extractors/primitives/type_deps.py",
        "lib_code_parser/extractors/primitives/contracts.py",
        "lib_code_parser/frontends/python.py",
        "lib_code_parser/adapters/pyright.py",
    ]


    def _grep(pattern: str, target: str) -> list[str]:
        result = subprocess.run(
            ["grep", "-rn", "-E", pattern, str(_REPO_ROOT / target), "--include=*.py"],
            check=False, capture_output=True, text=True,
        )
        return [line for line in result.stdout.splitlines() if line.strip()]


    def test_all_extractor_modules_declare_implements_req_id() -> None:
        """TRC-02: every extractor / frontend / pyright adapter declares Implements: REQ-ID."""
        for target in _EXPECTED_EXTRACTOR_FILES:
            matches = _grep(r"^Implements: (AST|DET|TRC|SCH|ARC)-\d+", target)
            assert matches, f"TRC-02 violation: no `Implements: AST-NN` line in {target}"


    def test_all_extractor_modules_have_traces_line() -> None:
        """TRC-03: every extractor / frontend / pyright adapter has Traces: REQ-ID line."""
        for target in _EXPECTED_EXTRACTOR_FILES:
            matches = _grep(r"Traces:\s*[A-Z]+-\d+", target)
            assert matches, f"TRC-03 violation: no `Traces: ...` line in {target}"


    def test_no_implements_in_non_extractor_files() -> None:
        """TRC-02 scope: Implements: AST-NN should NOT appear in non-extractor files."""
        # _paths.py / _dispatch.py / executor.py / models/ are NOT extractor modules
        for target in ["lib_code_parser/_paths.py", "lib_code_parser/_dispatch.py"]:
            matches = _grep(r"^Implements: AST-\d+", target)
            assert not matches, (
                f"Unexpected `Implements: AST-NN` in non-extractor file {target}: {matches}"
            )
    ```

    Step 2 — fixture file `tests/parity/fixtures/v01_snapshot.json` を生成。 throwaway script:
    ```
    # scripts/generate_v01_snapshot.py (本 plan で commit、 ただし生成済 fixture が source of truth)
    """Generate tests/parity/fixtures/v01_snapshot.json from conftest EXAMPLE_SOURCE.

    Run once with pyright installed:
        python scripts/generate_v01_snapshot.py

    Commits the resulting JSON as the byte-identical snapshot baseline. Re-run
    only when intentionally updating the snapshot (with rationale in plan docs).
    """
    from pathlib import Path
    from lib_code_parser import CodeParserExecutor, ParserConfig
    from tests.conftest import EXAMPLE_SOURCE, EXAMPLE_PATH

    def main() -> None:
        config = ParserConfig(artifact_type="code", executor_lib="lib_code_parser")
        exe = CodeParserExecutor()
        result = exe.execute(config, EXAMPLE_SOURCE.encode("utf-8"), EXAMPLE_PATH)
        json_out = result.model_dump_json(indent=2)
        target = Path(__file__).parent.parent / "tests/parity/fixtures/v01_snapshot.json"
        target.parent.mkdir(parents=True, exist_ok=True)
        target.write_text(json_out, encoding="utf-8")
        print(f"Wrote {target}: {len(json_out)} chars")

    if __name__ == "__main__":
        main()
    ```

    `scripts/generate_v01_snapshot.py` を実行して `tests/parity/fixtures/v01_snapshot.json` を初回生成。 fixture 内容は executor の dispatch walk + 4 primitive output + PyrightOutput経由の resolved annotation の確定 JSON。 生成された fixture を git add + commit。

    fixture 生成不可な環境 (pyright 未 install) では `scripts/generate_v01_snapshot.py` を実行できず、 fixture file が存在しない状態で Task が止まる。 この場合の workaround: Plan execution は pyright が available な環境 (本 plan は executor が pyright を稼働させる前提) で実行することを前提とする。 もし executor 実行環境で pyright が install されていなければ、 Plan 実装者は本 Task の完了条件として「fixture 生成成功 + commit」を満たさない可能性があり、 plan-checker / executor が re-plan を要求する。

    Step 3 — `tests/parity/test_snapshot_v01_fixture.py` を新規作成:

    ```
    """D-04 snapshot test: shipped v0.1.0 EXAMPLE_SOURCE fixture byte-identical comparison.

    Locks the Phase 2 dispatch-driven executor output for the conftest fixture
    so that any future drift (Phase 3+ primitive additions, sort changes, model
    field changes) is caught at PR-time.
    """

    from __future__ import annotations

    import json
    import subprocess
    from pathlib import Path

    import pytest

    from lib_code_parser import CodeParserExecutor, ParserConfig
    from tests.conftest import EXAMPLE_SOURCE, EXAMPLE_PATH

    _SNAPSHOT_PATH = Path(__file__).parent / "fixtures" / "v01_snapshot.json"


    def _has_pyright() -> bool:
        try:
            subprocess.run(
                ["pyright", "--version"],
                capture_output=True, timeout=10.0, check=False,
            )
            return True
        except (FileNotFoundError, subprocess.TimeoutExpired):
            return False


    pytestmark = pytest.mark.skipif(
        not _has_pyright() or not _SNAPSHOT_PATH.exists(),
        reason="pyright not installed or snapshot fixture missing",
    )


    def _execute() -> str:
        config = ParserConfig(artifact_type="code", executor_lib="lib_code_parser")
        exe = CodeParserExecutor()
        result = exe.execute(config, EXAMPLE_SOURCE.encode("utf-8"), EXAMPLE_PATH)
        return result.model_dump_json(indent=2)


    def test_v01_fixture_snapshot_byte_identical() -> None:
        """D-04 byte-identical: executor output equals captured snapshot."""
        actual = _execute()
        expected = _SNAPSHOT_PATH.read_text(encoding="utf-8")
        assert actual == expected, (
            f"v0.1.0 fixture snapshot drift detected.\n"
            f"Run scripts/generate_v01_snapshot.py to update if intentional.\n"
            f"First 200 chars actual: {actual[:200]!r}\n"
            f"First 200 chars expected: {expected[:200]!r}"
        )


    def test_v01_fixture_snapshot_deterministic_across_3_runs() -> None:
        """DET-01 prerequisite: executor output is byte-identical across re-runs."""
        outputs = [_execute() for _ in range(3)]
        assert outputs[0] == outputs[1] == outputs[2], (
            "Determinism broken: executor output varies across 3 consecutive runs."
        )
    ```
  </action>
  <verify>
    <automated>cd "C:/work/agent_company/spec-reviewer-libs/lib-code-parser" &amp;&amp; pytest tests/parity/test_trc_02_docstring.py tests/parity/test_snapshot_v01_fixture.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/parity/test_trc_02_docstring.py -x -q` exit 0 with 3 件 pass
    - `pytest tests/parity/test_snapshot_v01_fixture.py -x -q` exit 0 (実 pyright 起動環境では 2 件 pass、 skip 環境でも fail しない)
    - fixture file 存在: `[ -f tests/parity/fixtures/v01_snapshot.json ]` exit 0; `wc -c < tests/parity/fixtures/v01_snapshot.json` > 100 (内容あり)
    - `scripts/generate_v01_snapshot.py` 存在 + 実行可能
    - `grep -c "Implements: AST" lib_code_parser/extractors/primitives/functions.py` >= 1 (Plan 02-02 deliverable; TRC-02 grep gate と整合)
    - 全 TRC-02 grep gate 適用 file (6 files) で `Implements:` 行 + `Traces:` 行が存在
    - `pytest tests/ -x -q` exit 0 with **acceptance + parity + unit すべて green** (Plan 02-07 完了時点の最終 baseline)
    - VALIDATION.md sign-off の `nyquist_compliant: true` 条件達成: 全 task に automated verify あり、 6 Wave 0 file すべて charge close、 sampling continuity OK
    - `ruff check tests/parity/` exit 0
  </acceptance_criteria>
  <done>VALIDATION.md Wave 0 必須リスト 6 件 (test_ast_05_one_parse / test_trc_02_docstring / test_snapshot_v01_fixture + fixture / test_pyright_adapter / test_callgraph_sort + 02-04 contracts redesign) が Phase 2 全 plan 横断で charge close。 D-04 parity test 再設計完了。 Phase 2 全 ROADMAP success criteria + 全 phase requirements (8 件) 達成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v0.1.0 caller dict-style ParserConfig → typed v0.2.0 ParserConfig | D-02 explicit break. ValidationError raise で fail loudly |
| snapshot fixture (JSON file) → Phase 2 executor output | byte-identical comparison; fixture drift = test failure (intentional or PR-blocking) |
| legacy 4 file deletion → external caller import path | 外部 caller が `from lib_code_parser.ast_extractor import extract_functions` を書いていた場合 ImportError。 v0.2.0 は internal lib + Phase 1 D-01 / D-02 で明示 break、 外部 caller の手当ては Deferred Idea (CONTEXT.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-36 | Tampering | barrel graduation で typed object と barrel symbol が異なる object になる | mitigate | Task 2 acceptance に identity 検証 (`ParserConfig is Typed`) |
| T-02-37 | Tampering | snapshot fixture file が pyright off 環境で生成失敗 | mitigate | Task 3 で `_has_pyright()` skip + `_SNAPSHOT_PATH.exists()` skip の二重防御。 fixture 生成 script (`scripts/generate_v01_snapshot.py`) を含めて、 環境再現可能性を担保 |
| T-02-38 | Tampering | 既存外部 caller の legacy import が ImportError で壊れる | accept | PROJECT.md Constraints の互換性破壊明示要件を CONTEXT.md D-02 が満たす。 v0.3.0+ で compat shim を検討 (CONTEXT.md Deferred Ideas) |
| T-02-39 | Tampering | TRC-02 grep gate が新 file の追加で false positive (例: docstring に "Implements" を持つが extractor でない module) | mitigate | Task 3 test #3 (`test_no_implements_in_non_extractor_files`) で _paths.py / _dispatch.py 等の non-extractor file に Implements: AST-NN がないことを assert |
| T-02-40 | Tampering | Pydantic `@computed_field` の出力が `model_dump_json` で predictable でなくなり snapshot drift | accept | Pydantic v2 仕様で computed_field の field 名は alphabetical でなく declaration order; declaration order が安定する限り snapshot は安定。 declaration order を変更する PR は snapshot 更新を伴う (rationale を SUMMARY に書く運用) |
| T-02-41 | Supply chain | snapshot generation script `scripts/generate_v01_snapshot.py` が依存追加 | accept | スクリプトは Phase 2 既存依存 (stdlib + Phase 1 / 2 deliverables) のみ使用 |
</threat_model>

<verification>
- `pytest tests/ -x -q` exit 0 — Phase 2 最終全 suite green (acceptance 6 件 + unit 全件 + parity 16 件 ± skip)
- 全 ROADMAP Phase 2 success criteria が test レベルで実証 (SC-1 = AST-05 + functions extract, SC-2 = callgraph sort + pyright resolved annotation, SC-3 = ContractInfo per-entry source_kind, SC-4 = 4 extractor isolated import)
- 全 phase requirements (AST-01..05 / DET-03 / TRC-02 / TRC-03) が unit + acceptance + parity で satisfaction signal を出している
- ARC-04 / DET-04 hard gate (Phase 1 SC-3): 1 件 def (`_paths.py`)
- TRC-02 grep gate: 6 extractor module 全部に `Implements:` 行
- TRC-03 regex gate: 6 extractor module 全部に `Traces:` 行
- D-01 clean break: 4 legacy file absent
- D-02 explicit break: dict-style ParserConfig → ValidationError test 通過
- D-04 parity test redesign: test_v01_v02_compat.py 11 件 + test_trc_02_docstring.py 3 件 + test_snapshot_v01_fixture.py 2 件
- DET-03 grep gate (`PYRIGHT_PYTHON_FORCE_VERSION` + `"1.1.409"`) 維持
- v0.2.0 barrel surface: 22 名 (13 v0.1.0 + 6 Phase 1 v0.2.0 + 3 Phase 2 v0.2.0) + identity (barrel == infrastructure path)
- `ruff check lib_code_parser/ tests/` exit 0
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 1 ~ 4 すべて test レベルで rolling green
- D-01 clean break 完遂 (legacy 4 ファイル削除 + ParserConfig typed graduation + stub 撤廃)
- D-02 explicit break が parity test で文書化
- D-04 parity test 再設計完了 (3 軸: name surface + no-duplication + fixture snapshot)
- VALIDATION.md Wave 0 必須 6 file すべて charge close、 sign-off の `nyquist_compliant: true` 条件達成可能
- Phase 3 entry 前の v0.2.0 baseline 確定 (legacy なし + typed barrel + snapshot 確立)
</success_criteria>

<output>
Create `.planning/phases/02-python-frontend-ast-primitives-acl-2-adapters/02-07-SUMMARY.md` when done. Include:
1. pytest output (Phase 2 全 phase 完了時点の全 test 集計)
2. 削除した legacy ファイル list (4 source + 4 unit = 8 file)
3. barrel ParserConfig graduation の確認 (identity + ValidationError)
4. 新 parity test 3 件の追加内容 + 削除 3 件の rationale
5. fixture `v01_snapshot.json` の生成結果 (size + 含有内容の概要)
6. ROADMAP Phase 2 success criteria 4 件のチェックボックス状態
7. 全 8 phase requirements (AST-01..05 / DET-03 / TRC-02 / TRC-03) のカバレッジ表
8. Phase 3 entry condition (Plan 02-07 完了 = Phase 2 close、 Phase 3 plan-phase 可能)
</output>
